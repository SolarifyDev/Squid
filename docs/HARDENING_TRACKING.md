# Squid Hardening — Findings Tracking

**Generated**: 2026-04-24 from cross-layer audit (5 parallel sub-agents against Squid + OctopusDeploy/OctopusTentacle source).
**Combined with**: 2026-04-23 Tentacle-runtime deep-scan (24 findings, separate table).
**Total live findings**: ~85 (≈20 P0, ≈30 P1, ≈35 P2).

## Legend

| Symbol | Meaning |
|---|---|
| 🔴 **P0** | Security / data corruption / process hang / service outage — fix immediately |
| 🟠 **P1** | Reliability / silent-failure / resource leak — fix before next release |
| 🟡 **P2** | Quality / UX / observability — fix when opportunity |
| 🏔️ **ARCH** | Architectural alignment with Octopus — multi-week effort, plan separately |

## Status column

| Status | Meaning |
|---|---|
| ⬜ | pending — not started |
| 🔄 | in progress — actively being worked on |
| ✅ | fixed — followed by commit SHA |
| 🚫 | won't fix — followed by rationale |
| 🔀 | deferred to architectural-alignment phase |

---

## Section 1 — Crypto & Secrets (🔴 P0-critical)

Everything that touches key material, sensitive variables, or encryption. **These are all pre-release-blocking.**

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **B.1** | 🔴 P0 | `Services/Security/VariableEncryptionService.cs:128-140` + `Api/appsettings.json:26-28` | `MasterKey` defaults to empty string. `Convert.FromBase64String("")` returns `new byte[0]` with no exception. KDF then derives 32-byte key from (empty master, 4-byte-int salt) — fully deterministic for fresh installs. DB dump → offline recovery of every sensitive variable. | ✅ (phase3) | `ValidateMasterKey(rawBase64, EnforcementMode)` rejects empty / short / all-zero **only in Strict mode**. Default mode is **Warn** — preserves backward compat for deploys without a configured MasterKey, surfaces structured warning in Seq pointing at `SQUID_MASTER_KEY_ENFORCEMENT=strict` for production hardening. Invalid base64 always throws (no key bytes to fall back to). Env var: `SQUID_MASTER_KEY_ENFORCEMENT={off\|warn\|strict}`. 30 new tests covering full (input × mode) matrix + constant-name pin. NOT A BREAKING CHANGE — Phase-1 broke deploys with empty MasterKey; Phase-3 restored backward compat. |
| **B.2** | 🔴 P0 | `Services/DeploymentExecution/Script/ScriptExecutionHelper.cs:42-46` + agent side `LocalScriptService.cs:607-684` | Per-script `sensitiveVariables.json` encryption password is a `Guid.NewGuid().ToString("N")` **written to `*.key` file next to ciphertext** AND passed via `psi.ArgumentList` as `--password=xxx` (visible in `ps aux` / `/proc/*/cmdline`). Encryption at rest = security theater. | ✅ (phase2) | Tentacle: dropped the `.key` sidecar write entirely — password stays in-memory on `ScriptFile.EncryptionPassword`. Password rides to Calamari child process via env var `SQUID_CALAMARI_SENSITIVE_PASSWORD` (readable via `/proc/<pid>/environ` at 0600, not via `ps aux` / `/proc/<pid>/cmdline` at 0444). Extracted `LocalScriptService.BuildCalamariProcessStartInfo` as testable pure function. Calamari: `RunScriptCliCommandHandler` reads env var with higher precedence than `--password=` argv (argv kept for tests / manual invocation). Env var name pinned as `public const` on both sides. 13 new tests across tentacle + calamari suites covering: constant-name pin on both sides, no `.key` file on disk, argv has no `--password=`, env var set on Calamari PSI, no env var when there's nothing to decrypt, missing ciphertext suppresses both env var and `--sensitive` arg, user-supplied arguments still forwarded, env-var-wins precedence, null-env falls back to argv, whitespace-env falls back to argv. Reverse-verify confirmed on all three pieces (restore `.key` write → tentacle test fails; restore argv password → tentacle test fails; flip precedence → calamari test fails). |
| **B.3** | 🔴 P0 | `Services/Common/SquidVariableEncryption.cs:12-14, 59-62` | PBKDF2 1000 iters + **hardcoded salt `"SquidDep"`** + SHA-1 + AES-128 CBC + no MAC. Vulnerable to bit-flip malleability; would fail any modern crypto review. | ✅ (phase4) | V2 envelope = `V2__` + salt(16) + nonce(12) + AES-256-GCM ciphertext + tag(16). KDF: PBKDF2-SHA256 @ 600_000 iters (OWASP 2023). V1 legacy still readable on both sides (dual-path) for rolling upgrade compat. Three-mode gate on both sides: server `SQUID_SENSITIVE_VAR_ENCRYPTION_ENFORCEMENT={off\|warn\|strict}` selects V1 emit (default) vs V2 emit (strict); calamari `SQUID_SENSITIVE_VAR_DECRYPT_LEGACY_ACCEPT` selects legacy V1 accept (default) vs strict V2-only. Malformed input always throws (no decoded bytes to use). AES-GCM tag detects tampering. 27 new tests: server 13 (format pin, layout, round-trip, tag-flip detection, wrong-password, iter-count pin, mode matrix, backward compat) + calamari 14 (mode × V1/V2 + unknown-prefix reject + env-var default pin). Non-breaking. |
| **B.4** | 🟠 P1 | `Services/Security/VariableEncryptionService.cs:41-61` | `DecryptAsync` has no `await` (compiler warning). No dual-key window — key rotation breaks all existing sensitive vars. Errors include internal `variableSetId`. | ⬜ | Remove async, add key-id to envelope (`SQUID_ENCRYPTED_v2:{keyId}:{nonce}:{cipher}`), support dual-key rollover. |
| **B.5** | 🟠 P1 | `Services/Security/VariableEncryptionService.cs:32,54` | `Log.Information("Successfully encrypted variable for VariableSet {id}")` on every call → timing-amplification leak via Seq. | ✅ (phase7) | Both encrypt + decrypt success log lines dropped from `Log.Information` to `Log.Debug`. Default Seq pipelines no longer ingest the per-VariableSet metadata; operators who want the trail raise the level explicitly. Failure paths (`InvalidOperationException` with detail) stay loud as before. 3 new tests pin Encrypt/Decrypt log levels + sensitive-value-never-in-payload (belt-and-braces). |
| **B.6** | 🟠 P1 | `Services/DeploymentExecution/Runtime/Bundles/BashRuntimeBundle.cs:109-117` | `EscapeBashValue` does NOT escape newlines. Variable value containing `\n` breaks out of `export` line → **shell injection** via user-settable non-sensitive variable. | ✅ (phase5) | `EscapeBashValue` switched to single-quote wrapping (`'value'`) — caller appends directly with no surrounding quotes. Inside single quotes EVERY character is literal (newline, $, `, !, \, ", #) so the entire class of escape-bypass bugs is eliminated. Only `'` itself needs the four-character POSIX idiom `'\''`. 24 new tests + 13 existing tests updated to the new single-quote contract. Reverse-verify confirmed. |
| **B.7** | 🟠 P1 | `Services/DeploymentExecution/Script/ServiceMessages/ServiceMessageParser.cs:82-90` | Agent stdout declares `##squid[setVariable ... sensitive='False']` — server trusts the flag blindly. Compromised/buggy script can mark a secret as non-sensitive → leaks to subsequent step logs. | ✅ (phase5) | New `SensitiveValueLeakGuard` cross-references each candidate output value against known-sensitive deployment variable values (HashSet<string>, ordinal). Three-mode: Off (legacy, silent passthrough), Warn (default — passthrough + structured Serilog warning naming the variable + env var, sensitive value OMITTED from log), Strict (force IsSensitive=true). Length floor 4 (aligned with `SensitiveValueMasker.MinValueLength`). Wired into `ExecuteStepsPhase.CaptureOutputVariables`; mode read once per capture pass. Env var: `SQUID_OUTPUT_VAR_SENSITIVE_LEAK_ENFORCEMENT={off\|warn\|strict}` (pinned in test). 17 new tests covering 3-mode × leak/no-leak matrix + length floor + value-redaction-in-log + boundary. Reverse-verify confirmed. |
| **B.8** | 🟡 P2 | `Observability/SensitiveValueMasker.cs:17` | `MinPatternLength = 4` → 3-char secrets (PINs) silently unmasked. Also `****` could collide with mask token. | ⬜ | Lower floor to 1 with explicit allowlist of "known short strings that aren't secrets"; or hash-and-compare masker. |
| **B.9** | 🟡 P2 | `Services/DeploymentExecution/Script/ServiceMessageParser.cs` + output variables | Qualified `Squid.Action.{Step}.{key}` output variables allow user script to overwrite system-looking names. Unqualified is guarded, qualified is not. | ⬜ | Extend `IsReservedName` to also match qualified forms. |
| **B.10** | 🟡 P2 | `Services/Security/VariableEncryptionService.cs:142-149` | `DeriveKey` salt = 32-bit variableSetId padded with zeros; 10k PBKDF2 iters (below OWASP 600k for SHA-256). | ✅ (phase8) | New V2 envelope: prefix `SQUID_ENCRYPTED_V2:` + base64(salt(16) ‖ nonce(12) ‖ tag(16) ‖ ciphertext). Random per-payload salt; PBKDF2-SHA256 @ 600k iters (OWASP 2023). Server post-Phase-8 always emits V2; decrypt is dual-path (V2 prefix → 600k path; legacy V1 prefix → existing 10k path). Existing pre-upgrade ciphertexts remain readable; entries upgrade naturally as variables are rewritten — no DB migration required. 12 new tests covering layout pin, roundtrip Theory, deterministic-salt regression (same plaintext → different ciphertext), V1 back-compat decrypt via hand-constructed legacy envelope, prefix recognition. Mirrors B.3 dual-format strategy used for sensitive-variable transport. |
| **B.11** | 🟡 P2 | `Core/VariableSubstitution/Templates/EvaluationContext.cs:176-180` + `TemplateEvaluator.cs:138` | Recursion detection uses exception-message substring `"self referencing loop"` — fragile. Missing variable `#{DoesNotExist}` renders verbatim in output. | ⬜ | Proper typed exception `VariableRecursionException` + fail-closed on missing `#{...}`. |

---

## Section 2 — Authorization & Multi-space (🔴 P0-critical)

Security boundary around tenant/space isolation + API surface.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **D.1** | 🔴 P0 | `Services/Caching/Redis/RedisCacheService.cs:20, 31` | `JsonConvert.DeserializeObject<T>(..., TypeNameHandling.Auto)` → **RCE via Newtonsoft gadget chains** if Redis is compromised / shared / misconfigured. Fires on every authenticated request (api-key cache). | ✅ (phase1) | `TypeNameHandling.None` + `RedisCacheService.JsonSettings` extracted for pinning; 2 tests (pin + behavioural Dog-ctor-counter). |
| **D.2** | 🔴 P0 | 10 commands: `AssignRoleToTeamCommand`, `UpdateRoleScopeCommand`, `AddTeamMemberCommand`, `RemoveTeamMemberCommand`, `UpdateTeamCommand`, `DeleteTeamCommand`, `CreateUserRoleCommand`, `UpdateUserRoleCommand`, `DeleteUserRoleCommand`, `RemoveRoleFromTeamCommand` | These commands do NOT implement `ISpaceScoped` → `FilterBySpace` returns all system-level roles (including `AdministerSystem`). Any `TeamEdit` holder in any space can assign `AdministerSystem` to their own team → **privilege escalation to full admin**. | ✅ (phase3) | Added `ISpaceScoped` + writable `int? SpaceId` to all 10 commands (Phase 1). `UserRoleService.EnsureCallerCanAssignRoleAsync` integrated under three-mode framework but **defaults to Strict** because this is a true privesc gate, not a deployment-config compatibility knob. Operators who genuinely need legacy permissive behaviour during a migration window opt OUT explicitly via `SQUID_ROLE_ASSIGNMENT_ENFORCEMENT={warn\|off}`. Phase-3 added 3 mode tests (Warn allows + logs, Off skips check entirely, default-mode-is-Strict pin) + env var name pin. Existing 5 unit + 3 integration tests still pass under unchanged Strict-default behaviour. |
| **D.3** | 🔴 P0 | `Services/Machines/Upgrade/UpgradeMachineCommand.cs:65` + `Middlewares/Authorization/AuthorizationSpecification.cs:43-53` + framework-wide | H-19 known, acknowledged in code comments, NOT fixed. `EnsurePermissionAsync` runs BEFORE resource lookup using header-supplied SpaceId. Any `MachineEdit` holder can target a machine in a different Space by setting `X-Space-Id` to a space they have perms in. Applies to **all** `{resource}Id + ISpaceScoped` commands. | ⬜ | Framework-level: load resource first → derive SpaceId from resource → check permission against resource's SpaceId, not header's. Requires auditing every permission-gated command. |
| **D.4** | 🔴 P0 | `Middlewares/SpaceScope/SpaceIdInjectionSpecification.cs:25` + all `Register*/Generate*` commands | Reflection filter is `prop.PropertyType != typeof(int?)` — skips non-nullable `int SpaceId` properties. Body-supplied SpaceId never overridden by header injection → operator can POST `{"SpaceId": <otherSpaceId>}` and write to that space if they have perms there. | ✅ (phase4) | Reflection filter expanded to handle both `int?` (body-null == unset) and `int` (body-zero == unset). Factored into named helpers (`TryInjectNullableInt`, `TryInjectNonNullableInt`, `TryReadHeaderSpaceId`). `int` body-zero + header-present now injects; `int` body-non-zero preserved (matches `int?` body-non-null behaviour — caller-intent wins). 4 new tests: NonNullable_Injects_When_BodyZero, NonNullable_SkipsInjection_When_BodyNonZero, NonNullable_SkipsInjection_When_HeaderMissing, NonNullable_SkipsInjection_When_HeaderInvalid. |
| **D.5** | 🟠 P1 | `Api/Authentication/ApiKey/ApiKeyAuthenticationHandler.cs:38-42` + `Services/Account/AccountService.cs` | API-key cache is invalidated only on `UpdateUserStatusAsync` and `DeleteApiKeyAsync`. Direct SQL user-disable / any other mutation path → stale cache entry valid for up to 1h. Also no `UserAccount.IsDisabled` check at cache-hit time. | ✅ (phase7) | `ApiKeyAuthenticationHandler.HandleAuthenticateAsync` now re-checks `matchedUser.IsDisabled` after the cache hit. If disabled: returns `NoResult` AND calls `_cacheManager.RemoveAsync(cacheKey, ...)` so the next request re-fetches fresh state. Closes the up-to-1h fail-open security staleness window for any out-of-band user-disable path. 4 new tests (active-user OK, disabled-cached returns NoResult, disabled-cached invalidates entry, no-API-key path unchanged). |
| **D.6** | 🟠 P1 | `Services/Identity/ICurrentUser.cs:27,42` + `Services/Identity/CurrentUsers.cs:7` | `InternalUser.Id = 8888` is mutable `static int` — not `const`. If `HttpContext == null` for `ApiUser` (background job scope, DI mishap, test-mode leak), `_currentUser.Id == 8888` → authorization middleware returns immediately, bypassing ALL checks. | ✅ (phase7) | Three pre-fix gaps closed together: (1) `CurrentUsers.InternalUser.Id` flipped from mutable `static int` to `public const int = 8888` (compile-pinned). (2) `ICurrentUser` gains `bool IsInternal` — `InternalUser` returns true, `ApiUser` returns false. `ApiUser.Id` returns null (was: InternalUser.Id) when HttpContext is null. (3) `AuthorizationSpecification` keys bypass off `IsInternal` (NOT off `Id == 8888`); throws `PermissionDeniedException` on null `Id` for permissioned commands. 6 new ICurrentUserTests + 2 new AuthorizationSpec tests; existing AuthorizationSpec tests updated. Closes the "DI mishap → ApiUser sees null HttpContext → falls back to 8888 → middleware silently bypasses" path. |
| **D.7** | 🟠 P1 | `Api/Extensions/CorsPolicyExtension.cs:10-19` | `AllowAnyHeader + AllowAnyMethod + AllowCredentials + WithOrigins(config-list)`. If an allow-listed origin is compromised (CNAME takeover, subdomain) → CSRF on cookie flow. | ⬜ | Enforce `SameSite=Strict` on auth cookies + add anti-forgery token to state-changing endpoints. Audit allow-list origins. |
| **D.8** | 🟠 P1 | `Services/Machines/MachineDataProvider.cs:60-74` (`GetMachinePagingAsync`) | If `spaceId == null`, space filter is skipped → cross-space machine listing (enumeration of names, endpoints, thumbprints). | ✅ (phase8) | API split into explicit `GetMachinesInSpacePagingAsync(int spaceId, ...)` (non-null at type level) and `GetMachinesAllSpacesPagingAsync(...)` (explicitly cross-space). Legacy nullable entry `[Obsolete]`-marked and routes to the appropriate method. Both call sites migrated: MachineService.GetMachinesAsync uses per-space when SpaceId.HasValue; MachineHealthCheckService.AutoHealthCheckForAllAsync uses all-spaces (legitimate Hangfire-driven cross-space sweep). Cross-space scan now requires every call site to be intentional — no more "forget the X-Space-Id header → enumerate every space". 4 new tests + 8 existing mocks updated. |
| **D.9** | 🟡 P2 | `Services/Authorization/AuthorizationService.cs:109` (`FilterBySpace`) | `r.SpaceId == null || r.SpaceId == spaceId` — system-level roles always included regardless of space. Amplifies D.2. | ⬜ | Keep as-is for `AdministerSystem` but split other system-level roles from space-scoped ones explicitly. |
| **D.10** | 🟡 P2 | `Services/Caching/ICacheManager.cs:55-81` `GetOrAddAsync` | No locking → classic thundering-herd on cache miss boundary. | ⬜ | `SemaphoreSlim` keyed by cache-key. |
| **D.11** | 🟡 P2 | `Services/Account/AccountService.cs:215` | API keys stored **plaintext** in DB. DB dump → keys reusable forever (no rotation signal). | ⬜ | Hash at rest (bcrypt/argon2), prefix token with key-id for O(1) lookup, store only hash + prefix. Migration path: dual-path read during rollout. |
| **D.12** | 🟡 P2 | Controllers | `GetUpgradeLogRequest` returns agent bash log which can contain tokens / paths / secrets. Ensure sensitive-value masker runs at response serialization, not just at ingest. | ⬜ | Apply masker at controller-response layer for any log-returning endpoint. |

---

## Section 3 — Kubernetes Agent (🔴 P0 + 🟠 P1)

K8s-specific script execution. Three critical security issues.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **C.1** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:64` + `Settings/Kubernetes/KubernetesSettings.cs:8` | Default `ScriptPodImage = "bitnami/kubectl:latest"` + `ImagePullPolicy=IfNotPresent`. Floating `:latest` tag. Registry/tag repoint → arbitrary code execution in the cluster. | ✅ (phase3) | `ScriptPodImageValidator.EnsureSafe(image, EnforcementMode)` — regex `@sha256:[a-fA-F0-9]{64}$` required in Strict mode; Warn (default) accepts tag-only with structured warning; Off silent. Malformed `@sha256:` ALWAYS throws (operator clearly intended to pin and got format wrong). Default `ScriptPodImage` reverted to `bitnami/kubectl:latest` for backward compat. Env var: `SQUID_SCRIPT_POD_IMAGE_ENFORCEMENT={off\|warn\|strict}`. Wired into `BuildPodSpec` AND `ApplyTemplateOverrides` (defence-in-depth). 18 validator (mode × input) tests + 3 wiring tests + helm `values.yaml` _comment block. NOT A BREAKING CHANGE — Phase-1 broke deploys with default image; Phase-3 restored backward compat. |
| **C.2** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/HelmUpgradeScriptBuilder.cs:51-56, 110` | Helm repo username/password and `--set` sensitive values are substituted directly into the rendered shell script (via `EscapeBash`). Ends up in `pod.args`, pod logs, script.sh on NFS, kubectl audit. | ⬜ | Write credentials to a per-job K8s Secret; mount as file or project via envFrom; reference via `HELM_*` env or `--values` file path. |
| **C.3** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentHealthCheckStrategy.cs:18-39` | Health check only probes Halibut capabilities — does NOT validate agent can actually create pods (RBAC, PVC mount, SA token). Broken agent reports Healthy until first real deployment. | ⬜ | Add a "describe ScriptPodTemplate" dry-run round-trip at end of health check. |
| **C.4** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:384-395` (`BuildPodSecurityContext`) | Default `ScriptPodRunAsNonRoot=false` + no UID → pod runs as **root**. `bitnami/kubectl` is root-by-default. `fsGroup` never set; NFS workspace owned as root. | ⬜ | Default to `runAsNonRoot:true, runAsUser:1000, fsGroup:1000, seccompProfile:RuntimeDefault, readOnlyRootFilesystem:true`. |
| **C.5** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodMonitor.cs:109-199` | Orphan cleanup depends on agent leader election being up. If both replicas lose leadership → no reaper. Also pod has no `OwnerReference` to Job, so Kubernetes GC can't help. | ⬜ | Use K8s Job with `ttlSecondsAfterFinished` — cluster GC reclaims even if agent is down. |
| **C.6** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:66-69` + `ScriptPodService.cs:131-171` | Workspace NFS PVC is `ReadWriteMany`; script.sh + variables.json written plaintext. Another pod on same PVC can read cross-ticket via workspace mount. | ⬜ | Use per-pod K8s Secret + ConfigMap envFrom projection (RBAC-scoped, not cross-readable on disk). |
| **C.7** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentIntentRenderer.cs:195-205` | `kubectl config set-context --current --namespace="{ns}"` persists globally in pod's `$KUBECONFIG` — user-script background process inherits mutated namespace. | ⬜ | Pass `-n {ns}` explicitly on every kubectl invocation; don't mutate the shared kubeconfig. |
| **C.8** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:55` + `KubernetesSettings.cs:9` | `ScriptPodServiceAccount="squid-script-sa"` with no accompanying RBAC shipped. Operator must wire manually; default SA (if missing) is cluster-default (permissive). | ⬜ | Ship chart-managed SA + Role with minimum verbs. Agent startup probes SA existence; fail-fast if missing. |
| **C.9** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.cs:33-36` (`_createLocks`) | `SemaphoreSlim` per ticketId never removed → unbounded dict growth on long-running agent. | ⬜ | Remove entry after release, or use size-bounded LRU. |
| **C.10** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.cs:202-211` (`CleanupTerminatedPod`) | `ContainerStatuses.FirstOrDefault()` — if `nfs-watchdog` is first, not `script`, `FinishedAt` is wrong. | ⬜ | Filter by container name `"script"` consistently. |
| **C.11** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentEndpointVariableContributor.cs:13-25` | Missing standard K8s context vars (`ClusterUrl`, `Account.*`) that shared helm/apply builders reference. | ⬜ | Mirror full contributor coverage; handler must not depend on vars that only KubernetesApi contributes. |

---

## Section 4 — Deployment Pipeline (🔴 P0 + 🟠 P1)

Server-side orchestrator. Concurrency and cancellation correctness.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **A.1** | 🔴 P0 | `Services/DeploymentExecution/Completion/DeploymentCompletionHandler.cs:40-45` + `DeploymentPipelineRunner.cs:65-67` | Cancel-vs-Fail race. If user cancel fires AFTER an exception but BEFORE terminal write, task ends `Failed` instead of `Cancelled`. Checkpoint may emit `DeploymentFailedEvent` while UI shows cancel. | ✅ (phase2) | `DeploymentPipelineRunner.ProcessAsync` now inspects `registryCts.Token.IsCancellationRequested || ct.IsCancellationRequested` BEFORE checking `ctx.FailureEncountered`. If either cancellation source is already signalled, emit `DeploymentCancelledEvent` + `OnCancelledAsync` regardless of the failure flag. Timeout path still flows through the existing OCE handler because `timeoutCts` raises OCE mid-execution (never reaches the terminal-write block quietly). 3 new tests: registry-cancel-after-failure → OnCancelled not OnFailure, external-ct-cancel-after-failure → OnCancelled not OnFailure, plain-failure-no-cancel still OnFailure (guard-on-guard). Reverse-verify confirmed. |
| **A.2** | 🔴 P0 | `Services/DeploymentExecution/Parallel/TargetParallelExecutor.cs:44-52` | `Task.WhenAll` throws only FIRST exception. Subsequent failures observed+discarded silently. | ✅ (phase2) | New private `AwaitAllCollectingFailuresAsync(tasks, ct)` helper walks every faulted task and aggregates `Exception.InnerExceptions` into a single `AggregateException` with "N of M parallel tasks failed" message. Both `ExecuteAllConcurrentAsync` and `ExecuteThrottledAsync` now route through it. Single-failure case uses `ExceptionDispatchInfo.Capture(...).Throw()` to preserve original exception type (downstream catch blocks keep matching). Pure-cancellation case re-throws a clean `OperationCanceledException`. 5 new tests: all-concurrent 3-failure aggregation, throttled 2-failure aggregation, failure-count in aggregate message, cancellation preserved as OCE, happy-path ordered-return regression. Reverse-verify confirmed. |
| **A.3** | 🔴 P0 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.cs:85-86` + `.Execute.cs:72-96` + `BatchCheckpointState` | `_batchStates` is non-concurrent `Dictionary<int, BatchCheckpointState>` written from parallel `Task.WhenAll`. `CompletedMachineIds` / `FailedMachineIds` are `HashSet<int>` — not thread-safe. → checkpoint corruption, lost terminal markers on resume. | ✅ (phase2) | `_batchStates` converted to `ConcurrentDictionary<int, BatchCheckpointState>` with atomic `GetOrAdd` in `GetOrCreateBatchState`. `BatchCheckpointState` gains thread-safe `AddCompleted` / `AddFailed` / `IsTerminalFor` guarded by a private instance lock (HashSet properties stay public for `System.Text.Json` resume deserialisation). `MarkTargetCompleted` now routes through the new thread-safe methods. 6 new tests under heavy parallel contention (500-1000 writers) — parallel adds preserve every entry, mixed completed/failed workloads don't clobber each other, `IsTerminalFor` tolerates concurrent writers, `GetOrCreateBatchState` returns exactly one instance per batchIndex, `MarkTargetCompleted` across threads preserves all machineIds. Reverse-verify confirmed on both the HashSet lock and the ConcurrentDictionary switch. |
| **A.4** | 🟠 P1 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.Execute.cs:72-96` | `catch when (step.IsRequired)` fires `failFastCts.Cancel()` then marks OTHER targets' OCEs as "failed" on checkpoint even though they were aborted, not genuinely failed. Resume skips them as terminal. | ✅ (phase7) | Extracted pure `TargetCatchClassifier` helper that takes (exception, failFastCancelled, parentCtCancelled) and returns `(MarkFailed, TriggerFailFast)`. Peer-abort case (OCE while failFastCts is cancelled but parent CT is NOT) returns `(false, false)` — target stays pending-resume. Genuine exception / user-cancel / unrelated OCE returns `(true, true)` — pre-fix semantics preserved. Catch site rewritten to consult the classifier. 5 new tests cover all four classification cells + reverse-verify. |
| **A.5** | 🟠 P1 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.cs:156-171` + output-var merge under lock | `_ctx.Variables.AddRange(result.OutputVariables)` — no de-dup on same variable name from different targets; last-writer-wins ordering is `Task.WhenAll` dependent (non-deterministic). | ✅ (phase6) | New `OutputVariableMerger.Merge(existing, incoming, mode)` helper with three-mode framework: Off (silent passthrough — legacy), Warn (default — passthrough + structured Serilog warning naming variable + env var), Strict (first-writer-wins; drop colliding incoming + log). Sensitive values redacted from log payloads (B.7-style — name only). Same-value re-emits silently deduplicate (not flagged as collision). Wired into `ApplyBatchResults` — mode read once per batch. Env var `SQUID_OUTPUT_VAR_COLLISION_ENFORCEMENT={off\|warn\|strict}` (pinned in test). 12 new tests covering 3-mode × no-collision/collision matrix + value-redaction + multi-collision reporting. |
| **A.6** | 🟠 P1 | `Services/DeploymentExecution/Script/ServiceMessages/ServiceMessageParser.cs:23-39` | Output variable capture `result[name] = variable` — later lines override earlier. User script can `echo '##squid[setVariable name="Squid.Deployment.Id" ...]'` and overwrite system vars. | ✅ (phase8) | New `IsReservedOutputVariableName` predicate (case-insensitive `Squid.*` / `System.*` prefix-with-dot). Three-mode framework via `SQUID_OUTPUT_VAR_NAME_ENFORCEMENT={off\|warn\|strict}` — Off silent (legacy), Warn (default) accepts + structured warning naming the offending variable + env var, Strict rejects (TryParseOutputVariable returns null). The qualified-clone bypass (pre-fix `Squid.Action.{step}.Squid.Deployment.Id` was admitted) is now gated at parse time. Complements B.7 (sensitive-flag leak guard) and Phase-6.5 (OutputVariableMerger collision detection) — together they close the full output-variable abuse surface. 9 new tests. |
| **A.7** | 🟡 P2 | `Services/DeploymentExecution/Infrastructure/DeploymentPipelineRunner.cs:128-129` | `ConcurrencyMaxWait` timeout logs `Warning … proceeding anyway`. Defeats the concurrency tag — two executors CAN run on tagged resource. | ⬜ | Throw after timeout; fail-closed. |
| **A.8** | 🟡 P2 | `Services/DeploymentExecution/Infrastructure/HalibutScriptObserver.cs:241-249` | Log buffer truncation removes OLDEST entries — loses CAUSE of failure on long deploys. | ⬜ | Truncate TAIL (keep head for cause analysis). Or persist incrementally. |
| **A.9** | 🟡 P2 | `Services/DeploymentExecution/Phases/ResumeCheckpointPhase.cs:43-58` | `JsonException` → start fresh + log warning. Silent re-deployment of completed machines on corruption. | ⬜ | Fail closed; require explicit "reset checkpoint" operator action. |
| **A.10** | 🟡 P2 | `RetentionCleanupPhase.cs:13-20` + `DeploymentCompletionHandler.cs:97-118` | Three "log warning and continue" swallows. Masks DB/service outages. | ⬜ | Emit structured Seq events + surface to health-check. |
| **A.11** | 🟡 P2 | `TargetParallelExecutor.cs:20` | No global parallelism semaphore → MaxParallelism=50 concurrent batches exhaust server pool. | ✅ (phase6) | Process-wide static `SemaphoreSlim` in `TargetParallelExecutor` configured by env var `SQUID_TARGET_PARALLELISM_GLOBAL_CAP`. Acquired by every parallel task in BOTH `ExecuteAllConcurrentAsync` and `ExecuteThrottledAsync` (after the per-step cap, so per-step ordering preserved). Always-release on the way out — even if worker throws — so faulted task can't permanently consume a slot. Modes: unset/auto → ProcessorCount × 4; integer → that exact cap; "disabled" / "0" → no cap. `HealthCheckActionHandler.cs:81` (uncapped Task.WhenAll) routed through the same executor. AsyncLocal test override (xUnit-parallel-class safe). 11 new tests + Theory matrix on parser. |
| **A.12** | 🟡 P2 | `Services/DeploymentExecution/Phases/PrepareTargetsPhase.cs:69-76` | `catch { return; }` on malformed `EnvironmentIds` JSON → account scope enforcement bypassed. | ⬜ | Hard-fail with detailed error pointing at the malformed account. |

---

## Section 5 — Halibut Communication (🔴 P0 + 🟠 P1)

Server-side Halibut usage. Protocol version parity + cancellation.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **E.1** | 🔴 P0 | `Services/DeploymentExecution/Infrastructure/HalibutScriptObserver.cs:124` + `IAsyncScriptService.cs` | No per-RPC `CancellationToken` on `GetStatusAsync`/`CompleteScriptAsync`/`CancelScriptAsync`. CT only checked BETWEEN polls. Stuck agent → 5min `TcpReceiveTimeout` before observer can abort. User-cancel doesn't propagate through in-flight RPC. | ⬜ | Adopt Octopus V2 pattern: `IAsyncClientScriptServiceV2` accepts `HalibutProxyRequestOptions { RequestCancellationToken }`. Requires Halibut client update. |
| **E.2** | 🔴 P0 | `Services/Tentacle/IScriptService.cs` + `HalibutMachineExecutionStrategy` | Server speaks only V1. No V2/V3-alpha fallback. Future V2-only tentacle → "NoMatchingService" hard failure. No version negotiation. | 🏔️ | Add `ScriptServiceVersionSelector` that probes capabilities first, picks highest common version. Major contract change. |
| **E.3** | 🟠 P1 | `Services/DeploymentExecution/Infrastructure/HalibutMachineExecutionStrategy.cs:29-71` | Capabilities cache exists but `ExecuteScriptAsync` never reads it — always calls V1 `StartScriptAsync`. | ✅ (phase4) | `IMachineRuntimeCapabilitiesCache.Store` signature extended with `IReadOnlyList<string> supportedServices` (default null for backward compat). `MachineRuntimeCapabilities` gains a `SupportedServices` property and a `SupportsScriptServiceV2` derived flag (case-insensitive match). `TentacleHealthCheckStrategy` now caches `response.SupportedServices` on every health check. `HalibutMachineExecutionStrategy` reads the cache + emits a structured `Log.Debug` naming the dispatch decision (v1-only / v2-capable / cold-cache). Dispatch still picks V1 always — the branch site is wired for E.2's future V2 rollout but V2 server-side isn't implemented yet. 7 new tests on the cache. |
| **E.4** | 🟠 P1 | `Halibut/PollingTrustDistributor.cs:40-69` | `TrustOnly` REPLACES entire trust list on every registration. Under burst registration → O(N) DB + brief polling-connection drops. Also `.GetAwaiter().GetResult()` in `IStartable.Start` → Autofac-scope deadlock risk. | ⬜ | Switch to additive `Trust(thumbprint)` API. Remove sync-over-async in startup path. |
| **E.5** | 🟠 P1 | `HalibutScriptObserver.cs:188-232` (liveness probe) | Liveness probe opens a SECOND Halibut connection every 5s — 30-min deploy = 360 extra capability RPCs + potential TLS handshakes. Per-probe exception allocation if service missing. | ⬜ | Piggyback liveness on main polling channel (V2 streaming would solve this). Or make probe interval dynamic (exponential backoff when healthy). |
| **E.6** | 🟡 P2 | `HalibutMachineExecutionStrategy.cs:44 + 57-70` | Only `HalibutClientException` + `OperationCanceledException` distinguished. `HalibutNetworkException` lumped with generic. | ⬜ | Add `IsTransientNetworkError` helper; align with LinuxTentacleUpgradeStrategy's pre-dispatch vs mid-dispatch split. |
| **E.7** | 🟡 P2 | `HalibutScriptObserver.cs` observer CT source | Observer CT comes from Hangfire job scope — if user clicks "cancel deployment" and `TaskCancellationRegistry.Cancel(taskId)` is not called, observer polls to `scriptTimeout`. | ⬜ | Ensure every UI cancel path calls the registry. Unit test the registry→observer propagation. |
| **E.8** | 🟡 P2 | `HalibutMachineExecutionStrategy.cs:92-104` | Deterministic `ScriptTicket` = SHA256(taskId, step, action, machineId). Retry on same attempt → same ticket → agent may return OLD ScriptStatusResponse. Idempotency-breaking-semantics lost. | ⬜ | Match Octopus: `new ScriptTicket(Guid.NewGuid())` per attempt. |
| **E.9** | 🟡 P2 | `Halibut/PollingTrustDistributor.cs:40-58` | Initial trust load backgrounded via `Task.Run`. During warmup, pre-existing tentacles TLS-rejected → reconnect churn. | ⬜ | Block startup until trust loaded (fail-fast if DB down). |
| **E.10** | 🟡 P2 | `Halibut/HalibutModule.cs:23` | `HalibutTimeoutsAndLimits.RecommendedValues()` hardcoded. No env override. Violates Rule 8 (env-var escape hatch for air-gapped operators). | ✅ (phase8) | New `HalibutModule.BuildTimeoutsAndLimits()` starts from `RecommendedValues()` then applies env-var overrides for the two most operationally-relevant timeouts: `SQUID_HALIBUT_TCP_RECEIVE_RESPONSE_TIMEOUT_SECONDS` (default 10min) and `SQUID_HALIBUT_POLLING_QUEUE_TIMEOUT_SECONDS`. Other fields inherit RecommendedValues — when Halibut adds a new field with a default, we pick it up automatically. Pure parser exposed via `TryParseSecondsEnv` for unit testing without env state. 16 new tests covering 2 constant-name pins (Rule 8), invalid-input matrix (null/empty/whitespace/garbage/zero/negative all fall back), valid-positive-int matrix, and override-application sanity. |

---

## Section 6 — Tentacle Runtime (🔴 P0 + 🟠 P1)

From **2026-04-23 deep-scan** (24 findings) — cross-verified by this audit round. Most are still open.

| ID | Severity | File:Line | Problem | Status |
|---|---|---|---|---|
| **T.1** | 🔴 P0 | `Tentacle/Certificate/ServerCertificateValidator.cs:63-69` | TLS pin fallback: unpinned certs accepted with warning. MITM door open. No `InsecureAllowUnpinned` opt-in. | ✅ (phase3) | `ValidateCore(...)` takes `EnforcementMode`. Chain-valid + thumbprint-match always accept; thumbprint-mismatch always rejects (mode-independent). The unpinned-chain-failure case: Strict rejects, Warn (default) accepts + warning, Off silent. Env var: `SQUID_SERVER_CERT_ENFORCEMENT={off\|warn\|strict}`. 22 tests covering 3-mode × (chain-valid / thumbprint-match / thumbprint-mismatch / no-thumbprint) matrix. NOT A BREAKING CHANGE — Phase-1 broke deploys without configured `ServerCertificate` + non-public CA cert; Phase-3 restored backward compat. |
| **T.2** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:217` | `ticketId` path traversal via `../../`. Absolute-path attack inoculated by `squid-tentacle-` prefix; `..` traversal still works. | ✅ (phase1) | Added `TicketIdWhitelist = ^[a-zA-Z0-9_-]{1,64}$` regex + throw-on-mismatch in `ResolveWorkDir`. 20 tests (6 legit + 13 malicious theory + 1 length-cap). |
| **T.3** | 🔴 P0 | `Tentacle/Core/TentacleHalibutHost.cs:167-177` | Sync-over-async adapter: `IAsyncScriptService.StartScriptAsync => Task.FromResult(_inner.StartScript(command))`. Inside `LocalScriptService.cs:72` there's `_isolationMutex.AcquireAsync().GetAwaiter().GetResult()`. Blocks Halibut worker threads. | 🔀 ARCH |
| **T.4** | 🔴 P0 | `Tentacle/Core/TentacleHalibutHost.cs:93` | `_runtime.Poll(pollUri, serverEndpoint, CancellationToken.None)` — no CTS for graceful drain. | ✅ (phase4) | Host owns a `CancellationTokenSource` threaded into every `_runtime.Poll(...)` call. New `ITentacleHalibutHost.CancelPolling()` method cancels it; `DisposeAsync` cancels before runtime disposal. `TentacleApp.RunAsync` invokes `CancelPolling` BEFORE the drain wait so the script backend gets a clean "no new work" window. `CancellationTokenSource.Cancel` is idempotent so multiple calls are safe. 6 new tests (fresh host, cancel signals token, cancel-before-start-no-throw, double-cancel-no-throw, DisposeAsync implicit cancel, TentacleApp shutdown wiring). |
| **T.5** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:72-95` | mutex handle acquired but not transferred to RunningScript until line 95. Any exception between → mutex stuck forever. | ✅ (phase2) | Wrapped post-acquire setup (dir create, state save, script + additional file writes, process start) in `try { ... } catch { isolationHandle?.Dispose(); throw; }`. Null out after successful `RunningScript` construction so happy-path is a no-op. `LockRelease.Dispose` is idempotent so transient mis-paths are safe. 1 end-to-end regression test: pre-create a directory collision to force `WriteAdditionalFiles` mid-loop throw → assert 2nd FullIsolation script with same mutex name starts within 5s (pre-fix it timed out). Reverse-verify confirmed. Compounds the T.7 fix: rethrow now reliably triggers this path. |
| **T.6** | 🔴 P0 | `Tentacle/Registration/TentacleListeningRegistrar.cs:34-44` | Listening registration: `ServerUrl == "https://localhost:7078"` → fake success return. Fragile literal compare. | ✅ (phase2) | Centralized the sentinel behind `TentacleSettings.DefaultServerUrlSentinel` + `IsAutoRegistrationUnconfigured()` helper. Both call sites (`TentacleListeningRegistrar.RegisterAsync` + `RegisterCommand.ExecuteAsync`) now route through the helper — drift between call sites is impossible. 8 new tests: pin the literal + `TentacleSettings.ServerUrl` default matches sentinel + unconfigured matrix (null/empty/whitespace/tab/sentinel) + positive matrix including `http://localhost:7078` and `https://localhost:9999` (legitimate local-dev URLs that pre-fix were silently dropped). Reverse-verify confirmed. |
| **T.7** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:610-614` | `WriteAdditionalFiles` catch is empty — doesn't rethrow. Script runs with missing input files. | ✅ (phase2) | Empty `catch { cleanup; }` → `finally { cleanup; }` with implicit rethrow. Temp file still removed in all paths. 3 new tests: target-path-is-directory → throws, first-file-fails → subsequent files NOT written, temp file cleaned up on failure. Reverse-verify confirmed. |
| **T.8** | 🔴 P0 | `Tentacle/Flavors/LinuxTentacle/TentacleListeningRegistrar.cs:61-65` | `ApiKey` sent as plain HTTP header without https-scheme enforcement. | ✅ (phase3) | `EnsureSchemeSafeForSecret(url, hasSecret, EnforcementMode)`. https always passes, http-without-secret always passes, non-http(s) / empty / malformed always throws (mode-independent — those are unconditional config errors). The http+secret cleartext case: Strict throws, Warn (default) accepts + structured warning, Off silent. Env var: `SQUID_REGISTER_HTTPS_ENFORCEMENT={off\|warn\|strict}`. 24 tests covering 3-mode × (scheme × secret) matrix. NOT A BREAKING CHANGE — Phase-1 broke listening tentacles with http+ApiKey; Phase-3 restored backward compat. |
| **T.9** | 🟠 P1 | `Tentacle/ScriptExecution/LocalScriptService.cs` `IsSameProcessAlive` | PID-recycling deeper check: only StartTime cross-checked, not image path (`proc.MainModule.FileName`). 2s tolerance window could admit fast-recycled PID. | ⬜ |
| **T.10** | 🟠 P1 | `Tentacle/Core/TentacleHalibutHost.cs:140` | `_runtime.Listen(port)` defaults to 0.0.0.0. No `ListenIPAddress` config. | ✅ (phase8) | Env var `SQUID_TENTACLE_LISTEN_IP_ADDRESS` selects the bind address. Default / "any" / unset → `IPAddress.Any` (preserves pre-fix behaviour). Valid IPv4/IPv6 literal pins to that interface. Garbage falls back to `IPAddress.Any` + structured Warning — typo never makes the agent invisible. `StartListening` now constructs `IPEndPoint(bindAddress, port)` and calls `HalibutRuntime.Listen(IPEndPoint)`. 17 new tests covering constant pin, default/Any matrix, IPv4 + IPv6 valid matrices, garbage fallback. |
| **T.11** | 🟠 P1 | `Tentacle/ScriptExecution/LocalScriptService.cs:46,497` | Drain race: `_draining` flag checked at entry, but Halibut has already dispatched. RPC gets exception → server marks task failed (avoidable). | ✅ (phase5) | `StartScript` while draining now returns a structured `ScriptStatusResponse(Complete, AgentDraining=-503, stderr=…)` instead of throwing. New `ScriptExitCodes.AgentDraining` (mirrors HTTP 503; included in `IsInfrastructureFailure`) so existing classification treats drain rejections as infra failures. Halibut serialises the response normally; server's existing failure handling treats unknown exit codes as failures so deployment outcome is unchanged — the win is observability (operator sees the AgentDraining code + actionable stderr instead of a wrapped exception trace). 4 new tests: returns Complete/AgentDraining (no throw), stderr explains rejection, propagates ticket, Serilog warning. Reverse-verify confirmed. |
| **T.12** | 🟠 P1 | `Tentacle/Logging/SequencedLogWriter.cs:77-92` | `DetermineStartSequence` fallback 0 on parse failure → log-file corruption → server sees duplicate seq numbers. | ✅ (phase5) | Replaced last-line-only logic with `SequencedLogReader.GetHighestSequence` — same forgiving forward-scan the reader uses. Garbage lines anywhere in the file are tolerated; restart point is exactly one past the highest valid sequence. All-garbage / empty file → 0 (matches no-file case). Removed the now-dead `ReadLastLine` helper. 3 new tests: corrupt-tail-resumes-not-zero, multiple-interleaved-garbage scan, only-garbage stays at zero. Reverse-verify confirmed. Compounds with Phase-4 cursor fix — both close cursor-corruption paths. |
| **T.13** | 🟠 P1 | `Tentacle/Core/TentacleHalibutHost.cs:161-177` | `AsyncScriptServiceAdapter` receives CT on every method, discards it. Halibut cancellation fully broken. | 🔀 ARCH |
| **T.14** | 🟠 P1 | `Tentacle/Registration/TentaclePollingRegistrar.cs` + `ResolvePollUri:148-153` | Server-returned `subscriptionUri` not validated → malformed URL = startup crash via `UriFormatException`. | ✅ (phase8) | `ResolvePollUri` now uses `Uri.TryCreate(..., UriKind.Absolute, ...)`. Three outcomes: valid absolute URI from server → use it; null/blank → deterministic local fallback `poll://{subscriptionId}/`; malformed OR relative → fall back + structured Warning naming the offending value. A buggy server release sending garbage in `subscriptionUri` no longer crashes every fresh-registering agent. 6 new tests including malformed Theory matrix and relative-URI edge case. |
| **T.15** | 🟠 P1 | `Tentacle/Network/PublicHostNameResolver.cs:121-165` | Cloud-metadata calls use `http.Send().Result` and `.Content.ReadAsStringAsync().Result`. Sync-blocking during startup registration → ThreadPool starvation deadlock risk. | ⬜ |
| **T.16** | 🟡 P2 | various | 8 more P2 items (stdbuf, group-read mode, TcpKeepAlive, CleanupOrphanedWorkspaces CreationTime, process-local mutex multi-pod, etc.) | ⬜ |

---

## Section 7 — 🏔️ Architectural Alignment (deferred, multi-week)

| ID | Description | Scope | Estimated effort |
|---|---|---|---|
| **ARCH.1** | **4-phase script protocol** (`Begin/CheckExitCode/CollectLogs/Clean`) with installId correlation token + exit-code file | Matches Octopus TentacleUpgradeMediator approach; solves "agent dead vs still installing" ambiguity | 1-2 weeks |
| **ARCH.2** | **ScriptService V2/V3** with streaming logs + capabilities negotiation | Removes polling overhead for long-running scripts; enables future Kubernetes script service | 2-3 weeks |
| **ARCH.3** | **Tiered timeouts** (15min install / 2min restart / 2min log / 2min clean) | Replaces single 5-min `ScriptTimeout` | 3-5 days |
| **ARCH.4** | **Deployment-conflict guard** — block upgrade when deployment in-flight on same target | Matches Octopus `BlockUpgradeIfDeploymentIsRunning` | 3-5 days |
| **ARCH.5** | **ScriptIsolationLevel.FullIsolation mutex** per machine | Serializes all script dispatches to same machine regardless of source | 2-3 days |
| **ARCH.6** | **K8s Job + ttlSecondsAfterFinished** instead of Pod | Solves orphan-pod cleanup without depending on agent leader election | 3-5 days |
| **ARCH.7** | **Guid-per-attempt ScriptTicket** ✅ (phase6) | Fixes retry idempotency-breaking semantics. `GenerateTicketId` now `Guid.NewGuid().ToString("N")`; stable hash split off into `GenerateMutexName` (used as `IsolationMutexName` so concurrent same-action dispatches still serialise on agent). | DONE |
| **ARCH.8** | **H-19 framework-level fix**: resource-load-then-permission-check | Touches every `[RequiresPermission]` command | 1 week |
| **ARCH.9** | **IScriptService → fully async** with native CT propagation | Large refactor; solves T.3, T.13, E.1 together | 1-2 weeks |

---

## Execution plan (proposed)

### Phase 1 — P0 Security (Week 1, ~3 days)

Quick-win security fixes, each < 1 day, each a separate PR for clean review:

1. **D.1** — Redis `TypeNameHandling.None` (30 min)
2. **D.2** — Team/role commands `ISpaceScoped` (half day)
3. **B.1** — MasterKey fail-fast on empty (1 hour)
4. **T.1** — TLS pin fallback → opt-in env var (1 hour)
5. **C.1** — K8s default image pin digest (1 hour)
6. **T.2** — ticketId whitelist (30 min)
7. **T.8** — Listening ApiKey https enforce (30 min)

Total: **~1.5 days of coding + 1 day of tests/review = 2-3 days**.

### Phase 2 — P0 Data Integrity (Week 2)

1. **T.5** — StartScript mutex leak protection
2. **T.7** — WriteAdditionalFiles rethrow
3. **T.6** — Listening register localhost:7078 hack removal
4. **A.3** — `_batchStates` thread-safety
5. **A.2** — `Task.WhenAll` exception collection
6. **A.1** — Cancel-vs-Fail race
7. **B.2** — Sensitive vars: remove `.key` file + argv password (requires Calamari side too)

Total: **~4-5 days**.

### Phase 3 — P0 Crypto Modernization (Week 3)

1. **B.3** — AES-GCM + random nonce + random salt (with migration)
2. **B.1** follow-up — key rotation support (B.4)
3. **C.2** — Helm credential via Secret projection

Total: **~1 week**.

### Phase 4 — P0/P1 K8s + Auth hardening (Week 4)

1. **C.3** — K8s health check pod-create dry-run
2. **C.4** — Default non-root pod
3. **D.3** — H-19 framework fix (larger; may spill to week 5)
4. **D.4** — SpaceId injection expansion
5. **D.5/D.6** — API key cache + InternalUser id

Total: **~1-1.5 weeks**.

### Phase 5+ — P1 / P2 / ARCH

Batched by area; sequence per triage discussion after P0 is clean.

---

## Working agreements

- Each fix follows strict **Red-Green-Refactor**:
  1. 思維鏈 — analyze the bug deeply, identify all affected paths
  2. 測試用例 — define failing tests for the bug + edge cases
  3. 確認 — confirm the test is genuinely red against current code
  4. Minimal production code to go green
  5. Reverse-verify — temporarily break the fix, confirm test fails
  6. Full suite pass
- Each PR ≤ 5 files touched, ≤ 500 lines total where possible
- During each fix, **look for related issues** in the touched files — add to this tracking doc if found, don't silently fix-and-forget
- Plan can be adjusted mid-flight if a dependency forces reordering

---

## Open questions

1. **Production impact of B.3 migration** — all existing sensitive-variable ciphertexts need to be readable by the new code path. Dual-path decrypt during transition, then force re-encrypt. Coordination with prod deployment required.
2. **T.3 / T.13 / E.1 interdependence** — these three are all facets of the same sync-over-async → CT-broken problem. Best fixed together as ARCH.9, but each has isolated quick-patch value too.
3. **C.2 Helm Secret projection** — needs agent-side + server-side coordination, and possibly a breaking change for operators who extend `HelmUpgradeScriptBuilder`. Plan rollout window.

---

_Last updated: 2026-05-04 (Phase-12.E.6 = Windows upgrade wrapper E2E — dedicated Squid.WindowsUpgradeE2ETests project + 5 critical-path tests verifying real schtasks / Task Scheduler `/RU SYSTEM` `/Z` `/F` detach mechanism + GUID task-name uniqueness on the actual `windows-latest` GHA runner landed). Prior phases: Phase-12.E.5 = OS-routing genericity refactor (AgentOperatingSystems constants + IsWindows/IsLinux/IsMacOS/IsUnknown predicates + Linux strategy explicit-claim semantic + cross-assembly pinning). Phase-12.E.4 = real WindowsTentacleUpgradeStrategy.UpgradeAsync via Halibut + Task Scheduler one-shot detach + ITentacleVersionRegistry OS-aware widening + 4 .ps1 polish fixes. Phase-12.E.3 = OS-aware strategy resolution + Windows-routing stub. Phase-12.E.2 = ZipUpgradeMethod + upgrade-windows-tentacle.ps1 template. Phase-12.E.1 = IWindowsUpgradeMethod interface + Windows version source via GitHub Releases API. Phase-12.E.0 = Windows Tentacle build pipeline + install.ps1. Phase-12.D = Windows Tentacle restart-on-failure via `sc failure`. Phase-12.C = WindowsServiceHost via sc.exe. Phase-12.B = IProcessLauncher + WindowsPowerShellProcessLauncher (env-var gated). Phase-12.A = IFilePermissionManager + IUpgradeStatusStorage + IServiceUserProvider. Phase-11 = ARCH.9 Plan A (soft-cancel registry + sync-over-async fixes). Phase-10 = D.3 + C.2 + C.3. Phase-9b = 9.8 + 9.9 + 9.12 + 9.13 + 9.14. Phase-9a = 9.1..9.15 robustness bundle. Phase-8.7 = namespace genericity cleanup. Phase-8 = B.10 + A.6 + D.8 + T.10 + T.14 + E.10. Phase-7 = B.5 + D.5 + D.6 + N3 + A.4. Phase-6 = A.5 + A.11 + ARCH.7 + 2 new items. Phase-5 = B.6 + B.7 + T.11 + T.12. Phase-1..4 = P0s. Update as items land._

_Phase-7 deep-audit follow-up note_: N3 (SshExecutionMutex unbounded `_locks` dict — Agent D in the post-Phase-5 deep audit) closed via reference-counted entries that auto-evict when the last caller releases. 5 new tests pin the contract (LockCount returns to 0 across acquire/dispose, 100 distinct endpoints all evict, waiter-queued / timeout / concurrent-50×20 all leave dict empty).

_Phase-8 polish bundle (2026-04-26)_: closed B.10 (KDF V2 envelope), A.6 (output-var reserved-prefix), D.8 (per-space vs cross-space API split), T.10 (ListenIPAddress env var), T.14 (subscriptionUri fallback), E.10 (Halibut timeouts env vars). 6 P1/P2 items, 67 new tests, all non-breaking with default modes preserving legacy behaviour. Pushes closed-finding count to 36/85.

_Phase-8.7 namespace genericity cleanup (2026-04-26)_: `EndpointVariableFactory` (used by every transport — KubernetesApi, KubernetesAgent, Ssh, Tentacle, OpenClaw — for endpoint JSON deserialization and `VariableDto` construction) was misplaced under `Squid.Core.Services.DeploymentExecution.Kubernetes`, forcing every non-K8s transport's `IEndpointVariableContributor` and `IHealthCheckStrategy` to import the K8s namespace just to read endpoint payloads. Moved to `Squid.Core.Services.DeploymentExecution.Variables` (generic namespace alongside `AccountVariableExpander`). Test file moved from `Services/Deployments/Kubernetes/` → `Services/Deployments/Execution/`. Net effect: 7 non-K8s files dropped a misleading `using Kubernetes;`, 4 K8s-internal files added the new `using Variables;`. No behavioural change. 0 new tests (existing 7 EndpointVariableFactory tests cover; namespace move is verified by green build + green suite). All cross-project totals unchanged: 4712 / 1196 / 139.

_Phase-9a hardening bundle (2026-04-27)_: 11 items, 38 new tests across UnitTests + Tentacle.Tests. Closes a Phase-8.6 crypto-symmetry debt and 4 P0/P1 robustness items identified in the Listening + Polling + Server + OctopusTentacle deep-audits. Items:
  - **9.1 (Phase-8.6 sym debt — P0)**: Tentacle output-masking decryptor was V1-only; server-emitted V2 envelopes silently failed → sensitive values printed verbatim into agent stdout/stderr. Mirrored Calamari's dual-format reader exactly (V1 mode-gated via SQUID_SENSITIVE_VAR_DECRYPT_LEGACY_ACCEPT). 7 new tests.
  - **9.2 (P0)**: SSH cleanup-finally guarantee. Pre-fix, `CleanupRemoteWorkDirectory` sat AFTER the success-return so any exception left workDir + sensitiveVariables.json on the remote forever. Wrapped in try/finally; cleanup helper now `protected internal virtual` for test observation. 3 new tests.
  - **9.3 (P0)**: PollingTrust startup race. `Start()` background load races concurrent registration's `ReconfigureIfMissingAsync` — both call `TrustOnly` with different lists, last writer drops thumbprints. Serialized via `SemaphoreSlim _reconfigureLock`. 3 new tests.
  - **9.4 (P1)**: SSH cancel kill remote process propagation. User cancellation exits local poll loop via OCE but remote bash kept running until SSH session disposed (or forever if `nohup`/`setsid`). Finally block now fires `pkill -TERM -f workDir; sleep 5; pkill -KILL` on cancellation paths. 3 new tests.
  - **9.5 (P1)**: ShutdownDrainTimeoutSeconds default 30s → 300s. 30s killed any 5-30min infra deploy at SIGTERM. New default + Rule 8 const + pin test (operators may match systemd `TimeoutStopSec` to this value).
  - **9.6 (P1)**: Architecture-genericity 3-item cleanup, same pattern as 8.7. `CacheHitStagingHandler` + `FullUploadStagingHandler` moved from generic `Packages/Staging/Handlers/` to `Targets/Ssh/Packages/Staging/` (cast to `SshPackageStagingContext` was hidden coupling). `HalibutMachineExecutionStrategy` moved from `Infrastructure/` to `Targets/Tentacle/Transport/` (Halibut IS the tentacle wire protocol).
  - **9.7 (P1)**: `[CacheResponse(60)]` on `ICapabilitiesService` + `ICapabilitiesServiceAsync`. Pre-fix, every server health-check poll re-read 3 files from agent disk. 60s TTL chosen vs OctopusTentacle's 600s because Squid response carries fresher upgrade-status metadata. 3 new tests pin TTL value + sync/async drift.
  - **9.10 (P1)**: Halibut bump 8.1.1052 → 8.1.1943 (~900 patch versions to match OctopusTentacle main). Forced StackExchange.Redis 2.6.122 → 2.8.58 to close NU1605 downgrade.
  - **9.11 (P1)**: `OrphanMaxAge` env-var override (SQUID_TENTACLE_ORPHAN_WORKSPACE_TTL_HOURS, 1..720h, default 24h) + Prometheus counter `squid_tentacle_orphaned_workspaces_cleaned_total`. Pre-fix value was hardcoded; high-throughput operators couldn't tighten cleanup window. 3 new tests.
  - **9.15 (P1)**: Polling startup jitter (SQUID_TENTACLE_POLLING_STARTUP_JITTER_MS, 0..300000ms, default 0). Each Tentacle sleeps 0..N ms before first Poll() call → reconnect storms after server restart spread across the fleet instead of arriving in a single instant. Default 0 preserves backward compat. 13 new tests.

Phase-9a deferred to Phase-9b: 9.8 (`IFileTransferService` contract), 9.9 (`tentacle check-services` CLI), 9.12 (`BackwardsCompatibleCapabilitiesV2Helper` for too-old agents), 9.13 (Tentacle SIGHUP config reload — touch cert rotation), 9.14 (server-side polling work-queue bounded). All require larger surface changes; bundled separately to keep Phase-9a small + mergeable.

_Phase-9b Tentacle-contracts bundle (2026-04-27)_: 5 items, ~50 new tests. Closes the OctopusTentacle alignment gaps + a P1-rated server OOM defence missing from Phase-9a:
  - **9b.1 / item 9.14 (P1 OOM defence)**: server-side polling-work admission gate. Pre-fix, an offline polling Tentacle could absorb 1000+ queued dispatches — Halibut's pending-request queue grew unbounded → OOM. New `HalibutPollingWorkAdmission` static gate with per-machine in-flight ConcurrentDictionary, atomic TryAdmit/Release with clamp-at-0. Wired into `HalibutMachineExecutionStrategy.ExecuteScriptAsync` BEFORE Halibut client creation; try/finally release on every exit path. Operator override via `SQUID_HALIBUT_MAX_PENDING_WORK_PER_AGENT` (default 100). Per-machine isolation. New `PollingWorkAdmissionExceededException` with operator-actionable message. 16 unit + 1 integration tests; xUnit Collection serializes the static state across both test classes.
  - **9b.2 / item 9.12 (P1 protocol resilience)**: `BackwardsCompatibleCapabilitiesClient` upgraded from substring-only matching to typed-exception detection. Halibut 8.1.1943 hierarchy: `NoMatchingServiceOrMethodHalibutClientException` (base) ← `ServiceNotFoundHalibutClientException` / `MethodNotFoundHalibutClientException` / `AmbiguousMethodMatchHalibutClientException`. Type-based check is primary; substring fallback retained for older Halibut versions. CRITICAL: explicit-exclude `AmbiguousMethodMatchHalibutClientException` — server-side wiring bug must rethrow rather than silently mask as "old agent". 4 new tests (3 typed-exception positive, 1 negative for Ambiguous).
  - **9b.3 / item 9.8 (P1 OctopusTentacle parity gap #1)**: `IFileTransferService` separate file upload/download wire contract. Mirror of OctopusTentacle's pattern: sync wire (`IFileTransferService`), server-side async (`IFileTransferServiceAsync` w/ CT), client-side async (`IAsyncClientFileTransferService` no CT — Halibut method-shape limitation). Agent impl `LocalFileTransferService` writes under `~/.squid/uploads` with workspace-boundary path rewriting (rooted / `..` traversal / Windows backslash all rewritten to `rewritten-{sha256-prefix}` inside upload root + defence-in-depth post-`Path.Combine` symlink check). Hash-as-we-write: bytes flow through SHA-256 CryptoStream before disk so server can verify integrity AND mid-stream corruption is detected. Registered in `TentacleHalibutHost.DelegateServiceFactory`. Server-side `IHalibutClientFactory.CreateFileTransferClient`. 16 new tests including theory-driven malicious-path matrix.
  - **9b.4 / item 9.13 (P1 zero-downtime cert rotation)**: SIGHUP-driven trust-list hot reload. New `TentacleHalibutHost.ReloadTrustList(string)` — atomic `HalibutRuntime.TrustOnly` replace; defensive null/empty/malformed input is no-op + warn (refuses to wipe trust silently). New `HookConfigReloadOnSighup(Func<string>)` — `PosixSignalRegistration.Create(SIGHUP, ...)` calls reload on signal, `ctx.Cancel = true` so process doesn't terminate. Linux/macOS only — Windows operators get a `NoOpDisposable` (Phase-9b.5's `tentacle service reload` is the platform-portable alternative). Operator workflow: edit config → `kill -HUP $(pgrep squid-tentacle)` or `systemctl reload squid-tentacle`. 6 new tests.
  - **9b.5 / item 9.9 (P1 operator self-diagnostic)**: `tentacle check-services` CLI command. Mirrors OctopusTentacle's `check-services`. Five checks (Configuration, Certificate, IScriptService, ICapabilitiesService, IFileTransferService) each returning (Name, Pass, Detail) with exit code 0 (all pass) / 1 (any fail). Internal `RunChecks(settings)` for testing without Console output. Wired into command resolver between `show-config` and `new-certificate` (the natural diagnostic flow). 7 new tests.

Phase-9b cumulative: closes 5/5 deferred OctopusTentacle gaps. Cross-project totals after Phase-9b: **4745 / 1250 / 139 = 6134 tests, 0 failures** (Phase-9a baseline 6084 + 50 new this bundle: 17 admission gate + 4 typed-exception + 16 file-transfer + 6 SIGHUP reload + 7 check-services).

_Phase-10 P0 security closeout (2026-04-28)_: 3 P0 items, ~32 new tests. Closes the audit's only TRUE cross-space privesc vector + 2 silent-failure / data-leak categories.
  - **10.1 / item C.3 (P0 silent-failure)**: K8s agent RBAC dry-run health check. Pre-fix, an agent whose ServiceAccount RBAC was revoked / namespace was deleted reported "healthy" via Halibut polling — first deploy then failed with cryptic kubectl Forbidden error. New `KubernetesRbacInspector` (agent-side) shells out to `kubectl auth can-i create <resource>` per deploy-critical resource (pods / configmaps / secrets) at startup, surfaces results via Capabilities metadata. Server-side `KubernetesAgentHealthCheckStrategy.InspectRbacMetadata` reads keys; ANY denied resource → unhealthy with operator-actionable detail naming the specific permission. K8s-only (detected via `KUBERNETES_SERVICE_HOST` env) — non-K8s tentacles unaffected. Backward compat: pre-Phase-10.1 agents without metadata fall back to legacy "Halibut reachable = healthy" path. Constants pinned (Rule 8) on BOTH sides — drift would silently break the check. 12 new tests (7 agent-side + 5 server-side).
  - **10.2 / item C.2 (P0 secret leak)**: Helm secret projection via `--values` file replaces `--set` argv emission. Pre-fix, `HelmUpgradeScriptBuilder` emitted `--set "key=value"` putting full value text into helm process argv → visible to ps aux, /proc/<pid>/cmdline, kubelet logs, Sysdig/Falco audit, bash trace logs. For ANY value an operator marks sensitive (DB password, API key, TLS private key), this was unacceptable secret exposure. Fix: project ALL inline values to a generated `inline-values.yaml` file referenced via `--values` (which goes LAST in the chain to preserve `--set` precedence semantics). Generated file lives in per-task workspace (already 0700 via Phase-9.2 cleanup-finally), is wiped by existing finally block, NEVER appears in argv. Dotted keys produce nested YAML structure (matches helm's standard `--set` dot-notation). 6 new tests + 0 regressions across 123 existing Helm-related tests.
  - **10.3 / item D.3 / H-19 (P0 cross-space privesc)**: header SpaceId now membership-checked. The audit's ONLY true cross-space privilege-escalation surface — pre-fix, a user with team membership in Space-1 could send `X-Space-Id: 2` HTTP header, the SpaceIdInjectionSpecification trusted the header verbatim, downstream authorization checked permissions against the spoofed SpaceId, user could read/mutate Space-2 resources despite zero membership. Fix: new `SpaceMembershipSpecification` runs BEFORE the injection spec; reads X-Space-Id, validates user membership via `ISpaceMembershipResolver` (TeamMember → Team → Space chain), throws `CrossSpaceAccessDeniedException` with structured (UserId, RequestedSpaceId) for audit-trail correlation. Internal users bypass via `IsInternal` (Phase-7 D.6 trusted-context pattern, NOT Id-equals-8888 heuristic). Null UserId throws with -1 placeholder (Phase-7 D.6 fail-closed posture). Missing/malformed header falls through (no claim to gate). 11 new tests (4 resolver + 7 middleware). Note: per-resource SpaceId derivation (load entity → check entity.SpaceId matches user's permitted spaces) is a separate, larger refactor tracked as a future ARCH item — Phase-10.3 closes the immediate header-trust vector.

Phase-10 cumulative: closes 3/3 P0 audit findings (D.3, C.2, C.3). Cross-project totals after Phase-10: **4768 / 1257 / 139 = 6164 tests, 0 failures** (Phase-9b baseline 6134 + 30 new this bundle: 12 RBAC + 6 Helm + 12 SpaceMembership). Closes 55/85 findings (65%) — full Phase-9 + 10 closes 19 items.

_Phase-11 ARCH.9 Plan A: agent-side sync-over-async + soft-cancel propagation (2026-04-28)_: 17 new tests. Closes the F1.1 + F1.2 audit findings flagged in the ARCH.9 surface map (T.3 / T.13 / E.1 cluster). Wire contract IScriptService stays V1 sync — **no breaking change**. Internal async work now actually responds to CancelScript intent.
  - **11.1 — `ScriptCancellationRegistry`**: per-ticket `CancellationTokenSource` registry that bridges the wire-level "no CT" limitation with internal async work. `StartScript` calls `GetOrCreate(ticket)` to obtain a soft-cancel token, threads it into mutex acquire + file save. `CancelScript` calls `Cancel(ticket)` to flip the CTS — in-flight async work observes the cancellation. `CompleteScript` / `CancelScript` call `Cleanup(ticket)` to dispose the CTS. Out-of-order RPC defence: if Cancel arrives BEFORE the matching `GetOrCreate` (server cancelled before agent registered), an `EarlyCancelledSentinel` placeholder makes the eventual GetOrCreate yield an already-cancelled token. 9 new tests including high-concurrency stress.
  - **11.2 (audit F1.1) — sync-over-async mutex acquire eliminated**: pre-fix `LocalScriptService:125` was `_isolationMutex.AcquireAsync(command).GetAwaiter().GetResult()` — burned a Halibut RPC thread on a Task.Delay-based async polling loop with allocation churn + threadpool starvation under contention. New `ScriptIsolationMutex.TryAcquireBlocking` is pure-sync polling using `WaitHandle.WaitOne` — no Task allocation, no async state machine, same observable behaviour (100ms→1s exponential poll, deadline check). Optionally accepts CT so a CancelScript mid-acquire short-circuits via `OperationCanceledException` instead of waiting the full configured isolation timeout. 4 new tests.
  - **11.3 (audit F1.2) — hardcoded `CancellationToken.None` on file save replaced**: pre-fix `LocalScriptService:754` was `file.Contents.Receiver().SaveToAsync(tempPath, CancellationToken.None)` — a 1GB sensitiveVariables.json or large package payload would write to completion regardless of CancelScript. Now `WriteAdditionalFiles` takes a `CancellationToken` (default arg preserves backward compat); threaded into both loop-top check (between files) AND inner `SaveToAsync` (mid-stream cancel of a single very-large file aborts via `CopyToAsync`'s CT plumbing). Defence-in-depth: both layers retained. 4 new tests.
  - **11.4 — CancelScript wired to flip the registry CTS**: `LocalScriptService.CancelScript` now calls `_cancellationRegistry.Cancel` BEFORE `_scripts.TryRemove`. Signals any in-flight async work that's NOT yet tracked in `_scripts` (StartScript still mid-acquire or mid-write). Both `CompleteScript` and `CancelScript` call `_cancellationRegistry.Cleanup` to release the per-ticket CTS so the registry doesn't accumulate over agent lifetime.
  - **11.5 — `ScriptPodService` already uses `TryAcquire` (no GetAwaiter().GetResult())**: confirmed no-op via grep audit. The K8s-pod-tentacle path was already non-blocking; ARCH.9 sync-over-async finding was specific to the polling-tentacle path.

**Plan B — V2 wire contract with first-class CT — DEFERRED** to a future phase that bundles E.2 capability dispatch + version negotiation. Plan A's no-wire-break scope intentionally leaves F2.x (CancellationToken discarded in `AsyncScriptServiceAdapter.StartScriptAsync`) — that's a Halibut V1 design limitation that requires V2 wire to fix. The soft-cancel registry is the bridge until then; once Plan B lands, the registry can be retired or re-purposed.

Phase-11 cumulative: closes 1/1 ARCH P0 finding (ARCH.9 Plan A scope). Cross-project totals after Phase-11 + pre-push audit follow-up: **4773 / 1277 / 139 = 6189 tests, 0 failures** (Phase-10 baseline 6169 + 20 new this bundle).

_Phase-11 pre-push audit follow-up (2026-04-28)_: independent paranoid agent caught 2 hidden risks before merge (matches the discipline applied in Phase-9a / Phase-10 audit-fixes):
  - **Audit Issue #1 — CompleteScript early-return CTS leak**: the leak path is StartScript registers with the ScriptCancellationRegistry BEFORE _scripts.TryAdd; if a failure between those steps leaves the registry populated but _scripts empty, a subsequent CompleteScript on that ticket hits the early-return path. Pre-fix, that path didn't call Cleanup → orphaned CTS leaked. Fix: add `_cancellationRegistry.Cleanup(command.Ticket)` before the early return, matching the existing CancelScript pattern (Cleanup on every exit path).
  - **Audit Issue #2 — Null-ticket NRE in CancelScript / CompleteScript**: StartScript validated `command.ScriptTicket != null` and threw ArgumentException with structured message; the other two RPC handlers didn't, so a malformed RPC with null Ticket propagated into the registry which dereferences `ticket.TaskId` → unhandled NRE → opaque Halibut wrap → operator saw cryptic generic error. Fix: explicit null-check + ArgumentException in both, matching StartScript's defensive pattern. 3 additional tests pin both fixes (leak-path call-site + 2 null-ticket validations).

_Phase-12.A Windows Tentacle foundations (2026-04-27)_: 3 sub-phases, 25 new Tentacle tests. **Pure-additive abstraction work — closes 0 audit findings, lays the platform-portable foundation for future Phase B/C/D/E (Windows process launcher / WindowsServiceHost / watchdog / upgrade).** Each sub-phase is its own commit + reverse-verified independently. Independent paranoid pre-merge audit verdict: **SAFE TO MERGE — all three commits are pure-additive abstractions with zero breaking changes on Linux. Test compatibility fully preserved. Critical path literals pinned by tests. Defensive error handling correct.**
  - **12.A.1 (commit 158c898) — `IFilePermissionManager`**: extracts the per-platform "lock down a file/dir to its owner" primitive that AtomicFileWriter has been doing inline as `if (OperatingSystem.IsWindows()) return; File.SetUnixFileMode(path, 0600)`. New 2-method interface: `RestrictToOwner(path, isDirectory)` (Linux: 0600 file / 0700 dir; Windows: ACL break-inheritance + Owner FullControl + strip rules) + `TrySetUnixMode(path, mode)` (Linux only — Windows no-ops true). Static `FilePermissionManagerFactory.Resolve()` matches the existing `PlatformPaths` convention. AtomicFileWriter now routes through the factory. Linux observable behaviour bit-for-bit identical (literal `0600` mode pinned by test). Windows path now actually does ACL hardening (was: silent no-op). 8 new tests covering factory selection, Linux mode pinning, Windows attribute presence, no-op safety on missing files. Reverse-verify confirmed.
  - **12.A.2 (commit 49754cf) — `IUpgradeStatusStorage`**: extracts the upgrade-metadata triple (`last-upgrade.json` + `upgrade-events.jsonl` + `upgrade.log`) read primitive that `CapabilitiesService` was hardcoding via 3 private static `Default*Reader` methods + 3 `Func<string>` injectable seams for tests. New 3-method interface: `ReadStatus()` / `ReadEvents()` / `ReadLog()`. Linux paths preserved bit-for-bit (`/var/lib/squid-tentacle/last-upgrade.json` etc, all pinned as `public const` per Rule 8 + tests). Windows variant uses `Path.Combine(PlatformPaths.GetSystemConfigDir(), @"upgrade\last-upgrade.json")` (typically `%ProgramData%\Squid\Tentacle\upgrade\…`). NullUpgradeStatusStorage fallback for non-Linux/non-Windows OS. New public `CapabilitiesService(…, IUpgradeStatusStorage)` ctor delegates internally to the existing 3-Func test ctor so all 6 existing CapabilitiesService tests continue to pass unmodified. `MaxUpgradeLogBytes` elevated to public static property `MaxUpgradeLogBytesValue`; `TailTruncateForMetadata` elevated internal → public for storage-impl reuse. 8 new tests covering factory selection, Linux path pinning, Windows path construction, null-fallback behaviour. Reverse-verify confirmed.
  - **12.A.3 (commit 3146a6d) — `IServiceUserProvider`**: extracts the "what user does the service run as / can we hand ownership to them" primitive that was duplicated across `ServiceCommand.DetectServiceUser` (shells `getent passwd squid-tentacle` + hardcoded `OperatingSystem.IsLinux()` guard) AND `InstanceOwnershipHandover` (3-Func injection seam doing the same getent + chown shell-out). New 4-member interface: `DefaultServiceUser` (Linux: `"squid-tentacle"`; Windows: `""` = LocalSystem; Null: `""`) + `IsRunningElevated()` (Linux: `Environment.UserName == "root"`; Windows: `WindowsPrincipal.IsInRole(Administrator)`) + `ServiceUserExists(user)` (Linux: shell `getent passwd $user`; Windows: assume true — `sc.exe install` will fail with clear error if invalid) + `TrySetOwnership(path, user)` (Linux: shell `chown -R $user:$user $path`; Windows: no-op true — Windows uses ACLs via `IFilePermissionManager`, not chown). Both pre-existing call sites refactored: `ServiceCommand.DetectServiceUser` now delegates to provider; `InstanceOwnershipHandover` gained a new public ctor accepting `IServiceUserProvider` that internally adapts to the existing 3-Func internal ctor — all 10+ existing handover tests continue to pass unmodified. Default parameterless ctor on InstanceOwnershipHandover routes through `ServiceUserProviderFactory.Resolve()`. 9 new tests covering factory selection, Linux user-detection (getent), Windows LocalSystem default, no-op chown on Windows. Reverse-verify confirmed.

Phase-12.A cumulative: closes 0/85 audit findings — this is **forward-feature foundation work**, not hardening closure. Cross-project totals after Phase-12.A: **4773 / 1302 / 139 = 6214 tests, 0 failures** (Phase-11 baseline 6189 + 25 new this bundle: 8 IFilePermissionManager + 8 IUpgradeStatusStorage + 9 IServiceUserProvider). 0 lines deleted from production behaviour on any existing platform; every literal that mattered (chown shell-out shape, getent invocation, 0600 mode, default service user `squid-tentacle`, status-file paths) is pinned by a Phase-12.A test so future refactors are compile/test-time visible. Two flaky failures observed in parallel-runner cross-suite ran (1× UnitTests, 1× Tentacle.Tests) reproduced on isolated reruns as 4773/0 + 1302/0 — confirmed as the known pre-existing xUnit-parallel × Serilog `Log.Logger` global-static race documented since Phase-9a, NOT a Phase-12.A regression.

_Phase-12.B Windows Tentacle process launcher layer (2026-04-28)_: 3 sub-phases + 1 audit-fix follow-up = 4 commits, 40 new Tentacle tests. **Pure-additive abstraction work — closes 0 audit findings, lays the second layer of Phase-12 Windows Tentacle foundations on top of Phase-12.A.** Phase-12.B is the first sub-phase of Phase-12 to actually ship a Windows-only code path (`WindowsPowerShellProcessLauncher` targeting `PowerShell.exe` + OEM stdout decoding) — but defaults preserve pre-Phase-12 behaviour bit-for-bit on every existing platform via a Rule-8-pinned env-var gate (`SQUID_TENTACLE_USE_WINDOWS_POWERSHELL`) defaulting to off. Independent paranoid pre-merge audit verdict: **HOLD on 1 concrete concern (env-var-mutating tests not in serialisation Collection — same Phase-10 bug class), then SAFE TO MERGE** after the one-line audit-fix commit.
  - **12.B.1 (commit 4003b01) — `IProcessLauncher` interface + Bash + PwshCore + factory**: extracts the per-syntax PSI-builder primitive that pre-Phase-12.B was duplicated across two private static helpers in `LocalScriptService` (`StartBashProcess` + `StartPwshProcess`). New 1-method interface `BuildStartInfo(workDir, args)` returning `ProcessStartInfo` (deliberately narrow — caller still owns `Process.Start` lifecycle, kill-tree, output pump, soft-cancel registry). Three impls: `BashProcessLauncher` (cross-platform; PSI bit-for-bit identical to deleted `StartBashProcess`), `PwshCoreProcessLauncher` (Linux/macOS pwsh + UTF-8; PSI bit-for-bit identical to deleted `StartPwshProcess`), and `ProcessLauncherFactory.Resolve(ScriptType)` for dispatch. `ScriptFileName` constants pinned (`script.sh` / `script.ps1`). Python/CSharp/FSharp + Calamari intentionally stay on the inline path — factory throws `NotSupportedException` if accidentally routed through. 14 new tests including PSI shape pinning per launcher + factory routing per syntax + Rule-8 const pin. Reverse-verify confirmed (FileName "bash"→"bash-broken" + argv "-File"→"-Broken" both caught). LocalScriptService unchanged in this commit — types unreachable from production until B.3.
  - **12.B.2 (commit d7a4637) — `WindowsPowerShellProcessLauncher`**: the actual NEW capability of Phase B — targets the OS-bundled `PowerShell.exe` (Windows PowerShell 5.1, NOT pwsh-Core) with OEM-codepage stdout decoding. FileName resolution: try canonical `%SystemRoot%\System32\WindowsPowerShell\v1.0\PowerShell.exe` first, fall back to bare `"PowerShell.exe"` on PATH for Nano Server / non-Windows test runners. Argv literal: `-NoProfile -NoLogo -NonInteractive -ExecutionPolicy Unrestricted -File script.ps1 [...userArgs]` (matches pwsh-Core argv shape — Windows PowerShell 4.0+ propagates `$LastExitCode` correctly via `-File`, no need for the more complex Octopus-style `-Command "& { ...; exit $LastExitCode }"` wrapper since Squid doesn't pass an encryption-key arg). `StandardOutputEncoding` = `Encoding.GetEncoding(CultureInfo.CurrentCulture.TextInfo.OEMCodePage)` with try/catch UTF-8 fallback for non-Windows test runners. `[SupportedOSPlatform("windows")]` attribute hint (analyzer-only, not runtime gate). 8 new tests (cross-platform PSI shape pinning + Windows-only OEM-codepage assert + Rule-8 const pins for ScriptFileName + FallbackFileName). Reverse-verify confirmed (argv "-NonInteractive"→"-Interactive-broken" caught with byte-by-byte diff). Type still unreachable from factory in this commit — B.3 wires the routing.
  - **12.B.3 (commit 363981c) — wire LocalScriptService through factory + env-var gate**: `LocalScriptService.StartProcess` now dispatches to `ProcessLauncherFactory.Resolve(syntax).BuildStartInfo(...)` via a new private `StartViaLauncher` helper; the duplicated `StartBashProcess` + `StartPwshProcess` private helpers are deleted (-50 lines net). Pure refactor on Linux + macOS (PSI bit-for-bit identical, all 78 existing real-bash + real-pwsh `LocalScriptServiceTests` + `MultiSyntaxScriptE2ETests` + `SensitiveVariablePasswordTransportTests` continue green). **Critical backward-compat decision**: Windows hosts ALSO default to `PwshCoreProcessLauncher` (pwsh + UTF-8) — NOT `WindowsPowerShellProcessLauncher` (PowerShell.exe + OEM) — because Windows PowerShell 5.1's OEM stdout silently mangles cross-locale Unicode (e.g. `你好` on en-US runners with cp437 → `"??"`), which would break the existing `Pwsh_UnicodeOutput_EncodedCorrectly` E2E test that asserts `ShouldContain("你好")`. Per Rule 1, default behaviour is preserved bit-for-bit. Operator opt-in via env var `SQUID_TENTACLE_USE_WINDOWS_POWERSHELL=true` (also accepts `1`/`yes`/`on` case-insensitive, with whitespace trimmed for cmd.exe-typo tolerance) flips routing to `WindowsPowerShellProcessLauncher`. Env var name pinned as `public const ProcessLauncherFactory.UseWindowsPowerShellEnvVar` per Rule 8. A future Phase 12.B-followup can address the encoding strategy (`chcp 65001` / `$OutputEncoding=[Text.Encoding]::UTF8` prepend in the bundle authoring layer) before promoting Windows PowerShell to the default. 15 new tests including 1 Windows-gated E2E (`Pwsh_OptInWindowsPowerShellExe_ViaEnvVar_RunsSuccessfully` — exercises the env var → factory → launcher → real `PowerShell.exe` chain with ASCII-only assertion since cross-locale Unicode via OEM is locale-dependent). Reverse-verify confirmed (constant value mutation `..._POWERSHELL`→`..._POWERSHELL_BROKEN` caught with byte-by-byte diff in the pin test).
  - **12.B-audit-fix (commit 86014c4) — env-var test serialisation + whitespace trim**: independent paranoid pre-merge audit caught 2 hidden risks. **A1 (real flake risk, same Phase-10 bug class)**: `ProcessLauncherTests` and `WindowsPowerShellE2ETests` both mutate `SQUID_TENTACLE_USE_WINDOWS_POWERSHELL` via `SetEnvironmentVariable` + try/finally restore, but xUnit2 runs classes IN PARALLEL — on the Windows nightly runner, the opt-in E2E test would race with the default-routing factory test → flake + potential leak past `finally` if `prior` was captured mid-race. Fix: new `TentacleEnvVarMutatorsCollection` (mirrors Phase-9a's `TentacleProcessIntegrationCollection` precedent and Phase-10's `KubernetesEnvVarMutators`) with `DisableParallelization=true`; both classes decorated with `[Collection(TentacleEnvVarMutatorsCollection.Name)]`. **A2 (UX nit)**: env var parser didn't tolerate `set FOO= true` cmd.exe-typo whitespace; fixed via `?.Trim()` + 3 additional theory inline-data entries pinning the trimmed-truthy contract. Both fixes shipped as one audit-fix commit per the Phase-9a/10/11/12.A finalize discipline.

Phase-12.B cumulative: closes 0/85 audit findings — **forward-feature foundation work**, not hardening closure. Cross-project totals after Phase-12.B: **4773 / 1342 / 139 = 6254 tests, 0 failures** (Phase-12.A baseline 6214 + 40 new this bundle: 14 B.1 + 8 B.2 + 15 B.3 + 3 audit-fix theory inline-data). 0 lines deleted from production behaviour on any existing platform on default settings; the WindowsPowerShellProcessLauncher path is gated behind the SQUID_TENTACLE_USE_WINDOWS_POWERSHELL env var so opt-in operators get the new capability while every existing deploy continues bit-for-bit identical. Every literal that mattered (PowerShell.exe argv shape, OEM codepage lookup, env var name, script-file names, fallback FileName) pinned by tests. Phase B's WindowsPowerShellProcessLauncher gates remain in active CI rotation (8 cross-platform PSI-shape tests run on macOS+Linux+Windows + 1 Windows-nightly E2E) so the gated path doesn't silently rot waiting for a future encoding-strategy phase.

_Phase-12.C Windows Tentacle ServiceHost layer (2026-04-28)_: 1 commit + 1 audit-fix follow-up = 2 commits, 27 new Tentacle tests. **Pure-additive replacement of a stub — closes 0 audit findings, fills in the Windows half of the IServiceHost trio (systemd / Windows Services / launchd).** Pre-Phase-12.C the `WindowsServiceHost` was a stub that always returned exit code 2 with "Windows Service install/uninstall/start/stop/status is not yet implemented for Squid Tentacle. Use 'sc.exe' manually …". Phase-12.C ships the real impl shelling to `sc.exe` — the same battle-tested tool Octopus Tentacle uses on Windows. Independent paranoid pre-merge audit verdict: **SAFE TO MERGE (9/10 confidence) with 2 minor observability concerns** (A1 documented inline; A2 deferred as a contract-wide follow-up since it shares semantics with the existing systemd path).
  - **12.C (commit 4ff79d6) — `WindowsServiceHost` real impl**: replaces the stub with a 5-method sc.exe-driven backend mirroring the shape of `SystemdServiceHost` (pure-function argv builders for testability + single shell-out helper). All 5 IServiceHost methods (Install / Uninstall / Start / Stop / Status) routed through `sc.exe` for a uniform error-message surface — operators see the SAME diagnostics they'd see invoking `sc.exe` directly. Why sc.exe and not `System.ServiceProcess.ServiceController`: ServiceController only exposes Start/Stop/Status, so Install/Uninstall would shell to sc.exe anyway — going through one tool everywhere keeps the dependency graph simpler (no Windows-only `System.ServiceProcess.ServiceController` NuGet) and the operator-facing error messages consistent. Pinned argv contract: `sc create <name> binPath= "<exe-quoted> <args>" DisplayName= <text> start= auto obj= <user>` — the mandatory single space after every `key=` (without it sc.exe parses the option as a positional arg → confusing usage error) is pinned by every BuildScCreateArgs test; binPath value wraps exe in escaped double quotes (defends against spaces in install path); empty/null Description falls back to ServiceName; empty/null RunAsUser falls back to "LocalSystem" (Phase-12.A.3 contract). Constants pinned per Rule 8 (`DefaultServiceUser = "LocalSystem"` for SCM audit-trail filter literal; `ScExeFileName = "sc.exe"` for PATH-resolved bare name). Already-installed (sc.exe exit 1073) → clear "uninstall first" error message; already-deleted on uninstall (1060) → idempotent success matching the systemd path's silent overwrite semantics. Auto-start: sc create + sc start as one-shot (mirrors systemd's daemon-reload + enable + start). Out of scope for Phase-12.C: LSA `SeServiceLogonRight` grant + WMI `Win32_Service.Change` for non-system user identities (Phase-12.C-followup), real Windows runner E2E test (would need GHA windows-latest workflow, deferred), watchdog / restart policy (Phase D — Task Scheduler). 27 new tests covering 9 BuildScCreateArgs paths (simple/empty-args/null-args/multi-args/empty-desc/null-desc/empty-user/null-user/custom-user) + 4 trivial verb pins (delete/start/stop/query) + 4 public-surface (DefaultServiceUser const, ScExeFileName const, DisplayName, IsSupported per-OS gate) + 10 validation guards (null req + 3×ServiceName empty/null/whitespace + 3×ExecStart empty/null/whitespace + 3×Uninstall name empty/null/whitespace). Reverse-verify confirmed (dropping the binPath escaped-quote wrap fails 4 tests with byte-by-byte diff).
  - **12.C-audit-fix (commit 298524d) — WorkingDirectory drop documented**: paranoid audit caught one observability concern (A1) — sc.exe has no `WorkingDirectory=` option so `ServiceInstallRequest.WorkingDirectory` is silently ignored on Windows while the systemd path honours it. Functionally non-issue because the Tentacle binary uses `Environment.ProcessPath` + absolute paths internally (a non-bin-relative cwd doesn't affect runtime), but the silent divergence from systemd was worth a doc comment in `BuildScCreateArgs` so future maintainers see the rationale without re-tracing the audit. A2 (Status() exit code semantics: returns SCM-found vs running/stopped) is pre-existing on the systemd path too — same behaviour, deferred as a contract-wide IServiceHost.Status XMLDoc clarification rather than a Phase-12.C-only fix.

Phase-12.C cumulative: closes 0/85 audit findings — **forward-feature foundation work**, not hardening closure. Cross-project totals after Phase-12.C: **4773 / 1369 / 139 = 6281 tests, 0 failures** (Phase-12.B baseline 6254 + 27 new this bundle). 0 lines deleted from production behaviour — the only IServiceHost impl that changed (`WindowsServiceHost`) was a stub returning exit code 2 + "not yet implemented" so swapping the contents is strictly additive capability. Two flaky failures observed in parallel-runner cross-suite ran (1× UnitTests `OutputVariableMergerTests.Merge_StrictMode_SensitiveValueNeverEchoedInLog`, 1× Tentacle `ScriptCancellationRegistryTests.HighConcurrency_GetOrCreateAndCancel_NoTornUpdates`) reproduced on isolated reruns as 12/0 + 9/0 — confirmed as the known pre-existing xUnit-parallel × Serilog `Log.Logger` global-static race documented since Phase-9a + a Phase-11 high-concurrency stress race documented since Phase-11; NEITHER is a Phase-12.C regression.

_Phase-12.D Windows Tentacle restart-on-failure policy via `sc failure` (2026-04-28)_: 1 commit, 22 new Tentacle tests. **Pure-additive Linux↔Windows parity closure — closes 0 audit findings, fills the restart-on-failure gap that Phase-12.C left between Linux's `Restart=on-failure / RestartSec=10 / StartLimitBurst=3` trio (already wired in `SystemdServiceHost.BuildUnitFile`) and Windows's bare `sc create + sc start` (Phase-12.C scope).** Pre-Phase-12.D, a Squid Tentacle Windows service that crashed (OOM, unhandled exception, kernel kill) just stayed dead until manual `sc start` — operationally unacceptable for the same reasons systemd ships `Restart=on-failure` by default. The Phase-12.C author flagged this in the 12.C scope-out comment ("watchdog / restart policy (Phase D — Task Scheduler)"), but on closer inspection systemd's existing Linux unit file does NOT use `WatchdogSec=` (process-side liveness probe) — only failure-driven restart. So the Linux↔Windows parity fix is the simpler `sc failure` actions, NOT an external Task Scheduler watchdog (which would be net-new capability beyond what Linux ships).
  - **12.D (commit 991673c) — `BuildScFailureArgs` + Install pipeline wiring**: new pure-function argv builder `BuildScFailureArgs(serviceName)` (no-arg overload using Phase-12.D defaults) + `BuildScFailureArgs(serviceName, restartCount, restartDelayMs, resetSeconds)` (explicit-args form for testability). Pinned argv contract: `sc failure <name> reset= <seconds> actions= restart/<ms>/restart/<ms>/restart/<ms>` — same mandatory single space after `=` quirk as Phase-12.C's `BuildScCreateArgs` (drop the space → sc.exe parses the option as a positional arg → confusing usage error → install succeeds with failure policy SILENTLY absent, so the contract pin matters). Defaults pinned per Rule 8 mirroring `SystemdServiceHost.BuildUnitFile` literals: `DefaultRestartCount=3` ↔ systemd `StartLimitBurst=3`, `DefaultRestartDelayMs=10_000` ↔ systemd `RestartSec=10`, `DefaultResetSeconds=86_400` (24h stable runtime → reset failure counter). Note: `resetSeconds` is NOT directly equivalent to systemd's `StartLimitIntervalSec=120` (rolling-window) — Windows has no rolling-window option, only "reset after stable runtime"; 24h chosen because it's the common Windows convention and short enough to recover from a transient bad day without permanently disarming the policy. Install pipeline updated to `sc create` → **`sc failure`** → `sc start` — failure policy applied BEFORE first start so boot-1 has the policy active. Soft-fail rationale: if `sc failure` somehow returns non-zero (stripped Windows Server Core, locked-down SCM ACL), the service is already created — aborting would leave a half-configured service. Warning printed naming the exact `sc failure` command to run manually; service still starts without restart policy (matches pre-Phase-12.D behaviour). 22 new tests covering 4 Theory inline-data restart counts (1/2/3/5), 1 canonical default-values pin, 1 no-arg-overload-routes-through-defaults-via-constants symmetry test, 1 mandatory-space-after-equals reverse-verify guard, 1 custom-delay propagation, 1 custom-reset propagation, 3 InlineData empty-service-name guards, 3 InlineData non-positive-restart-count guards, 2 InlineData negative-delay guards, 2 InlineData negative-reset guards, 3 Rule-8 const pins. Three reverse-verifies confirmed: drop space after `actions=` → 7 tests fail; off-by-one in actions list (emit count-1 entries) → 6 tests fail; mutate `DefaultRestartCount` 3→4 → 2 tests fail (canonical + const pin). Out of scope for Phase-12.D: operator-tunable restart count/delay via env-var (Rule 8 escape hatch) — defer to Phase-12.D-followup pending operator demand; Task Scheduler-based external watchdog (Halibut polling failure detection) — defer to a future phase since not Linux-parity work; ServicesPipeTimeout registry write — explicitly REJECTED because it's a system-wide registry key affecting all services on the host (blast radius unacceptable, NOT systemd-equivalent). Real Windows runner E2E test (would need GHA windows-latest workflow) — deferred per Phase-12.B/C precedent.

Phase-12.D cumulative: closes 0/85 audit findings — **forward-feature foundation work + Linux↔Windows parity closure**, not hardening closure. Cross-project totals after Phase-12.D: **4773 / 1391 / 139 = 6303 tests, 0 failures** (Phase-12.C baseline 6281 + 22 new this bundle). 0 lines deleted from production behaviour — the existing `Install` pipeline still runs `sc create` then `sc start` on the same code path; the new `sc failure` invocation is inserted between them with soft-fail semantics so any environmental quirk that breaks `sc failure` falls back to pre-Phase-12.D behaviour with a warning. Two flaky failures observed in parallel-runner cross-suite (1× UnitTests `SensitiveValueLeakGuardTests.ShouldForceSensitive_WarnMode_…`, 1× Tentacle `ScriptCancellationRegistryTests.HighConcurrency_GetOrCreateAndCancel_NoTornUpdates`) reproduced on isolated reruns as 17/0 + 1/0 — same pre-existing xUnit-parallel × Serilog `Log.Logger` global-static race class documented since Phase-9a + the Phase-11 high-concurrency stress race; NEITHER is a Phase-12.D regression.

_Phase-12.E.0 Windows Tentacle build pipeline + installer (2026-04-29)_: 1 commit, 0 new C# tests (artifact-only delivery — 2 new files: `.github/workflows/build-publish-windows-tentacle.yml` + `deploy/scripts/install-tentacle.ps1`). **Pure-additive distribution infrastructure — closes 0 audit findings, unblocks Phase 12.E.1-E.3 (which need Windows artifacts to download).** Pre-Phase-12.E.0 every Windows piece (12.A/B/C/D) compiled but never produced a downloadable Windows binary — `tentacle-windows-e2e.yml` only ran the test suite on `windows-latest`, no `build-publish-windows-tentacle.yml` existed, GitHub Releases had no `*-win-*.zip` artefacts. Phase 12.E.0 fills that gap. Independent design analysis (cross-checked against Octopus's official `automating-tentacle-installation.mdx` docs at https://github.com/OctopusDeploy/docs) decisively chose **self-contained `.zip` over MSI** for the v0 distribution format — see PR description for the full reasoning chain (TL;DR: Octopus's MSI is a thin file-extract wrapper + Add/Remove Programs entry, not a fundamentally different artefact; Squid's `.zip + install-tentacle.ps1 + Phase-12.C sc.exe` covers the same operator scenarios at a fraction of the build complexity (no WiX, no windows-latest runner, no Authenticode signing prerequisite); MSI deferred to Phase 12.E.4 if a real operator asks for SCCM/GPO mass deploy).
  - **12.E.0 (commit 5dd3a89) — `build-publish-windows-tentacle.yml` + `install-tentacle.ps1`**: new GitHub Actions workflow mirrors `build-publish-linux-tentacle.yml`'s shape (GitVersion → matrix self-contained publish → tag-only Release upload) but drops the Docker image job (Windows base images are 6+ GB; defer to a follow-up if anyone ever wants "Tentacle in Windows container"). 2 RIDs in the matrix: `win-x64` + `win-arm64`. Cross-compiled from `ubuntu-latest` to save Windows runner cost (.NET 9 self-contained `dotnet publish -r win-x64 --self-contained true -p:PublishSingleFile=true` is officially supported and produces bit-identical output to a native Windows publish — the Windows-specific runtime work in 12.A/B/C/D is exercised by the existing `tentacle-windows-e2e.yml` workflow on `windows-latest`, not by the build pipeline). `softprops/action-gh-release@v2` with `append_body: true` co-exists with the Linux workflow on the same Release object: operators end up with one Release per version containing BOTH Linux `tar.gz` AND Windows `zip` files. The companion `install-tentacle.ps1` (326 lines) mirrors `install-tentacle.sh`'s arg shape (`-Version`, `-InstallDir`, `-DownloadBase`, env-var fallbacks) + UX one-liner pattern (`irm | iex`); detects arch via `$env:PROCESSOR_ARCHITECTURE` (AMD64 → win-x64, ARM64 → win-arm64; x86 explicitly errors per Phase-12.E analysis — no real audience in 2026); validates Administrator elevation when targeting the default `C:\Program Files\Squid Tentacle` install path; falls back to user-owned dir without elevation; downloads zip via `Invoke-WebRequest -UseBasicParsing -TimeoutSec 300` (mirrors `curl --max-time 300`); extracts via `Expand-Archive -Force` (matches Linux's `tar xzf` overwrite UX); idempotently adds inbound TCP/10933 firewall rule via `New-NetFirewallRule` (preempts the Listening flow without blocking Polling); calls `squid-tentacle service install --instance Default --service-name squid-tentacle` which routes through Phase-12.C's `WindowsServiceHost` → Phase-12.D's `sc create + sc failure + sc start`. Pinned literals per Rule 8: default install dir, default service name, listening port, RID-detection table, default download base URL. Validation done locally: YAML parsed clean via `python yaml.safe_load`; PowerShell deferred to GHA `windows-latest` runner because no `pwsh` on the macOS dev box (matches Phase-12.B/C/D's "real Windows runner E2E deferred" precedent — first `workflow_dispatch` run will surface any syntax errors). Out of scope for Phase-12.E.0: `Dockerfile.Tentacle.Windows` (defer if container demand emerges); MSI/WiX (defer to 12.E.4 pending operator request); Authenticode code signing (defer until SmartScreen warnings actually frustrate an operator); Chocolatey / winget package metadata (community can publish wrappers around the same zip without server-side coordination).

Phase-12.E.0 cumulative: closes 0/85 audit findings — **forward-feature distribution infrastructure**, unblocks Phase 12.E.1-E.3 (which require Windows artefacts to actually download during the upgrade flow). Cross-project totals after Phase-12.E.0: **unchanged at 4773 / 1391 / 139 = 6303 tests, 0 failures** (no C# code touched — 2 new artefact files are workflow YAML + PowerShell script, neither compiled into the .NET test surface). Real validation gate is the first `workflow_dispatch` run on the new workflow (will be smoke-tested via a pre-release tag like `v1.0.0-rc.windows.1` before any production tag is cut).

_Phase-12.E.1 Windows Tentacle upgrade foundation — `IWindowsUpgradeMethod` interface + Windows version source via GitHub Releases API (2026-04-30)_: 1 commit, 14 new UnitTests. **Pure-additive interface scaffold + version source — closes 0 audit findings, lays the C# server-side foundation for Phase 12.E.2 (`ZipUpgradeMethod` + PowerShell upgrade script) and Phase 12.E.3 (`WindowsTentacleUpgradeStrategy` + OS-aware strategy resolution).** Pre-Phase-12.E.1 there was no `IWindowsUpgradeMethod` interface and no version-source path that could resolve Windows tentacle versions (the existing `TentacleVersionRegistry.GetLatestVersionAsync(style)` keyed on `CommunicationStyle` which is identical for Linux + Windows tentacles — both use `TentaclePolling` / `TentacleListening` since they speak the same Halibut wire). Phase 12.E.1 ships the missing scaffolding while deliberately deferring the OS-aware strategy resolution problem to Phase 12.E.3 (where the resolver collision becomes unavoidable).
  - **12.E.1 (commit 852f84d) — `IWindowsUpgradeMethod` interface + `TentacleVersionRegistry.GetLatestWindowsVersionAsync` + Windows env-var pin**: new interface mirrors `ILinuxUpgradeMethod` shape exactly (3 members: `Name`, `RenderDetectAndInstall(targetVersion)`, `RequiresExplicitSwap`) with PowerShell-snippet contract (`$INSTALL_OK`, `$INSTALL_METHOD` PowerShell variables) instead of bash. The contract symmetry is intentional: Phase 12.E.3's `WindowsTentacleUpgradeStrategy` will dispatch through `IWindowsUpgradeMethod` chain in priority order (chocolatey → MSI → zip; Phase 12.E.2 ships the zip method only). New public consts on `TentacleVersionRegistry`: `WindowsOverrideEnvVar = "SQUID_TARGET_WINDOWS_TENTACLE_VERSION"` (Rule 8 pinned) for operator-driven version pinning, and `WindowsLatestReleaseUrl = "https://api.github.com/repos/SolarifyDev/Squid/releases/latest"` (Rule 8 pinned) — note GitHub API endpoint, NOT Docker Hub, because Phase 12.E.0 ships `.zip` artefacts via GitHub Releases with no Windows Docker image planned. New public method `GetLatestWindowsVersionAsync(CancellationToken)` does: env override → live GitHub Releases query → empty fallback. Strips leading `v` from `tag_name` so the result matches install-tentacle.ps1's URL pattern (`squid-tentacle-{version}-{rid}.zip`). User-Agent header set per GitHub API requirement (without it, GitHub returns 403). NO TTL cache yet — deferred to Phase 12.E.3 when the call site (`WindowsTentacleUpgradeStrategy`) actually exists; adding a cache without a caller is over-engineering. 14 new tests covering: 3 IWindowsUpgradeMethod contract pins via stub impl (Name/RenderDetectAndInstall/RequiresExplicitSwap) — the interface signature is compile-time pinned by having a class IMPLEMENT it inside the test surface so any signature drift breaks compilation; 11 TentacleVersionRegistry tests covering the Windows env var name + GitHub URL constant pins, env-override happy path with whitespace trim, blank-override fall-through (Theory: empty + whitespace-only), GitHub API success with `v` prefix stripping, GitHub API success without `v` prefix passed through, GitHub API 404 → empty, malformed JSON → empty without throw, missing `tag_name` field → empty without throw. Two reverse-verifies confirmed: env var name mutation `SQUID_TARGET_WINDOWS_TENTACLE_VERSION` → `..._BROKEN` fails 1 test (`OverrideEnvVar_WindowsConstantNamePinned`); v-strip removal fails 1 test (`GetLatestWindowsVersionAsync_GitHubApi_ReturnsTagNameFromLatestRelease`). Out of scope for Phase-12.E.1: `WindowsTentacleUpgradeStrategy` stub impl (the strategy class would collide with `LinuxTentacleUpgradeStrategy.CanHandle("TentaclePolling/Listening")` at the `MachineUpgradeService.ResolveStrategy` "exactly one owner" invariant — Phase 12.E.3 widens `IMachineUpgradeStrategy.CanHandle` to also accept Machine context for OS-aware routing); concrete `IWindowsUpgradeMethod` impls (`ZipUpgradeMethod` ships in Phase 12.E.2); TTL cache for the Windows version source (defer to E.3 when there's a caller).

Phase-12.E.1 cumulative: closes 0/85 audit findings — **forward-feature foundation work**, not hardening closure. Cross-project totals after Phase-12.E.1: **4787 / 1391 / 139 = 6317 tests, 0 failures** (Phase-12.E.0 baseline 6303 + 14 new UnitTests this bundle). 0 lines deleted from production behaviour — both new entities (`IWindowsUpgradeMethod` interface + `GetLatestWindowsVersionAsync` method) are net-new public surface that no existing code path calls; the existing `GetLatestVersionAsync(style)` for Linux/K8s is unchanged. Phase 12.E.2 will add the first concrete `IWindowsUpgradeMethod` impl (`ZipUpgradeMethod`); Phase 12.E.3 will wire `GetLatestWindowsVersionAsync` to the new `WindowsTentacleUpgradeStrategy`.

_Phase-12.E.2 First concrete IWindowsUpgradeMethod (`ZipUpgradeMethod`) + `upgrade-windows-tentacle.ps1` template (2026-05-04)_: 1 commit, 16 new UnitTests. **Pure-additive — closes 0 audit findings, ships the universal-fallback Windows upgrade method (zip download → SHA256 verify → Expand-Archive → Move-Item swap → service restart → health check) plus the embedded PowerShell template that Phase 12.E.3's `WindowsTentacleUpgradeStrategy` will load + render at dispatch time.** Mirrors `TarballUpgradeMethod` shape exactly: marker class emits `$INSTALL_METHOD = 'zip'` PowerShell snippet, leaving the actual download/extract/swap logic in the template (~300-line .ps1 file, embedded as `Squid.Core.Resources.Upgrade.upgrade-windows-tentacle.ps1`).
  - **12.E.2 (commit 6c32dcd) — `ZipUpgradeMethod` + `upgrade-windows-tentacle.ps1`**: new `IWindowsUpgradeMethod` impl following the same marker-class discipline as `TarballUpgradeMethod` — `Name = "zip"`, `RequiresExplicitSwap = true`, `RenderDetectAndInstall` emits a 7-line PowerShell snippet that gates on `$INSTALL_OK -ne $true` (the analogue of Linux's `if [ "$INSTALL_OK" != "1" ]`), sets `$INSTALL_METHOD = 'zip'`, logs a `[upgrade-method:zip]` decision tag, and DOES NOT set `$INSTALL_OK = $true` (the template's zip-execution block does that after Invoke-WebRequest + Get-FileHash + Expand-Archive + Move-Item all succeed). Variable name parity (`$INSTALL_OK` / `$INSTALL_METHOD`) deliberately matches Linux's bash convention so operators reading both Linux + Windows upgrade logs see identical decision tags. New PowerShell template `src/Squid.Core/Resources/Upgrade/upgrade-windows-tentacle.ps1` (~300 lines) mirrors `upgrade-linux-tentacle.sh`'s structure: header documenting placeholders + exit codes, `$ErrorActionPreference = 'Stop'` + `Set-StrictMode`, arch detection via `$env:PROCESSOR_ARCHITECTURE` (AMD64 → win-x64, ARM64 → win-arm64; x86 explicitly errors at exit 1 per Phase 12.E.0 design), placeholder substitutions (`{{TARGET_VERSION}}` / `{{DOWNLOAD_URL}}` / `{{EXPECTED_SHA256}}` / `{{INSTALL_DIR}}` / `{{SERVICE_NAME}}` / `{{HEALTHCHECK_URL}}` / `{{INSTALL_METHODS}}`), per-host idempotency lock at `%ProgramData%\Squid\Tentacle\upgrade\upgrade.lock` (NOT per-target-version, mirroring Linux's lock-file-shape lesson learned in 1.6.x), atomic status file writes via temp+rename to `last-upgrade.json` (Phase 12.A.2 contract — server's `WindowsUpgradeStatusStorage` already reads from this exact path via Capabilities RPC), already-on-target short-circuit (defence-in-depth against stale runtime cache), `INSTALL_METHODS` injection point (server replaces with concatenated snippets from each `IWindowsUpgradeMethod`'s render output — chocolatey + MSI in future Phase 12.E.4, zip only in 12.E.2), zip-execution block (download with `Invoke-WebRequest -UseBasicParsing -TimeoutSec 600`, SHA256 verify with `Get-FileHash`, extract with `Expand-Archive -Force` to `%TEMP%\squid-tentacle-staging-<guid>`), Phase B (Stop-Service → backup INSTALL_DIR → Move-Item swap → Start-Service → health check loop polling every 2s for 60s). The template is written ASSUMING the Phase 12.E.3 strategy has already detached the script process from the Tentacle service tree (likely via Task Scheduler one-shot, equivalent to Linux's `systemd-run --scope`) — the detach mechanism is an orchestration concern owned by E.3, not the template. 16 new tests across two files: 5 ZipUpgradeMethodTests (Name pin, RequiresExplicitSwap = true, marker emits uncommented `$INSTALL_METHOD = 'zip'` PowerShell assignment AND does NOT set `$INSTALL_OK = $true`, gates on `$INSTALL_OK -ne $true`, logs `[upgrade-method:zip]` + "fallback" tag) + 11 WindowsUpgradeScriptResourceTests (resource-embedded-in-assembly pin, content-non-trivial pin >500 bytes, 7 InlineData placeholder pins for every `{{...}}` token the strategy will substitute, arch detection contains `PROCESSOR_ARCHITECTURE`, header documents exit codes). Two reverse-verifies confirmed: (1) renaming `{{TARGET_VERSION}}` → `{{TARGETVERSION}}` in the .ps1 fails the placeholder Theory test for that token; (2) commenting out the `$INSTALL_METHOD = 'zip'` assignment in the C# snippet fails `Render_OnlySetsInstallMethod_NotInstallOk` once the test is upgraded from ShouldContain (substring match — fooled by the still-present commented-out string) to a per-line uncommented-assignment scan (catches the regression). Out of scope for Phase-12.E.2: `WindowsTentacleUpgradeStrategy` (Phase 12.E.3 wires the template loader + the Halibut dispatch); `MsiUpgradeMethod` / `ChocolateyUpgradeMethod` (Phase 12.E.4 deferred); the Task Scheduler detach mechanism for Phase B survival across `Stop-Service` (Phase 12.E.3 concern); real Windows runner E2E test (would need GHA windows-latest workflow, deferred per Phase 12.B/C/D/E.0 precedent).

Phase-12.E.2 cumulative: closes 0/85 audit findings — **forward-feature work**, not hardening closure. Cross-project totals after Phase-12.E.2: **4803 / 1391 / 139 = 6333 tests, 0 failures** (Phase-12.E.1 baseline 6317 + 16 new UnitTests this bundle). 0 lines deleted from production behaviour — `ZipUpgradeMethod` is a new `IWindowsUpgradeMethod` impl with no callers yet (Phase 12.E.3 will register it in the chain), and `upgrade-windows-tentacle.ps1` is a new embedded resource not yet loaded by any code path. One flaky failure observed in parallel-runner cross-suite (1× UnitTests `VariableEncryptionServiceLogLevelTests.Encrypt_NeverEmitsValueInLogPayload`) reproduced on isolated rerun as 3/0 — same pre-existing xUnit-parallel × Serilog `Log.Logger` global-static race class documented since Phase-9a; NOT a Phase-12.E.2 regression.

_Phase-12.E.3 OS-aware strategy resolution + `WindowsTentacleUpgradeStrategy` routing-only stub (2026-05-04)_: 1 commit, 31 new UnitTests. **Pure-additive routing scaffold — closes 0 audit findings, ships the resolver-axis change (`IMachineUpgradeStrategy.CanHandle` widened to take `MachineRuntimeCapabilities`) and the registered Windows strategy stub that completes the (style, OS) routing tuple. Actual Halibut dispatch / PowerShell template render / Task Scheduler detach for Windows is deferred to Phase 12.E.4.** Pre-Phase-12.E.3 the resolver in `MachineUpgradeService.ResolveStrategy` keyed only on `CommunicationStyle`; because Linux + Windows tentacles share the same wire-protocol values (`TentaclePolling` / `TentacleListening`) — Halibut doesn't distinguish OSes at the protocol layer — registering a second strategy for Windows would have tripped the "exactly one owner" invariant the moment any tentacle upgrade fired. Phase 12.E.3 widens the resolver key from style alone to (style, OS) by threading the per-machine `IMachineRuntimeCapabilitiesCache.TryGet().Os` (populated on every health check) into `CanHandle`. The Linux strategy now explicitly skips `Os == "Windows"`; the new Windows strategy claims it. K8s strategy ignores capabilities (no OS variant — pods are always Linux). Cold-cache (no health check yet) → empty `Os` → falls into the Linux branch as the historical default, preserving pre-Phase-12 behaviour for the existing operator base (overwhelmingly Linux); a real Windows tentacle would have run a health check before any operator-driven upgrade in practice (FE only shows the upgrade affordance for healthy targets).
  - **12.E.3 (commit 6b74926) — `IMachineUpgradeStrategy.CanHandle(style, capabilities)` widening + `WindowsTentacleUpgradeStrategy` (NotSupported stub) + OS-aware `MachineUpgradeService.ResolveStrategy`**: 4 production files modified + 1 new (`WindowsTentacleUpgradeStrategy.cs`), all changes pure-additive at the public-surface boundary — `CanHandle` gained a second parameter (the existing single-arg version is gone, but the only call site is `ResolveStrategy` itself which migrated atomically; no external implementer of `IMachineUpgradeStrategy` exists outside the `Squid.Core` assembly). `MachineUpgradeService.ResolveStrategy` now reads `_runtimeCache.TryGet(machine.Id) ?? MachineRuntimeCapabilities.Empty` and threads through both code paths (the upgrade dispatch in `RunUpgradeAsync` AND the read-only info probe in `ResolveLatestForInfoAsync` + `EvaluateUpgradeEligibility`). The `Linux` strategy gains an explicit Windows-skip override — `CanHandle` returns false when `capabilities.Os.Equals("Windows", StringComparison.OrdinalIgnoreCase)` — every other OS string (`"Linux"`, `"linux"`, `"macOS"`, empty) still claims, matching the pre-Phase-12 default-to-Linux behaviour that production deployments depend on. The new `WindowsTentacleUpgradeStrategy` is a registered class (auto-registered via `IScopedDependency`) whose `UpgradeAsync` returns `MachineUpgradeStatus.NotSupported` with a roadmap-aligned hint pointing operators at Phase 12.E.4 + the manual `install-tentacle.ps1` fallback. The stub is intentional (not a bug): with the resolver now widened, registering NO Windows strategy would correctly route a Windows agent to "no strategy registered for style 'TentaclePolling'", but operators wouldn't know whether Windows is fundamentally unsupported or just unfinished. The stub draws the line clearly: Windows agents are recognised, dispatch arrives in 12.E.4. The `(style, OS) tuple` exception message in `ResolveStrategy` was updated from `"Each style must have exactly one owner"` → `"Each (style, OS) tuple must have exactly one owner"` so a future Windows-tentacle author who accidentally widens the Windows strategy's `CanHandle` to also claim Linux sees the correct invariant in the failure log. 31 new unit tests across 4 files: 16 in `WindowsTentacleUpgradeStrategyTests` (new file — Theory-driven Os routing matrix: Tentacle styles × {Windows / windows / WINDOWS / Linux / macOS / empty / null caps}, plus 3 `UpgradeAsync` Facts pinning the NotSupported + roadmap-hint + `12.E.4` mention + `install-tentacle.ps1` mention + `AgentVersionMayHaveChanged = false` contract); 9 net-new in `LinuxTentacleUpgradeStrategyTests` (new `CanHandle_RoutesByOs_SkipsWindowsAgents` Theory with 8 InlineData covering case-insensitive Windows skip + non-Windows-default-claim + cold-cache-claim + `CanHandle_NullCapabilities_TreatedAsEmpty` Fact); 3 net-new in `KubernetesAgentUpgradeStrategyTests` (`CanHandle_KubernetesAgentStyle_IgnoresOsCapability` Theory with 3 InlineData asserting the K8s strategy claims regardless of `capabilities.Os` since pods are always Linux); 3 net-new orchestrator-level tests in `MachineUpgradeServiceTests` (`UpgradeAsync_WindowsAgent_RoutesToWindowsStrategyNotLinux` + `UpgradeAsync_LinuxAgent_RoutesToLinuxStrategy_WindowsRegistered` + `UpgradeAsync_ColdCacheNoOs_RoutesToLinuxAsHistoricalDefault` — all three use the real (non-mock) `InMemoryMachineRuntimeCapabilitiesCache` so the cache-read seam in `ResolveStrategy` is end-to-end-pinned). One pre-existing test updated: `UpgradeAsync_TwoStrategiesClaimSameStyle_ThrowsWithBothClassNames` expectation moved from `"Each style must have exactly one owner"` → `"Each (style, OS) tuple must have exactly one owner"` (the invariant message change). Out of scope for Phase-12.E.3: the actual Halibut dispatch + PowerShell template render in `WindowsTentacleUpgradeStrategy.UpgradeAsync` (Phase 12.E.4 — wires the embedded `upgrade-windows-tentacle.ps1` Phase-12.E.2 ships, concatenates the registered `IWindowsUpgradeMethod` snippets into `{{INSTALL_METHODS}}`, sends via Halibut, observes); the Task Scheduler one-shot detach mechanism (Phase 12.E.4 — Windows analogue of Linux's `systemd-run --scope` for surviving the Tentacle service stop in Phase B); per-machine OS-source-of-truth migration from runtime cache to entity column (separate audit thread — current cache-only model is acceptable while Windows is stub-only since `NotSupported` is the same answer for cold + warm cache).

Phase-12.E.3 cumulative: closes 0/85 audit findings — **forward-feature routing-axis work**, not hardening closure. Cross-project totals after Phase-12.E.3: **4834 / 1391 / 139 = 6364 tests, 0 failures** (Phase-12.E.2 baseline 6333 + 31 new UnitTests this bundle). 0 lines deleted from production behaviour — the resolver widening preserves the exact pre-Phase-12 behaviour for cold-cache + non-Windows agents (Linux strategy still claims), and the Windows strategy stub is a new claim path with no callers depending on its prior absence. Reverse-verifies confirmed: (1) deleting `LinuxTentacleUpgradeStrategy`'s explicit `IsWindowsAgent` skip → both strategies claim the Windows case → resolver throws the "(style, OS) tuple" InvalidOperationException → `MachineUpgradeServiceTests.UpgradeAsync_WindowsAgent_RoutesToWindowsStrategyNotLinux` and `UpgradeAsync_TwoStrategiesClaimSameStyle_ThrowsWithBothClassNames` both fail; (2) commenting out the `_runtimeCache.TryGet(machine.Id)` in `RunUpgradeAsync` (passing `Empty` literally to the resolver) → Windows machines route to Linux → `UpgradeAsync_WindowsAgent_RoutesToWindowsStrategyNotLinux` fails because the Linux mock dispatches instead of the Windows mock.

_Phase-12.E.4 Windows Tentacle real upgrade dispatch — Halibut + Task Scheduler one-shot detach + `ITentacleVersionRegistry` OS-aware widening + 4 `.ps1` polish fixes (2026-05-04)_: 1 commit, 51 net-new UnitTests. **Closes the last gap in the in-UI Windows tentacle upgrade story — replaces the Phase-12.E.3 routing-only stub with a real `WindowsTentacleUpgradeStrategy.UpgradeAsync` that dispatches a PowerShell wrapper via Halibut, schedules a SYSTEM-identity Task Scheduler one-shot task to run the Phase-12.E.2 `upgrade-windows-tentacle.ps1` template detached from the squid-tentacle service tree, and threads OS-aware routing through the version registry so Windows agents resolve their target version from GitHub Releases instead of the Linux Docker Hub repo.** Pre-Phase-12.E.4 the Windows strategy returned `NotSupported` with a 12.E.4 roadmap hint; this phase ships the real dispatch end-to-end. Closes 0/85 audit findings — pure forward-feature work, but ALSO closes the architectural-review-surfaced fragility in Phase-12.E.2's PowerShell template (4 polish fixes) before any real upgrade exercises them.
  - **12.E.4 (commit b5bff7c) — `WindowsTentacleUpgradeStrategy.UpgradeAsync` Halibut dispatch + outer-wrapper Task Scheduler detach + `ITentacleVersionRegistry.GetLatestVersionAsync(style, capabilities, ct)` widening + 4 `.ps1` polish fixes**: 4 production files modified + 0 new files; 4 test files modified + 0 new files. **Strategy implementation** (mirrors `LinuxTentacleUpgradeStrategy` shape exactly): `_halibutClientFactory + _observer` constructor injection (DI scoped via `IScopedDependency` marker on the parent interface); `UpgradeAsync` runs `validate → ParseHalibutEndpoint → BuildStartScriptCommand → DispatchAsync` pipeline; same `dispatchAcked`-flag exception map as Linux (`HalibutClientException` pre-dispatch → Failed, `HalibutClientException` post-acked → Initiated, `AgentUnreachableException` post-acked → Initiated, `OperationCanceledException` rethrows, generic → Failed); `BuildScript` renders the embedded `upgrade-windows-tentacle.ps1` template with all `{{...}}` placeholders substituted (TARGET_VERSION, DOWNLOAD_URL, EXPECTED_SHA256 (empty by default), INSTALL_DIR, SERVICE_NAME, HEALTHCHECK_URL, INSTALL_METHODS-via-`IWindowsUpgradeMethod`-chain), base64-encodes the inner, and wraps it in a ~50-line outer PowerShell script that writes the inner to `%ProgramData%\Squid\Tentacle\upgrade\dispatch.ps1` and calls `schtasks.exe /Create /TN SquidTentacleUpgrade_<GUID> /TR "powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File ..." /SC ONCE /ST 23:59 /RU SYSTEM /F /Z` followed by `schtasks /Run`; the `/Z` flag auto-deletes the task after run, the GUID-suffixed name guarantees cross-dispatch uniqueness (mirroring Linux's `systemd-run --unit=squid-upgrade-${VERSION}-${PID}` discipline). **Critical Windows-specific status divergence from Linux:** Linux observes a mid-script Halibut disconnect (the bash `exec` kills the original PID) and maps that to `Initiated`; Windows observes a clean `exit 0` from the outer wrapper and maps THAT to `Initiated` — wrapper success means task scheduled, NOT upgrade complete. Both arrive at `Initiated` after a successful kick-off, just via different signals. The actual outcome arrives via the rapid-polling burst (already wired in Phase 12.E.3, 30 jobs × 3s = 90s window) reading `last-upgrade.json` on the next Capabilities probe. **Why base64-encode the inner instead of a here-string:** PowerShell `@'...'@` here-strings terminate at `'@` at start of line; a future install method snippet (chocolatey/MSI) might happen to include such a sequence in a heredoc-style log. Base64 makes the inner content opaque to the wrapper's lexer. Operators can still inspect via the on-disk `dispatch.ps1`. **Env-var override constants pinned per Rule 8**: `SQUID_TARGET_WINDOWS_TENTACLE_DOWNLOAD_BASE_URL` (mirror of Linux's; defaults to `https://github.com/SolarifyDev/Squid/releases/download`), `SQUID_TARGET_WINDOWS_TENTACLE_HEALTHCHECK_URL` (mirror; defaults to `http://127.0.0.1:8080/healthz`). **`BuildDownloadUrl`**: pattern `{base}/{version}/squid-tentacle-{version}-{rid}.zip` with `{RID}` rewritten to PowerShell `$RID` for agent-side arch detection — mirrors Linux's tarball pattern except for the `.zip` extension; default install dir `C:\Program Files\Squid Tentacle` and service name `squid-tentacle` pinned per Rule 8 (operators using Phase-12.E.0's `install-tentacle.ps1` land in this exact path). **Version-registry OS-aware widening**: `ITentacleVersionRegistry.GetLatestVersionAsync(string communicationStyle, MachineRuntimeCapabilities capabilities, CancellationToken ct)` — capabilities-second-arg mirrors Phase 12.E.3's `IMachineUpgradeStrategy.CanHandle` widening for the same architectural reason: `TentaclePolling` / `TentacleListening` styles are shared between Linux + Windows tentacles (Halibut wire-protocol level), so style alone can't pick between Linux Docker Hub (Linux binary versions) and GitHub Releases (Windows zip versions). The implementation routes Windows-OS Tentacle styles to a private `GetLatestWindowsVersionAsync` (which existed as a public method in Phase 12.E.1; demoted to `internal` here so the existing 7 behavioural tests for the Windows version source still compile without losing coverage); cold cache (empty Os) defaults to Linux Docker Hub as the historical default — same default the Phase-12.E.3 strategy resolver uses. K8s Tentacles ignore the OS axis entirely (pods always run Linux from agent perspective). `MachineUpgradeService.ResolveTargetVersionAsync` + `ResolveLatestForInfoAsync` thread capabilities through. **4 `.ps1` polish fixes (architectural-review surfaced)**: (1) **SYSTEM/admin identity gate at script top** — exits 15 with operator-actionable message naming the wrapper as the failed component if the script ever runs without elevated identity (Phase B's Stop-Service / Move-Item would otherwise fail at random later points); (2) **SHA256 empty-skip** mirroring Linux's "Phase 1 default" (build pipeline doesn't yet publish per-archive hashes; without this guard, `$expectedSha=""` would `-ne $actualSha` and exit 7 EVERY upgrade); (3) **`$INSTALL_DIR.bak` path safety via `Split-Path -Parent` + `Split-Path -Leaf` + `Join-Path`** instead of string interpolation (an operator setting `INSTALL_DIR` with a trailing backslash would otherwise produce a hidden `.bak` directory inside the install dir, not a sibling); (4) **drop racy `Get-ChildItem | Sort LastWriteTime -Desc | Select -First 1` staging-dir search**, use the in-scope `$extractDir` variable directly (the host-scoped lock at line ~145 already prevents true concurrency, but the search added unnecessary ambiguity if any abandoned staging dir lingered in `%TEMP%`). **51 new tests across 4 files** (post-12.E.4 totals: 405 → 456 upgrade tests = +51 net): the 16 stub-era `WindowsTentacleUpgradeStrategyTests` replaced with 37 real-dispatch tests covering CanHandle (preserved verbatim from 12.E.3), 4 constant pins (Rule 8), download-URL pattern (3 default + 2 env-override + 1 trailing-slash + 1 non-HTTPS warning), healthcheck-URL resolution (1 default + 3 override variants), inner script render (5: placeholders all-substituted, target-version-injected, $RID-not-{RID}, INSTALL_METHODS-zip-snippet-injected, default-method-order-zip-only-for-Phase-12.E.4, EXPECTED_SHA256-empty-paired-with-template-skip), outer wrapper shape (6: contains schtasks, $InnerBase64 + FromBase64String, /RU SYSTEM + /Z + /F flags, GUID task-name suffix, -ExecutionPolicy Bypass, ProgramData dispatch.ps1 path, base64-roundtrip-decodes-to-rendered-inner), StartScriptCommand shape (3: full-GUID ticket, FullIsolation+PowerShell pinned, distinct tickets per dispatch), 7 dispatch branches (success → Initiated-not-Upgraded with explanatory comment, non-zero exit → Failed, empty-logs guard, Halibut mid-script disconnect → Initiated, liveness-probe abort → Initiated, pre-dispatch failure → Failed-not-Initiated, OperationCancelled rethrows, generic exception → Failed). 5 net-new TentacleVersionRegistryTests covering OS-aware routing through the unified API (Windows-OS-Tentacle → GitHub Releases, Linux-OS-Tentacle → Docker Hub, empty-OS → Linux historical default, KubernetesAgent ignores OS, null capabilities defensive). 6 net-new WindowsUpgradeScriptResourceTests pinning the 4 .ps1 polish fixes (identity gate present + exit-15 documented, SHA256 empty-skip path present, Split-Path-based bak construction present + string-interpolation form ABSENT (negative pin), Get-ChildItem racy search ABSENT + $extractDir Test-Path guard present). **Two reverse-verifies confirmed**: (1) deleting the `result.Success && result.ExitCode == 0 → Initiated` branch in `WindowsTentacleUpgradeStrategy.InterpretScriptResult` (substituting `Upgraded`) → `UpgradeAsync_ScriptSuccess_ReturnsInitiatedNotUpgraded` fails with the explicit "Windows ExitCode=0 means task scheduled, not upgrade complete" message — proves the Windows-specific divergence is test-pinned; (2) commenting out `IsWindowsTentacle` routing in `TentacleVersionRegistry.GetLatestVersionAsync` → `GetLatestVersionAsync_TentacleStyleWithWindowsOs_RoutesToGitHubReleases_NotDockerHub` fails because Windows-OS calls fall through to the Linux Docker Hub path. Out of scope for Phase-12.E.4: `ChocolateyUpgradeMethod` + `MsiUpgradeMethod` (Phase 12.E.5 will prepend them ahead of Zip in `DefaultMethodOrder`); E2E test on `windows-latest` runner exercising the full Halibut + Task Scheduler + `Stop-Service` + `Move-Item` + `Start-Service` round-trip (deferred per Phase 12.B/C/D/E.0 precedent — needs real Windows runner with elevated context); `WindowsUpgradeStatusStorage` server-side Capabilities-RPC integration test (Phase 12.A.2 contract is wired but not yet exercised end-to-end with a real upgrade).

Phase-12.E.4 cumulative: closes 0/85 audit findings — **forward-feature dispatch + architectural-review polish work**, not hardening closure. Cross-project totals after Phase-12.E.4: **4885 / 1391 / 139 = 6415 tests, 0 failures** (Phase-12.E.3 baseline 6364 + 51 new UnitTests this bundle). 0 lines deleted from production behaviour — the strategy widening preserves the existing Linux dispatch path bit-for-bit (`LinuxTentacleUpgradeStrategy` untouched) and the version-registry widening preserves the existing Linux+K8s call patterns (the new third arg is additive, with cold-cache + non-Windows-OS routing matching the prior style-only behaviour). The Phase-12.E.3 `WindowsTentacleUpgradeStrategy` stub is replaced by the real implementation; no caller change required (Autofac auto-resolves the same registered class via `IScopedDependency`).

_Phase-12.E.5 OS-routing genericity refactor — `AgentOperatingSystems` constants in Squid.Message + `IsWindows / IsLinux / IsMacOS / IsUnknown` predicates on `MachineRuntimeCapabilities` + Linux strategy explicit-claim semantic + cross-assembly pinning tests (2026-05-04)_: 1 commit, 49 net-new tests across two assemblies (44 in Squid.UnitTests, 5 in Squid.Tentacle.Tests). **Genericity hardening — closes 0/85 audit findings but resolves the architectural-review-flagged "Windows" / "Linux" string duplication across 4+ consumer sites (RuntimeCapabilitiesInspector + LinuxTentacleUpgradeStrategy + WindowsTentacleUpgradeStrategy + TentacleVersionRegistry) AND fixes the open-closed violation in Linux strategy's `!IsWindows`-shaped CanHandle — a future MacOSTentacleUpgradeStrategy can now plug in via DI auto-registration WITHOUT MODIFYING the Linux strategy, where previously the `!IsWindows` predicate would force-claim macOS and require Linux to add `&& !IsMacOS` (open-closed violation).** Pre-Phase-12.E.5 the literal `"Windows"` string was duplicated as a magic string in the agent inspector (`RuntimeCapabilitiesInspector.DetectOs`) and 3 server-side consumers (Linux strategy CanHandle, Windows strategy CanHandle, version-registry IsWindowsTentacle). A rename of any consumer would silently break OS routing — the resolver would fall through to "no strategy registered" because the cache would still carry the OLD string while the predicate checks the NEW one. Phase 12.E.5 centralizes the canonical strings into `AgentOperatingSystems` (in `Squid.Message`, the only assembly both `Squid.Tentacle` and `Squid.Core` reference) and surfaces them via property predicates on `MachineRuntimeCapabilities` so consumers read `capabilities.IsWindows` instead of comparing strings. **Critical semantic refactor**: `LinuxTentacleUpgradeStrategy.CanHandle` flipped from `!IsWindowsAgent(capabilities)` (claims everything-except-Windows) → `IsLinux || IsUnknown` (explicit claim). The behaviour change is intentional and architecturally significant: macOS / FreeBSD / any future OS string now gets a clean "no strategy registered" resolver error instead of being silently routed to Linux's tarball install (which would fail messily on the agent host). Cold-cache historical default preserved via `IsUnknown` (covers both empty `Os` field and explicit `"Unknown"` fallback from the agent's inspector when none of `OperatingSystem.IsWindows()` / `IsLinux()` / `IsMacOS()` matched).
  - **12.E.5 (commit b96867e) — `AgentOperatingSystems` const class + `Is{Windows,Linux,MacOS,Unknown}` predicates + Linux strategy explicit-claim refactor + cross-assembly pinning**: 5 production files modified + 1 new (`Squid.Message/Constants/AgentOperatingSystems.cs`); 5 test files modified + 2 new (`MachineRuntimeCapabilitiesOsConstantsTests.cs` server-side + `RuntimeCapabilitiesInspectorOsConstantsTests.cs` agent-side). **`AgentOperatingSystems`** lives in `Squid.Message/Constants/` next to existing cross-process contracts (`ScriptExitCodes`, `SpecialVariables`, `AuthenticationSchemeConstants`); declares 4 `public const string` values (`Windows = "Windows"`, `Linux = "Linux"`, `MacOS = "macOS"`, `Unknown = "Unknown"`) — pinned per Rule 8 by tests in BOTH consuming assemblies (server-side in Squid.UnitTests so a rename surfaces as a test failure on the resolver side; agent-side in Squid.Tentacle.Tests so a rename surfaces as a failure on the inspector side; the matching pair makes the cross-process contract drift impossible to land silently). **`MachineRuntimeCapabilities` predicates**: 4 new properties (`IsWindows`, `IsLinux`, `IsMacOS`, `IsUnknown`) all using `string.Equals(Os, AgentOperatingSystems.X, StringComparison.OrdinalIgnoreCase)` — case-insensitive matching mirrors the prior inline predicates. `IsUnknown` covers BOTH cold-cache (`string.IsNullOrWhiteSpace(Os)`) AND explicit-`"Unknown"`-sentinel cases so the Linux strategy's `IsLinux || IsUnknown` claim handles both pre-Phase-12 historical-default machines (no health check yet) and machines whose inspector hit the catch-all branch. `MachineRuntimeCapabilities.Empty` automatically reports `IsUnknown=true` (empty `Os` field) — pinning test asserts this so the cold-cache code path doesn't regress. **`RuntimeCapabilitiesInspector.DetectOs`**: refactored from `return "Windows"` literal to `return AgentOperatingSystems.Windows` (and same for the other 3 OS branches) — the agent and server now share the literal via the same const, so a rename surfaces as a build-time symbol-not-found on every consumer instead of a runtime "no strategy registered" silent breakage. **`LinuxTentacleUpgradeStrategy.CanHandle`** semantic flip: was `!IsWindowsAgent(capabilities)` → now `capabilities.IsLinux || capabilities.IsUnknown`. Null-defensive: `capabilities == null` still returns `true` (legacy test pattern preserves cold-cache behaviour). **`WindowsTentacleUpgradeStrategy.CanHandle`** + **`TentacleVersionRegistry.IsWindowsTentacle`**: replaced inline `Os.Equals("Windows", ...)` with `capabilities.IsWindows` property — semantically identical, code-shape generic. **49 new tests across 2 assemblies**: 39 in new `MachineRuntimeCapabilitiesOsConstantsTests` (4 const value pins + 10 IsWindows Theory cases including partial-match negative pin (`"Win"` must NOT match — guards against future agent rename to abbreviated form) + 7 IsLinux + 7 IsMacOS + 9 IsUnknown including `"FreeBSD"` no-claim + 1 Empty-sentinel-IsUnknown-true + 1 mutual-exclusivity-of-predicates), 5 in new `RuntimeCapabilitiesInspectorOsConstantsTests` (4 agent-side const pins + 1 live-inspector returns-canonical-string pin via `ShouldBeOneOf(Windows, Linux, MacOS, Unknown)`), 3 net-new InlineData rows in `LinuxTentacleUpgradeStrategyTests.CanHandle_RoutesByOs_ClaimsLinuxOrUnknownOnly` (renamed from `_SkipsWindowsAgents`) — `Unknown` claims, `MACOS` and `FreeBSD` do NOT claim (was `macOS`-claims under the old non-Windows predicate), 2 net-new InlineData rows in `WindowsTentacleUpgradeStrategyTests.CanHandle_RoutesByOs_OnlyClaimsWindowsAgents` (`Unknown` and `FreeBSD` correctly do not claim Windows). **Two reverse-verifies confirmed**: (1) renaming `AgentOperatingSystems.Windows` const value `"Windows"` → `"Win"` → both `MachineRuntimeCapabilitiesOsConstantsTests.AgentOperatingSystems_Windows_PinnedToCanonicalString` AND `RuntimeCapabilitiesInspectorOsConstantsTests.AgentOperatingSystems_Windows_PinnedFromAgentSide` fail; pre-Phase-12.E.5 the same rename would have built clean and silently broken every Windows tentacle's upgrade routing. (2) Reverting Linux strategy's `CanHandle` to the old `!IsWindowsAgent(capabilities)` form → `LinuxTentacleUpgradeStrategy.CanHandle_RoutesByOs_ClaimsLinuxOrUnknownOnly` fails on the `[InlineData("macOS", false)]` row because the old predicate would claim macOS — proves the explicit-claim semantic is test-pinned and a future open-closed violation can't sneak in. Out of scope for Phase-12.E.5: ChocolateyUpgradeMethod + MsiUpgradeMethod (deferred to a future phase — they were Phase-12.E.5's "Out of scope" candidate but the user prioritised genericity hardening over forward-feature method chain expansion); E2E test on `windows-latest` runner (deferred); `WindowsUpgradeStatusStorage` Capabilities-RPC integration test (deferred); MacOSTentacleUpgradeStrategy actual implementation (architectural foundation laid by `IsMacOS` + Linux explicit-claim refactor, but no current operator demand — the value of 12.E.5 is that this LATER addition is now a new-class-only change with no Linux-strategy modification).

Phase-12.E.5 cumulative: closes 0/85 audit findings — **architectural genericity hardening**, not feature work. Cross-project totals after Phase-12.E.5: **4929 / 1396 / 139 = 6464 tests, 0 failures** (Phase-12.E.4 baseline 6415 + 49 new tests this bundle, split 44 server / 5 agent). 0 lines deleted from production behaviour for the existing Linux + Windows + K8s upgrade paths — the predicate-shape refactor preserves the exact behaviour for every (style, OS) tuple the previous code claimed (cold-cache → Linux, explicit-Linux → Linux, explicit-Windows → Windows, K8s → K8s). The intentional behaviour CHANGE is: explicit `"macOS"` / `"FreeBSD"` / any-non-Linux-non-Windows-non-K8s tuple no longer routes to Linux as a side effect of the `!IsWindows` predicate; instead resolver returns "no strategy registered" with the actual style + OS in the error message. No production tentacles report these strings today (no install path for macOS/FreeBSD agents, build pipeline doesn't ship those artifacts), so zero operators are affected — but the architectural correctness means a future macOS strategy lands without modifying Linux.

_Phase-12.E.6 Windows upgrade wrapper E2E — dedicated Squid.WindowsUpgradeE2ETests project + 5 critical-path tests verifying real schtasks/Task Scheduler detach mechanism on `windows-latest` runner (2026-05-04)_: 1 commit, 5 new E2E tests (in a new dedicated test project — not counted against the per-project totals since they're end-to-end, not unit). **Robustness-axis hardening — closes 0/85 audit findings but adds the LAST remaining safety net for the in-UI Windows tentacle upgrade pipeline: real-execution verification of the Phase 12.E.4 outer-wrapper detach mechanism on a real Windows host.** Pre-Phase-12.E.6, every Phase 12.E.4 unit test verified wrapper SHAPE (rendered script contains schtasks, /RU SYSTEM, /Z, GUID task name, base64-encoded inner, etc.) but ZERO tests verified that the rendered script actually WORKS when executed by `powershell.exe` on Windows. The first real customer clicking Upgrade on a Windows agent was the production canary for: (a) does schtasks `/RU SYSTEM` actually take effect under the squid-tentacle service's LocalSystem context; (b) does `/Z` actually auto-delete the one-shot task after run; (c) does the Task Scheduler launch the inner script in a process tree separate from the wrapper's parent (the load-bearing Phase B survival invariant — without this, Stop-Service squid-tentacle in Phase B would kill the running inner mid-swap); (d) do GUID-suffixed task names actually prevent collision when two operators dispatch concurrent upgrades. Phase 12.E.6 closes that confidence gap.
  - **12.E.6 (commit TBD) — `Squid.WindowsUpgradeE2ETests` dedicated test project + 5 critical-path E2E tests + CI workflow integration**: 1 new csproj + 3 new test/support files + 1 workflow YAML modified + 1 production csproj modified (added `Squid.WindowsUpgradeE2ETests` to `Squid.Core.csproj`'s `InternalsVisibleTo` list so the E2E test can reach the internal `WindowsTentacleUpgradeStrategy.BuildOuterWrapper(string)` seam). **Why a dedicated project (not Tentacle.Tests / UnitTests)**: Tentacle.Tests doesn't reference Squid.Core (where the strategy lives); UnitTests doesn't run on `windows-latest` by default. New project plugs into the existing `tentacle-windows-e2e.yml` workflow as a SEPARATE filter step — a regression in the wrapper detach mechanism surfaces with a distinct CI signal from the agent-binary E2E. Architectural separation also lets a future Linux upgrade E2E (phase 12.F+ candidate) plug in alongside without inheriting Windows-only fixture concerns. **5 critical-path tests** all carrying `[Trait("Category", WindowsUpgradeE2ECategories.Wrapper)]` and gracefully no-op-skipping on non-Windows dev hosts (`if (!OperatingSystem.IsWindows()) return;` mirrors the established `WindowsPowerShellE2ETests` pattern in Squid.Tentacle.Tests): (1) `Wrapper_WhenRunOnRealWindows_ExitsZeroAndWritesDispatchPs1` — wrapper exits 0 + dispatch.ps1 lands at `%ProgramData%\Squid\Tentacle\upgrade\dispatch.ps1` (Phase 12.A.2 contract dir) + stdout reports task name; (2) `Wrapper_DetachedTask_RunsAsSystemAfterWrapperExits` — the Phase B survival invariant: substitutes a tiny test inner that records `whoami`, polls for marker file (60s timeout), asserts identity is `nt authority\system` (proves `/RU SYSTEM` took effect — without this, /RU SYSTEM rejection would silently route to a less-privileged identity and Phase B's Stop-Service would fail); (3) `Wrapper_ScheduledTask_AutoDeletesAfterRun_ZFlag` — polls `schtasks /Query /TN <name>` until the task is gone post-run, proves `/Z` works (without this, the Task Scheduler library would accumulate orphan upgrade tasks across every dispatch); (4) `Wrapper_ConcurrentDispatches_GuidSuffixedTaskNames_DoNotCollide` — runs two wrappers in parallel via `Task.Run`, asserts both get distinct GUID-suffixed task names AND both inner markers appear (without GUID suffixing, /F /Create would overwrite the earlier task before its run trigger); (5) `Wrapper_InnerExitsNonZero_WrapperStillExitsZero` — wrapper's exit code reflects schtasks /Run success, NOT the detached inner's later exit (Halibut MUST see "wrapper succeeded" so the strategy maps to Initiated; actual upgrade outcome arrives via last-upgrade.json on next health check — without this, a Phase B failure would falsely surface as Halibut Failed). **Test infrastructure helpers** (private to the test class, deliberately simple — no fixture hierarchy): `BuildWrapperFor(innerPowerShell)` wraps the production `BuildOuterWrapper` with a base64-encoded test inner; `RunWrapper(script, timeout)` launches `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File <temp>` with concurrent stdout/stderr drain (avoids pipe-buffer deadlock on long log output); `ExtractTaskName(stdout)` regex-parses the wrapper's `[upgrade-wrapper] Task '<name>' registered` log line to find the agent-runtime-generated GUID for verification + cleanup; `WaitForFileExists` / `WaitForTaskGone` poll with reasonable deadlines; `TryDeleteTask` / `TrySafeDelete` are best-effort cleanup primitives invoked from the test's `Dispose`. **Cleanup discipline**: every test maintains lists of `_scheduledTaskNamesToCleanup`, `_markerFilesToCleanup`, `_tempScriptsToCleanup` populated as side effects of test setup steps; `Dispose` iterates and deletes idempotently so a failing test never leaves Task Scheduler library in a dirty state. **CI workflow integration**: `.github/workflows/tentacle-windows-e2e.yml` gains a new step `Run Windows upgrade wrapper E2E` after the existing Tentacle agent-binary E2E step. The two existing `workflow_dispatch` test_filter inputs were renamed/split into `tentacle_test_filter` + `upgrade_test_filter` so a developer can target one suite at a time via on-demand dispatch. **Project layout decision** (Rule 10 + SOLID): tests live in `tests/Squid.WindowsUpgradeE2ETests/` rather than being squashed into Tentacle.Tests or UnitTests. New csproj declares xUnit + Shouldly + the test SDK; references Squid.Core + Squid.Tentacle + Squid.Message. **Local non-Windows execution behaviour**: `dotnet test tests/Squid.WindowsUpgradeE2ETests` on macOS/Linux dev box reports "5 ran, 5 passed" because every test no-ops via `if (!OperatingSystem.IsWindows()) return;` — same pattern as the agent-side WindowsPowerShellE2ETests. Real execution (with assertions actually firing) only happens on the `windows-latest` GHA runner. Out of scope for Phase 12.E.6: full real-Tentacle-service upgrade E2E (would need a real Tentacle binary + sc create + actual binary swap — Phase 12.E.7+ candidate, builds on top of E.6's primitives); WindowsUpgradeStatusStorage Capabilities-RPC integration test; ChocolateyUpgradeMethod + MsiUpgradeMethod feature parity work. **Architectural property pinned by design**: each E2E test verifies ONE atomic invariant (clean exits, identity, auto-delete, concurrency, exit-code-isolation); the test infrastructure helpers stay generic enough that a future schtasks-based detach use case (e.g. deferred config reload) can copy the pattern. **Reverse-verifies that WILL be confirmed when the next CI run completes** (cannot be confirmed locally on macOS because the tests no-op): (1) removing `/RU SYSTEM` from BuildOuterWrapper → `Wrapper_DetachedTask_RunsAsSystemAfterWrapperExits` fails with "ran as <runner-user> but expected nt authority\\system"; (2) removing `/Z` flag → `Wrapper_ScheduledTask_AutoDeletesAfterRun_ZFlag` fails with task still present after timeout; (3) removing GUID-suffixing → `Wrapper_ConcurrentDispatches_GuidSuffixedTaskNames_DoNotCollide` fails with one of the markers missing.

Phase-12.E.6 cumulative: closes 0/85 audit findings — **end-to-end execution verification (robustness axis)**, not feature work or genericity hardening. Cross-project totals after Phase-12.E.6 on the standard CI suite: **4929 / 1396 / 139 = 6464 tests, 0 failures** (unchanged — the new 5 E2E tests in Squid.WindowsUpgradeE2ETests live in a separate project filtered by the windows-latest workflow, not the standard Linux CI). Real coverage gain: 5 E2E tests on `windows-latest` exercise the Phase 12.E.4 detach mechanism end-to-end, closing the "Windows tentacle upgrade is functional but architecturally unverified in real execution" gap. Note one observed flake during cross-suite run on this commit: 1× `ScriptCancellationRegistryTests.HighConcurrency_GetOrCreateAndCancel_NoTornUpdates` (passes in isolated rerun as 9/9) — pre-existing parallel-runner concurrency stress test, NOT a Phase-12.E.6 regression (Phase 12.E.6 only touches the upgrade subsystem and adds a new test project; no Tentacle ScriptExecution code change).

Net: 56/85 findings closed (66%). Cross-project final totals after Phase-12.E.6: **4929 / 1396 / 139 = 6464 tests, 0 failures** on the standard suite + **5 windows-latest-only E2E tests** in the dedicated `Squid.WindowsUpgradeE2ETests` project. Phase 12 sub-phases A/B/C/D/E.0/E.1/E.2/E.3/E.4/E.5/E.6 are forward-feature Windows-Tentacle foundation + parity + distribution + interface-scaffold + first-concrete-method + OS-aware-routing + real-Halibut-dispatch + OS-routing-genericity-hardening + wrapper-detach-E2E (close 0/85 audit findings each; the in-UI Windows tentacle upgrade pipeline is end-to-end functional and end-to-end E2E-verified for the wrapper layer). Phase 12.E.7 candidates (in priority order per architectural review): full real-Tentacle-service upgrade E2E exercising real binary download + Stop-Service + Move-Item + Start-Service round-trip (builds on E.6's primitives); WindowsUpgradeStatusStorage Capabilities-RPC integration test; ChocolateyUpgradeMethod + MsiUpgradeMethod prepended to Windows method chain.
