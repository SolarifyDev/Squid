# Squid Hardening — Findings Tracking

**Generated**: 2026-04-24 from cross-layer audit (5 parallel sub-agents against Squid + OctopusDeploy/OctopusTentacle source).
**Combined with**: 2026-04-23 Tentacle-runtime deep-scan (24 findings, separate table).
**Total live findings**: ~85 (≈20 P0, ≈30 P1, ≈35 P2).

## Legend

| Symbol | Meaning |
|---|---|
| 🔴 **P0** | Security / data corruption / process hang / service outage — fix immediately |
| 🟠 **P1** | Reliability / silent-failure / resource leak — fix before next release |
| 🟡 **P2** | Quality / UX / observability — fix when opportunity |
| 🏔️ **ARCH** | Architectural alignment with Octopus — multi-week effort, plan separately |

## Status column

| Status | Meaning |
|---|---|
| ⬜ | pending — not started |
| 🔄 | in progress — actively being worked on |
| ✅ | fixed — followed by commit SHA |
| 🚫 | won't fix — followed by rationale |
| 🔀 | deferred to architectural-alignment phase |

---

## Section 1 — Crypto & Secrets (🔴 P0-critical)

Everything that touches key material, sensitive variables, or encryption. **These are all pre-release-blocking.**

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **B.1** | 🔴 P0 | `Services/Security/VariableEncryptionService.cs:128-140` + `Api/appsettings.json:26-28` | `MasterKey` defaults to empty string. `Convert.FromBase64String("")` returns `new byte[0]` with no exception. KDF then derives 32-byte key from (empty master, 4-byte-int salt) — fully deterministic for fresh installs. DB dump → offline recovery of every sensitive variable. | ✅ (phase3) | `ValidateMasterKey(rawBase64, EnforcementMode)` rejects empty / short / all-zero **only in Strict mode**. Default mode is **Warn** — preserves backward compat for deploys without a configured MasterKey, surfaces structured warning in Seq pointing at `SQUID_MASTER_KEY_ENFORCEMENT=strict` for production hardening. Invalid base64 always throws (no key bytes to fall back to). Env var: `SQUID_MASTER_KEY_ENFORCEMENT={off\|warn\|strict}`. 30 new tests covering full (input × mode) matrix + constant-name pin. NOT A BREAKING CHANGE — Phase-1 broke deploys with empty MasterKey; Phase-3 restored backward compat. |
| **B.2** | 🔴 P0 | `Services/DeploymentExecution/Script/ScriptExecutionHelper.cs:42-46` + agent side `LocalScriptService.cs:607-684` | Per-script `sensitiveVariables.json` encryption password is a `Guid.NewGuid().ToString("N")` **written to `*.key` file next to ciphertext** AND passed via `psi.ArgumentList` as `--password=xxx` (visible in `ps aux` / `/proc/*/cmdline`). Encryption at rest = security theater. | ✅ (phase2) | Tentacle: dropped the `.key` sidecar write entirely — password stays in-memory on `ScriptFile.EncryptionPassword`. Password rides to Calamari child process via env var `SQUID_CALAMARI_SENSITIVE_PASSWORD` (readable via `/proc/<pid>/environ` at 0600, not via `ps aux` / `/proc/<pid>/cmdline` at 0444). Extracted `LocalScriptService.BuildCalamariProcessStartInfo` as testable pure function. Calamari: `RunScriptCliCommandHandler` reads env var with higher precedence than `--password=` argv (argv kept for tests / manual invocation). Env var name pinned as `public const` on both sides. 13 new tests across tentacle + calamari suites covering: constant-name pin on both sides, no `.key` file on disk, argv has no `--password=`, env var set on Calamari PSI, no env var when there's nothing to decrypt, missing ciphertext suppresses both env var and `--sensitive` arg, user-supplied arguments still forwarded, env-var-wins precedence, null-env falls back to argv, whitespace-env falls back to argv. Reverse-verify confirmed on all three pieces (restore `.key` write → tentacle test fails; restore argv password → tentacle test fails; flip precedence → calamari test fails). |
| **B.3** | 🔴 P0 | `Services/Common/SquidVariableEncryption.cs:12-14, 59-62` | PBKDF2 1000 iters + **hardcoded salt `"SquidDep"`** + SHA-1 + AES-128 CBC + no MAC. Vulnerable to bit-flip malleability; would fail any modern crypto review. | ✅ (phase4) | V2 envelope = `V2__` + salt(16) + nonce(12) + AES-256-GCM ciphertext + tag(16). KDF: PBKDF2-SHA256 @ 600_000 iters (OWASP 2023). V1 legacy still readable on both sides (dual-path) for rolling upgrade compat. Three-mode gate on both sides: server `SQUID_SENSITIVE_VAR_ENCRYPTION_ENFORCEMENT={off\|warn\|strict}` selects V1 emit (default) vs V2 emit (strict); calamari `SQUID_SENSITIVE_VAR_DECRYPT_LEGACY_ACCEPT` selects legacy V1 accept (default) vs strict V2-only. Malformed input always throws (no decoded bytes to use). AES-GCM tag detects tampering. 27 new tests: server 13 (format pin, layout, round-trip, tag-flip detection, wrong-password, iter-count pin, mode matrix, backward compat) + calamari 14 (mode × V1/V2 + unknown-prefix reject + env-var default pin). Non-breaking. |
| **B.4** | 🟠 P1 | `Services/Security/VariableEncryptionService.cs:41-61` | `DecryptAsync` has no `await` (compiler warning). No dual-key window — key rotation breaks all existing sensitive vars. Errors include internal `variableSetId`. | ⬜ | Remove async, add key-id to envelope (`SQUID_ENCRYPTED_v2:{keyId}:{nonce}:{cipher}`), support dual-key rollover. |
| **B.5** | 🟠 P1 | `Services/Security/VariableEncryptionService.cs:32,54` | `Log.Information("Successfully encrypted variable for VariableSet {id}")` on every call → timing-amplification leak via Seq. | ✅ (phase7) | Both encrypt + decrypt success log lines dropped from `Log.Information` to `Log.Debug`. Default Seq pipelines no longer ingest the per-VariableSet metadata; operators who want the trail raise the level explicitly. Failure paths (`InvalidOperationException` with detail) stay loud as before. 3 new tests pin Encrypt/Decrypt log levels + sensitive-value-never-in-payload (belt-and-braces). |
| **B.6** | 🟠 P1 | `Services/DeploymentExecution/Runtime/Bundles/BashRuntimeBundle.cs:109-117` | `EscapeBashValue` does NOT escape newlines. Variable value containing `\n` breaks out of `export` line → **shell injection** via user-settable non-sensitive variable. | ✅ (phase5) | `EscapeBashValue` switched to single-quote wrapping (`'value'`) — caller appends directly with no surrounding quotes. Inside single quotes EVERY character is literal (newline, $, `, !, \, ", #) so the entire class of escape-bypass bugs is eliminated. Only `'` itself needs the four-character POSIX idiom `'\''`. 24 new tests + 13 existing tests updated to the new single-quote contract. Reverse-verify confirmed. |
| **B.7** | 🟠 P1 | `Services/DeploymentExecution/Script/ServiceMessages/ServiceMessageParser.cs:82-90` | Agent stdout declares `##squid[setVariable ... sensitive='False']` — server trusts the flag blindly. Compromised/buggy script can mark a secret as non-sensitive → leaks to subsequent step logs. | ✅ (phase5) | New `SensitiveValueLeakGuard` cross-references each candidate output value against known-sensitive deployment variable values (HashSet<string>, ordinal). Three-mode: Off (legacy, silent passthrough), Warn (default — passthrough + structured Serilog warning naming the variable + env var, sensitive value OMITTED from log), Strict (force IsSensitive=true). Length floor 4 (aligned with `SensitiveValueMasker.MinValueLength`). Wired into `ExecuteStepsPhase.CaptureOutputVariables`; mode read once per capture pass. Env var: `SQUID_OUTPUT_VAR_SENSITIVE_LEAK_ENFORCEMENT={off\|warn\|strict}` (pinned in test). 17 new tests covering 3-mode × leak/no-leak matrix + length floor + value-redaction-in-log + boundary. Reverse-verify confirmed. |
| **B.8** | 🟡 P2 | `Observability/SensitiveValueMasker.cs:17` | `MinPatternLength = 4` → 3-char secrets (PINs) silently unmasked. Also `****` could collide with mask token. | ⬜ | Lower floor to 1 with explicit allowlist of "known short strings that aren't secrets"; or hash-and-compare masker. |
| **B.9** | 🟡 P2 | `Services/DeploymentExecution/Script/ServiceMessageParser.cs` + output variables | Qualified `Squid.Action.{Step}.{key}` output variables allow user script to overwrite system-looking names. Unqualified is guarded, qualified is not. | ⬜ | Extend `IsReservedName` to also match qualified forms. |
| **B.10** | 🟡 P2 | `Services/Security/VariableEncryptionService.cs:142-149` | `DeriveKey` salt = 32-bit variableSetId padded with zeros; 10k PBKDF2 iters (below OWASP 600k for SHA-256). | ✅ (phase8) | New V2 envelope: prefix `SQUID_ENCRYPTED_V2:` + base64(salt(16) ‖ nonce(12) ‖ tag(16) ‖ ciphertext). Random per-payload salt; PBKDF2-SHA256 @ 600k iters (OWASP 2023). Server post-Phase-8 always emits V2; decrypt is dual-path (V2 prefix → 600k path; legacy V1 prefix → existing 10k path). Existing pre-upgrade ciphertexts remain readable; entries upgrade naturally as variables are rewritten — no DB migration required. 12 new tests covering layout pin, roundtrip Theory, deterministic-salt regression (same plaintext → different ciphertext), V1 back-compat decrypt via hand-constructed legacy envelope, prefix recognition. Mirrors B.3 dual-format strategy used for sensitive-variable transport. |
| **B.11** | 🟡 P2 | `Core/VariableSubstitution/Templates/EvaluationContext.cs:176-180` + `TemplateEvaluator.cs:138` | Recursion detection uses exception-message substring `"self referencing loop"` — fragile. Missing variable `#{DoesNotExist}` renders verbatim in output. | ⬜ | Proper typed exception `VariableRecursionException` + fail-closed on missing `#{...}`. |

---

## Section 2 — Authorization & Multi-space (🔴 P0-critical)

Security boundary around tenant/space isolation + API surface.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **D.1** | 🔴 P0 | `Services/Caching/Redis/RedisCacheService.cs:20, 31` | `JsonConvert.DeserializeObject<T>(..., TypeNameHandling.Auto)` → **RCE via Newtonsoft gadget chains** if Redis is compromised / shared / misconfigured. Fires on every authenticated request (api-key cache). | ✅ (phase1) | `TypeNameHandling.None` + `RedisCacheService.JsonSettings` extracted for pinning; 2 tests (pin + behavioural Dog-ctor-counter). |
| **D.2** | 🔴 P0 | 10 commands: `AssignRoleToTeamCommand`, `UpdateRoleScopeCommand`, `AddTeamMemberCommand`, `RemoveTeamMemberCommand`, `UpdateTeamCommand`, `DeleteTeamCommand`, `CreateUserRoleCommand`, `UpdateUserRoleCommand`, `DeleteUserRoleCommand`, `RemoveRoleFromTeamCommand` | These commands do NOT implement `ISpaceScoped` → `FilterBySpace` returns all system-level roles (including `AdministerSystem`). Any `TeamEdit` holder in any space can assign `AdministerSystem` to their own team → **privilege escalation to full admin**. | ✅ (phase3) | Added `ISpaceScoped` + writable `int? SpaceId` to all 10 commands (Phase 1). `UserRoleService.EnsureCallerCanAssignRoleAsync` integrated under three-mode framework but **defaults to Strict** because this is a true privesc gate, not a deployment-config compatibility knob. Operators who genuinely need legacy permissive behaviour during a migration window opt OUT explicitly via `SQUID_ROLE_ASSIGNMENT_ENFORCEMENT={warn\|off}`. Phase-3 added 3 mode tests (Warn allows + logs, Off skips check entirely, default-mode-is-Strict pin) + env var name pin. Existing 5 unit + 3 integration tests still pass under unchanged Strict-default behaviour. |
| **D.3** | 🔴 P0 | `Services/Machines/Upgrade/UpgradeMachineCommand.cs:65` + `Middlewares/Authorization/AuthorizationSpecification.cs:43-53` + framework-wide | H-19 known, acknowledged in code comments, NOT fixed. `EnsurePermissionAsync` runs BEFORE resource lookup using header-supplied SpaceId. Any `MachineEdit` holder can target a machine in a different Space by setting `X-Space-Id` to a space they have perms in. Applies to **all** `{resource}Id + ISpaceScoped` commands. | ⬜ | Framework-level: load resource first → derive SpaceId from resource → check permission against resource's SpaceId, not header's. Requires auditing every permission-gated command. |
| **D.4** | 🔴 P0 | `Middlewares/SpaceScope/SpaceIdInjectionSpecification.cs:25` + all `Register*/Generate*` commands | Reflection filter is `prop.PropertyType != typeof(int?)` — skips non-nullable `int SpaceId` properties. Body-supplied SpaceId never overridden by header injection → operator can POST `{"SpaceId": <otherSpaceId>}` and write to that space if they have perms there. | ✅ (phase4) | Reflection filter expanded to handle both `int?` (body-null == unset) and `int` (body-zero == unset). Factored into named helpers (`TryInjectNullableInt`, `TryInjectNonNullableInt`, `TryReadHeaderSpaceId`). `int` body-zero + header-present now injects; `int` body-non-zero preserved (matches `int?` body-non-null behaviour — caller-intent wins). 4 new tests: NonNullable_Injects_When_BodyZero, NonNullable_SkipsInjection_When_BodyNonZero, NonNullable_SkipsInjection_When_HeaderMissing, NonNullable_SkipsInjection_When_HeaderInvalid. |
| **D.5** | 🟠 P1 | `Api/Authentication/ApiKey/ApiKeyAuthenticationHandler.cs:38-42` + `Services/Account/AccountService.cs` | API-key cache is invalidated only on `UpdateUserStatusAsync` and `DeleteApiKeyAsync`. Direct SQL user-disable / any other mutation path → stale cache entry valid for up to 1h. Also no `UserAccount.IsDisabled` check at cache-hit time. | ✅ (phase7) | `ApiKeyAuthenticationHandler.HandleAuthenticateAsync` now re-checks `matchedUser.IsDisabled` after the cache hit. If disabled: returns `NoResult` AND calls `_cacheManager.RemoveAsync(cacheKey, ...)` so the next request re-fetches fresh state. Closes the up-to-1h fail-open security staleness window for any out-of-band user-disable path. 4 new tests (active-user OK, disabled-cached returns NoResult, disabled-cached invalidates entry, no-API-key path unchanged). |
| **D.6** | 🟠 P1 | `Services/Identity/ICurrentUser.cs:27,42` + `Services/Identity/CurrentUsers.cs:7` | `InternalUser.Id = 8888` is mutable `static int` — not `const`. If `HttpContext == null` for `ApiUser` (background job scope, DI mishap, test-mode leak), `_currentUser.Id == 8888` → authorization middleware returns immediately, bypassing ALL checks. | ✅ (phase7) | Three pre-fix gaps closed together: (1) `CurrentUsers.InternalUser.Id` flipped from mutable `static int` to `public const int = 8888` (compile-pinned). (2) `ICurrentUser` gains `bool IsInternal` — `InternalUser` returns true, `ApiUser` returns false. `ApiUser.Id` returns null (was: InternalUser.Id) when HttpContext is null. (3) `AuthorizationSpecification` keys bypass off `IsInternal` (NOT off `Id == 8888`); throws `PermissionDeniedException` on null `Id` for permissioned commands. 6 new ICurrentUserTests + 2 new AuthorizationSpec tests; existing AuthorizationSpec tests updated. Closes the "DI mishap → ApiUser sees null HttpContext → falls back to 8888 → middleware silently bypasses" path. |
| **D.7** | 🟠 P1 | `Api/Extensions/CorsPolicyExtension.cs:10-19` | `AllowAnyHeader + AllowAnyMethod + AllowCredentials + WithOrigins(config-list)`. If an allow-listed origin is compromised (CNAME takeover, subdomain) → CSRF on cookie flow. | ⬜ | Enforce `SameSite=Strict` on auth cookies + add anti-forgery token to state-changing endpoints. Audit allow-list origins. |
| **D.8** | 🟠 P1 | `Services/Machines/MachineDataProvider.cs:60-74` (`GetMachinePagingAsync`) | If `spaceId == null`, space filter is skipped → cross-space machine listing (enumeration of names, endpoints, thumbprints). | ✅ (phase8) | API split into explicit `GetMachinesInSpacePagingAsync(int spaceId, ...)` (non-null at type level) and `GetMachinesAllSpacesPagingAsync(...)` (explicitly cross-space). Legacy nullable entry `[Obsolete]`-marked and routes to the appropriate method. Both call sites migrated: MachineService.GetMachinesAsync uses per-space when SpaceId.HasValue; MachineHealthCheckService.AutoHealthCheckForAllAsync uses all-spaces (legitimate Hangfire-driven cross-space sweep). Cross-space scan now requires every call site to be intentional — no more "forget the X-Space-Id header → enumerate every space". 4 new tests + 8 existing mocks updated. |
| **D.9** | 🟡 P2 | `Services/Authorization/AuthorizationService.cs:109` (`FilterBySpace`) | `r.SpaceId == null || r.SpaceId == spaceId` — system-level roles always included regardless of space. Amplifies D.2. | ⬜ | Keep as-is for `AdministerSystem` but split other system-level roles from space-scoped ones explicitly. |
| **D.10** | 🟡 P2 | `Services/Caching/ICacheManager.cs:55-81` `GetOrAddAsync` | No locking → classic thundering-herd on cache miss boundary. | ⬜ | `SemaphoreSlim` keyed by cache-key. |
| **D.11** | 🟡 P2 | `Services/Account/AccountService.cs:215` | API keys stored **plaintext** in DB. DB dump → keys reusable forever (no rotation signal). | ⬜ | Hash at rest (bcrypt/argon2), prefix token with key-id for O(1) lookup, store only hash + prefix. Migration path: dual-path read during rollout. |
| **D.12** | 🟡 P2 | Controllers | `GetUpgradeLogRequest` returns agent bash log which can contain tokens / paths / secrets. Ensure sensitive-value masker runs at response serialization, not just at ingest. | ⬜ | Apply masker at controller-response layer for any log-returning endpoint. |

---

## Section 3 — Kubernetes Agent (🔴 P0 + 🟠 P1)

K8s-specific script execution. Three critical security issues.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **C.1** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:64` + `Settings/Kubernetes/KubernetesSettings.cs:8` | Default `ScriptPodImage = "bitnami/kubectl:latest"` + `ImagePullPolicy=IfNotPresent`. Floating `:latest` tag. Registry/tag repoint → arbitrary code execution in the cluster. | ✅ (phase3) | `ScriptPodImageValidator.EnsureSafe(image, EnforcementMode)` — regex `@sha256:[a-fA-F0-9]{64}$` required in Strict mode; Warn (default) accepts tag-only with structured warning; Off silent. Malformed `@sha256:` ALWAYS throws (operator clearly intended to pin and got format wrong). Default `ScriptPodImage` reverted to `bitnami/kubectl:latest` for backward compat. Env var: `SQUID_SCRIPT_POD_IMAGE_ENFORCEMENT={off\|warn\|strict}`. Wired into `BuildPodSpec` AND `ApplyTemplateOverrides` (defence-in-depth). 18 validator (mode × input) tests + 3 wiring tests + helm `values.yaml` _comment block. NOT A BREAKING CHANGE — Phase-1 broke deploys with default image; Phase-3 restored backward compat. |
| **C.2** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/HelmUpgradeScriptBuilder.cs:51-56, 110` | Helm repo username/password and `--set` sensitive values are substituted directly into the rendered shell script (via `EscapeBash`). Ends up in `pod.args`, pod logs, script.sh on NFS, kubectl audit. | ⬜ | Write credentials to a per-job K8s Secret; mount as file or project via envFrom; reference via `HELM_*` env or `--values` file path. |
| **C.3** | 🔴 P0 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentHealthCheckStrategy.cs:18-39` | Health check only probes Halibut capabilities — does NOT validate agent can actually create pods (RBAC, PVC mount, SA token). Broken agent reports Healthy until first real deployment. | ⬜ | Add a "describe ScriptPodTemplate" dry-run round-trip at end of health check. |
| **C.4** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:384-395` (`BuildPodSecurityContext`) | Default `ScriptPodRunAsNonRoot=false` + no UID → pod runs as **root**. `bitnami/kubectl` is root-by-default. `fsGroup` never set; NFS workspace owned as root. | ⬜ | Default to `runAsNonRoot:true, runAsUser:1000, fsGroup:1000, seccompProfile:RuntimeDefault, readOnlyRootFilesystem:true`. |
| **C.5** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodMonitor.cs:109-199` | Orphan cleanup depends on agent leader election being up. If both replicas lose leadership → no reaper. Also pod has no `OwnerReference` to Job, so Kubernetes GC can't help. | ⬜ | Use K8s Job with `ttlSecondsAfterFinished` — cluster GC reclaims even if agent is down. |
| **C.6** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:66-69` + `ScriptPodService.cs:131-171` | Workspace NFS PVC is `ReadWriteMany`; script.sh + variables.json written plaintext. Another pod on same PVC can read cross-ticket via workspace mount. | ⬜ | Use per-pod K8s Secret + ConfigMap envFrom projection (RBAC-scoped, not cross-readable on disk). |
| **C.7** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentIntentRenderer.cs:195-205` | `kubectl config set-context --current --namespace="{ns}"` persists globally in pod's `$KUBECONFIG` — user-script background process inherits mutated namespace. | ⬜ | Pass `-n {ns}` explicitly on every kubectl invocation; don't mutate the shared kubeconfig. |
| **C.8** | 🟠 P1 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.PodSpec.cs:55` + `KubernetesSettings.cs:9` | `ScriptPodServiceAccount="squid-script-sa"` with no accompanying RBAC shipped. Operator must wire manually; default SA (if missing) is cluster-default (permissive). | ⬜ | Ship chart-managed SA + Role with minimum verbs. Agent startup probes SA existence; fail-fast if missing. |
| **C.9** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.cs:33-36` (`_createLocks`) | `SemaphoreSlim` per ticketId never removed → unbounded dict growth on long-running agent. | ⬜ | Remove entry after release, or use size-bounded LRU. |
| **C.10** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesPodManager.cs:202-211` (`CleanupTerminatedPod`) | `ContainerStatuses.FirstOrDefault()` — if `nfs-watchdog` is first, not `script`, `FinishedAt` is wrong. | ⬜ | Filter by container name `"script"` consistently. |
| **C.11** | 🟡 P2 | `Services/DeploymentExecution/Kubernetes/KubernetesAgentEndpointVariableContributor.cs:13-25` | Missing standard K8s context vars (`ClusterUrl`, `Account.*`) that shared helm/apply builders reference. | ⬜ | Mirror full contributor coverage; handler must not depend on vars that only KubernetesApi contributes. |

---

## Section 4 — Deployment Pipeline (🔴 P0 + 🟠 P1)

Server-side orchestrator. Concurrency and cancellation correctness.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **A.1** | 🔴 P0 | `Services/DeploymentExecution/Completion/DeploymentCompletionHandler.cs:40-45` + `DeploymentPipelineRunner.cs:65-67` | Cancel-vs-Fail race. If user cancel fires AFTER an exception but BEFORE terminal write, task ends `Failed` instead of `Cancelled`. Checkpoint may emit `DeploymentFailedEvent` while UI shows cancel. | ✅ (phase2) | `DeploymentPipelineRunner.ProcessAsync` now inspects `registryCts.Token.IsCancellationRequested || ct.IsCancellationRequested` BEFORE checking `ctx.FailureEncountered`. If either cancellation source is already signalled, emit `DeploymentCancelledEvent` + `OnCancelledAsync` regardless of the failure flag. Timeout path still flows through the existing OCE handler because `timeoutCts` raises OCE mid-execution (never reaches the terminal-write block quietly). 3 new tests: registry-cancel-after-failure → OnCancelled not OnFailure, external-ct-cancel-after-failure → OnCancelled not OnFailure, plain-failure-no-cancel still OnFailure (guard-on-guard). Reverse-verify confirmed. |
| **A.2** | 🔴 P0 | `Services/DeploymentExecution/Parallel/TargetParallelExecutor.cs:44-52` | `Task.WhenAll` throws only FIRST exception. Subsequent failures observed+discarded silently. | ✅ (phase2) | New private `AwaitAllCollectingFailuresAsync(tasks, ct)` helper walks every faulted task and aggregates `Exception.InnerExceptions` into a single `AggregateException` with "N of M parallel tasks failed" message. Both `ExecuteAllConcurrentAsync` and `ExecuteThrottledAsync` now route through it. Single-failure case uses `ExceptionDispatchInfo.Capture(...).Throw()` to preserve original exception type (downstream catch blocks keep matching). Pure-cancellation case re-throws a clean `OperationCanceledException`. 5 new tests: all-concurrent 3-failure aggregation, throttled 2-failure aggregation, failure-count in aggregate message, cancellation preserved as OCE, happy-path ordered-return regression. Reverse-verify confirmed. |
| **A.3** | 🔴 P0 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.cs:85-86` + `.Execute.cs:72-96` + `BatchCheckpointState` | `_batchStates` is non-concurrent `Dictionary<int, BatchCheckpointState>` written from parallel `Task.WhenAll`. `CompletedMachineIds` / `FailedMachineIds` are `HashSet<int>` — not thread-safe. → checkpoint corruption, lost terminal markers on resume. | ✅ (phase2) | `_batchStates` converted to `ConcurrentDictionary<int, BatchCheckpointState>` with atomic `GetOrAdd` in `GetOrCreateBatchState`. `BatchCheckpointState` gains thread-safe `AddCompleted` / `AddFailed` / `IsTerminalFor` guarded by a private instance lock (HashSet properties stay public for `System.Text.Json` resume deserialisation). `MarkTargetCompleted` now routes through the new thread-safe methods. 6 new tests under heavy parallel contention (500-1000 writers) — parallel adds preserve every entry, mixed completed/failed workloads don't clobber each other, `IsTerminalFor` tolerates concurrent writers, `GetOrCreateBatchState` returns exactly one instance per batchIndex, `MarkTargetCompleted` across threads preserves all machineIds. Reverse-verify confirmed on both the HashSet lock and the ConcurrentDictionary switch. |
| **A.4** | 🟠 P1 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.Execute.cs:72-96` | `catch when (step.IsRequired)` fires `failFastCts.Cancel()` then marks OTHER targets' OCEs as "failed" on checkpoint even though they were aborted, not genuinely failed. Resume skips them as terminal. | ✅ (phase7) | Extracted pure `TargetCatchClassifier` helper that takes (exception, failFastCancelled, parentCtCancelled) and returns `(MarkFailed, TriggerFailFast)`. Peer-abort case (OCE while failFastCts is cancelled but parent CT is NOT) returns `(false, false)` — target stays pending-resume. Genuine exception / user-cancel / unrelated OCE returns `(true, true)` — pre-fix semantics preserved. Catch site rewritten to consult the classifier. 5 new tests cover all four classification cells + reverse-verify. |
| **A.5** | 🟠 P1 | `Services/DeploymentExecution/Phases/ExecuteStepsPhase.cs:156-171` + output-var merge under lock | `_ctx.Variables.AddRange(result.OutputVariables)` — no de-dup on same variable name from different targets; last-writer-wins ordering is `Task.WhenAll` dependent (non-deterministic). | ✅ (phase6) | New `OutputVariableMerger.Merge(existing, incoming, mode)` helper with three-mode framework: Off (silent passthrough — legacy), Warn (default — passthrough + structured Serilog warning naming variable + env var), Strict (first-writer-wins; drop colliding incoming + log). Sensitive values redacted from log payloads (B.7-style — name only). Same-value re-emits silently deduplicate (not flagged as collision). Wired into `ApplyBatchResults` — mode read once per batch. Env var `SQUID_OUTPUT_VAR_COLLISION_ENFORCEMENT={off\|warn\|strict}` (pinned in test). 12 new tests covering 3-mode × no-collision/collision matrix + value-redaction + multi-collision reporting. |
| **A.6** | 🟠 P1 | `Services/DeploymentExecution/Script/ServiceMessages/ServiceMessageParser.cs:23-39` | Output variable capture `result[name] = variable` — later lines override earlier. User script can `echo '##squid[setVariable name="Squid.Deployment.Id" ...]'` and overwrite system vars. | ✅ (phase8) | New `IsReservedOutputVariableName` predicate (case-insensitive `Squid.*` / `System.*` prefix-with-dot). Three-mode framework via `SQUID_OUTPUT_VAR_NAME_ENFORCEMENT={off\|warn\|strict}` — Off silent (legacy), Warn (default) accepts + structured warning naming the offending variable + env var, Strict rejects (TryParseOutputVariable returns null). The qualified-clone bypass (pre-fix `Squid.Action.{step}.Squid.Deployment.Id` was admitted) is now gated at parse time. Complements B.7 (sensitive-flag leak guard) and Phase-6.5 (OutputVariableMerger collision detection) — together they close the full output-variable abuse surface. 9 new tests. |
| **A.7** | 🟡 P2 | `Services/DeploymentExecution/Infrastructure/DeploymentPipelineRunner.cs:128-129` | `ConcurrencyMaxWait` timeout logs `Warning … proceeding anyway`. Defeats the concurrency tag — two executors CAN run on tagged resource. | ⬜ | Throw after timeout; fail-closed. |
| **A.8** | 🟡 P2 | `Services/DeploymentExecution/Infrastructure/HalibutScriptObserver.cs:241-249` | Log buffer truncation removes OLDEST entries — loses CAUSE of failure on long deploys. | ⬜ | Truncate TAIL (keep head for cause analysis). Or persist incrementally. |
| **A.9** | 🟡 P2 | `Services/DeploymentExecution/Phases/ResumeCheckpointPhase.cs:43-58` | `JsonException` → start fresh + log warning. Silent re-deployment of completed machines on corruption. | ⬜ | Fail closed; require explicit "reset checkpoint" operator action. |
| **A.10** | 🟡 P2 | `RetentionCleanupPhase.cs:13-20` + `DeploymentCompletionHandler.cs:97-118` | Three "log warning and continue" swallows. Masks DB/service outages. | ⬜ | Emit structured Seq events + surface to health-check. |
| **A.11** | 🟡 P2 | `TargetParallelExecutor.cs:20` | No global parallelism semaphore → MaxParallelism=50 concurrent batches exhaust server pool. | ✅ (phase6) | Process-wide static `SemaphoreSlim` in `TargetParallelExecutor` configured by env var `SQUID_TARGET_PARALLELISM_GLOBAL_CAP`. Acquired by every parallel task in BOTH `ExecuteAllConcurrentAsync` and `ExecuteThrottledAsync` (after the per-step cap, so per-step ordering preserved). Always-release on the way out — even if worker throws — so faulted task can't permanently consume a slot. Modes: unset/auto → ProcessorCount × 4; integer → that exact cap; "disabled" / "0" → no cap. `HealthCheckActionHandler.cs:81` (uncapped Task.WhenAll) routed through the same executor. AsyncLocal test override (xUnit-parallel-class safe). 11 new tests + Theory matrix on parser. |
| **A.12** | 🟡 P2 | `Services/DeploymentExecution/Phases/PrepareTargetsPhase.cs:69-76` | `catch { return; }` on malformed `EnvironmentIds` JSON → account scope enforcement bypassed. | ⬜ | Hard-fail with detailed error pointing at the malformed account. |

---

## Section 5 — Halibut Communication (🔴 P0 + 🟠 P1)

Server-side Halibut usage. Protocol version parity + cancellation.

| ID | Severity | File:Line | Problem | Status | Fix strategy |
|---|---|---|---|---|---|
| **E.1** | 🔴 P0 | `Services/DeploymentExecution/Infrastructure/HalibutScriptObserver.cs:124` + `IAsyncScriptService.cs` | No per-RPC `CancellationToken` on `GetStatusAsync`/`CompleteScriptAsync`/`CancelScriptAsync`. CT only checked BETWEEN polls. Stuck agent → 5min `TcpReceiveTimeout` before observer can abort. User-cancel doesn't propagate through in-flight RPC. | ⬜ | Adopt Octopus V2 pattern: `IAsyncClientScriptServiceV2` accepts `HalibutProxyRequestOptions { RequestCancellationToken }`. Requires Halibut client update. |
| **E.2** | 🔴 P0 | `Services/Tentacle/IScriptService.cs` + `HalibutMachineExecutionStrategy` | Server speaks only V1. No V2/V3-alpha fallback. Future V2-only tentacle → "NoMatchingService" hard failure. No version negotiation. | 🏔️ | Add `ScriptServiceVersionSelector` that probes capabilities first, picks highest common version. Major contract change. |
| **E.3** | 🟠 P1 | `Services/DeploymentExecution/Infrastructure/HalibutMachineExecutionStrategy.cs:29-71` | Capabilities cache exists but `ExecuteScriptAsync` never reads it — always calls V1 `StartScriptAsync`. | ✅ (phase4) | `IMachineRuntimeCapabilitiesCache.Store` signature extended with `IReadOnlyList<string> supportedServices` (default null for backward compat). `MachineRuntimeCapabilities` gains a `SupportedServices` property and a `SupportsScriptServiceV2` derived flag (case-insensitive match). `TentacleHealthCheckStrategy` now caches `response.SupportedServices` on every health check. `HalibutMachineExecutionStrategy` reads the cache + emits a structured `Log.Debug` naming the dispatch decision (v1-only / v2-capable / cold-cache). Dispatch still picks V1 always — the branch site is wired for E.2's future V2 rollout but V2 server-side isn't implemented yet. 7 new tests on the cache. |
| **E.4** | 🟠 P1 | `Halibut/PollingTrustDistributor.cs:40-69` | `TrustOnly` REPLACES entire trust list on every registration. Under burst registration → O(N) DB + brief polling-connection drops. Also `.GetAwaiter().GetResult()` in `IStartable.Start` → Autofac-scope deadlock risk. | ⬜ | Switch to additive `Trust(thumbprint)` API. Remove sync-over-async in startup path. |
| **E.5** | 🟠 P1 | `HalibutScriptObserver.cs:188-232` (liveness probe) | Liveness probe opens a SECOND Halibut connection every 5s — 30-min deploy = 360 extra capability RPCs + potential TLS handshakes. Per-probe exception allocation if service missing. | ⬜ | Piggyback liveness on main polling channel (V2 streaming would solve this). Or make probe interval dynamic (exponential backoff when healthy). |
| **E.6** | 🟡 P2 | `HalibutMachineExecutionStrategy.cs:44 + 57-70` | Only `HalibutClientException` + `OperationCanceledException` distinguished. `HalibutNetworkException` lumped with generic. | ⬜ | Add `IsTransientNetworkError` helper; align with LinuxTentacleUpgradeStrategy's pre-dispatch vs mid-dispatch split. |
| **E.7** | 🟡 P2 | `HalibutScriptObserver.cs` observer CT source | Observer CT comes from Hangfire job scope — if user clicks "cancel deployment" and `TaskCancellationRegistry.Cancel(taskId)` is not called, observer polls to `scriptTimeout`. | ⬜ | Ensure every UI cancel path calls the registry. Unit test the registry→observer propagation. |
| **E.8** | 🟡 P2 | `HalibutMachineExecutionStrategy.cs:92-104` | Deterministic `ScriptTicket` = SHA256(taskId, step, action, machineId). Retry on same attempt → same ticket → agent may return OLD ScriptStatusResponse. Idempotency-breaking-semantics lost. | ⬜ | Match Octopus: `new ScriptTicket(Guid.NewGuid())` per attempt. |
| **E.9** | 🟡 P2 | `Halibut/PollingTrustDistributor.cs:40-58` | Initial trust load backgrounded via `Task.Run`. During warmup, pre-existing tentacles TLS-rejected → reconnect churn. | ⬜ | Block startup until trust loaded (fail-fast if DB down). |
| **E.10** | 🟡 P2 | `Halibut/HalibutModule.cs:23` | `HalibutTimeoutsAndLimits.RecommendedValues()` hardcoded. No env override. Violates Rule 8 (env-var escape hatch for air-gapped operators). | ✅ (phase8) | New `HalibutModule.BuildTimeoutsAndLimits()` starts from `RecommendedValues()` then applies env-var overrides for the two most operationally-relevant timeouts: `SQUID_HALIBUT_TCP_RECEIVE_RESPONSE_TIMEOUT_SECONDS` (default 10min) and `SQUID_HALIBUT_POLLING_QUEUE_TIMEOUT_SECONDS`. Other fields inherit RecommendedValues — when Halibut adds a new field with a default, we pick it up automatically. Pure parser exposed via `TryParseSecondsEnv` for unit testing without env state. 16 new tests covering 2 constant-name pins (Rule 8), invalid-input matrix (null/empty/whitespace/garbage/zero/negative all fall back), valid-positive-int matrix, and override-application sanity. |

---

## Section 6 — Tentacle Runtime (🔴 P0 + 🟠 P1)

From **2026-04-23 deep-scan** (24 findings) — cross-verified by this audit round. Most are still open.

| ID | Severity | File:Line | Problem | Status |
|---|---|---|---|---|
| **T.1** | 🔴 P0 | `Tentacle/Certificate/ServerCertificateValidator.cs:63-69` | TLS pin fallback: unpinned certs accepted with warning. MITM door open. No `InsecureAllowUnpinned` opt-in. | ✅ (phase3) | `ValidateCore(...)` takes `EnforcementMode`. Chain-valid + thumbprint-match always accept; thumbprint-mismatch always rejects (mode-independent). The unpinned-chain-failure case: Strict rejects, Warn (default) accepts + warning, Off silent. Env var: `SQUID_SERVER_CERT_ENFORCEMENT={off\|warn\|strict}`. 22 tests covering 3-mode × (chain-valid / thumbprint-match / thumbprint-mismatch / no-thumbprint) matrix. NOT A BREAKING CHANGE — Phase-1 broke deploys without configured `ServerCertificate` + non-public CA cert; Phase-3 restored backward compat. |
| **T.2** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:217` | `ticketId` path traversal via `../../`. Absolute-path attack inoculated by `squid-tentacle-` prefix; `..` traversal still works. | ✅ (phase1) | Added `TicketIdWhitelist = ^[a-zA-Z0-9_-]{1,64}$` regex + throw-on-mismatch in `ResolveWorkDir`. 20 tests (6 legit + 13 malicious theory + 1 length-cap). |
| **T.3** | 🔴 P0 | `Tentacle/Core/TentacleHalibutHost.cs:167-177` | Sync-over-async adapter: `IAsyncScriptService.StartScriptAsync => Task.FromResult(_inner.StartScript(command))`. Inside `LocalScriptService.cs:72` there's `_isolationMutex.AcquireAsync().GetAwaiter().GetResult()`. Blocks Halibut worker threads. | 🔀 ARCH |
| **T.4** | 🔴 P0 | `Tentacle/Core/TentacleHalibutHost.cs:93` | `_runtime.Poll(pollUri, serverEndpoint, CancellationToken.None)` — no CTS for graceful drain. | ✅ (phase4) | Host owns a `CancellationTokenSource` threaded into every `_runtime.Poll(...)` call. New `ITentacleHalibutHost.CancelPolling()` method cancels it; `DisposeAsync` cancels before runtime disposal. `TentacleApp.RunAsync` invokes `CancelPolling` BEFORE the drain wait so the script backend gets a clean "no new work" window. `CancellationTokenSource.Cancel` is idempotent so multiple calls are safe. 6 new tests (fresh host, cancel signals token, cancel-before-start-no-throw, double-cancel-no-throw, DisposeAsync implicit cancel, TentacleApp shutdown wiring). |
| **T.5** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:72-95` | mutex handle acquired but not transferred to RunningScript until line 95. Any exception between → mutex stuck forever. | ✅ (phase2) | Wrapped post-acquire setup (dir create, state save, script + additional file writes, process start) in `try { ... } catch { isolationHandle?.Dispose(); throw; }`. Null out after successful `RunningScript` construction so happy-path is a no-op. `LockRelease.Dispose` is idempotent so transient mis-paths are safe. 1 end-to-end regression test: pre-create a directory collision to force `WriteAdditionalFiles` mid-loop throw → assert 2nd FullIsolation script with same mutex name starts within 5s (pre-fix it timed out). Reverse-verify confirmed. Compounds the T.7 fix: rethrow now reliably triggers this path. |
| **T.6** | 🔴 P0 | `Tentacle/Registration/TentacleListeningRegistrar.cs:34-44` | Listening registration: `ServerUrl == "https://localhost:7078"` → fake success return. Fragile literal compare. | ✅ (phase2) | Centralized the sentinel behind `TentacleSettings.DefaultServerUrlSentinel` + `IsAutoRegistrationUnconfigured()` helper. Both call sites (`TentacleListeningRegistrar.RegisterAsync` + `RegisterCommand.ExecuteAsync`) now route through the helper — drift between call sites is impossible. 8 new tests: pin the literal + `TentacleSettings.ServerUrl` default matches sentinel + unconfigured matrix (null/empty/whitespace/tab/sentinel) + positive matrix including `http://localhost:7078` and `https://localhost:9999` (legitimate local-dev URLs that pre-fix were silently dropped). Reverse-verify confirmed. |
| **T.7** | 🔴 P0 | `Tentacle/ScriptExecution/LocalScriptService.cs:610-614` | `WriteAdditionalFiles` catch is empty — doesn't rethrow. Script runs with missing input files. | ✅ (phase2) | Empty `catch { cleanup; }` → `finally { cleanup; }` with implicit rethrow. Temp file still removed in all paths. 3 new tests: target-path-is-directory → throws, first-file-fails → subsequent files NOT written, temp file cleaned up on failure. Reverse-verify confirmed. |
| **T.8** | 🔴 P0 | `Tentacle/Flavors/LinuxTentacle/TentacleListeningRegistrar.cs:61-65` | `ApiKey` sent as plain HTTP header without https-scheme enforcement. | ✅ (phase3) | `EnsureSchemeSafeForSecret(url, hasSecret, EnforcementMode)`. https always passes, http-without-secret always passes, non-http(s) / empty / malformed always throws (mode-independent — those are unconditional config errors). The http+secret cleartext case: Strict throws, Warn (default) accepts + structured warning, Off silent. Env var: `SQUID_REGISTER_HTTPS_ENFORCEMENT={off\|warn\|strict}`. 24 tests covering 3-mode × (scheme × secret) matrix. NOT A BREAKING CHANGE — Phase-1 broke listening tentacles with http+ApiKey; Phase-3 restored backward compat. |
| **T.9** | 🟠 P1 | `Tentacle/ScriptExecution/LocalScriptService.cs` `IsSameProcessAlive` | PID-recycling deeper check: only StartTime cross-checked, not image path (`proc.MainModule.FileName`). 2s tolerance window could admit fast-recycled PID. | ⬜ |
| **T.10** | 🟠 P1 | `Tentacle/Core/TentacleHalibutHost.cs:140` | `_runtime.Listen(port)` defaults to 0.0.0.0. No `ListenIPAddress` config. | ✅ (phase8) | Env var `SQUID_TENTACLE_LISTEN_IP_ADDRESS` selects the bind address. Default / "any" / unset → `IPAddress.Any` (preserves pre-fix behaviour). Valid IPv4/IPv6 literal pins to that interface. Garbage falls back to `IPAddress.Any` + structured Warning — typo never makes the agent invisible. `StartListening` now constructs `IPEndPoint(bindAddress, port)` and calls `HalibutRuntime.Listen(IPEndPoint)`. 17 new tests covering constant pin, default/Any matrix, IPv4 + IPv6 valid matrices, garbage fallback. |
| **T.11** | 🟠 P1 | `Tentacle/ScriptExecution/LocalScriptService.cs:46,497` | Drain race: `_draining` flag checked at entry, but Halibut has already dispatched. RPC gets exception → server marks task failed (avoidable). | ✅ (phase5) | `StartScript` while draining now returns a structured `ScriptStatusResponse(Complete, AgentDraining=-503, stderr=…)` instead of throwing. New `ScriptExitCodes.AgentDraining` (mirrors HTTP 503; included in `IsInfrastructureFailure`) so existing classification treats drain rejections as infra failures. Halibut serialises the response normally; server's existing failure handling treats unknown exit codes as failures so deployment outcome is unchanged — the win is observability (operator sees the AgentDraining code + actionable stderr instead of a wrapped exception trace). 4 new tests: returns Complete/AgentDraining (no throw), stderr explains rejection, propagates ticket, Serilog warning. Reverse-verify confirmed. |
| **T.12** | 🟠 P1 | `Tentacle/Logging/SequencedLogWriter.cs:77-92` | `DetermineStartSequence` fallback 0 on parse failure → log-file corruption → server sees duplicate seq numbers. | ✅ (phase5) | Replaced last-line-only logic with `SequencedLogReader.GetHighestSequence` — same forgiving forward-scan the reader uses. Garbage lines anywhere in the file are tolerated; restart point is exactly one past the highest valid sequence. All-garbage / empty file → 0 (matches no-file case). Removed the now-dead `ReadLastLine` helper. 3 new tests: corrupt-tail-resumes-not-zero, multiple-interleaved-garbage scan, only-garbage stays at zero. Reverse-verify confirmed. Compounds with Phase-4 cursor fix — both close cursor-corruption paths. |
| **T.13** | 🟠 P1 | `Tentacle/Core/TentacleHalibutHost.cs:161-177` | `AsyncScriptServiceAdapter` receives CT on every method, discards it. Halibut cancellation fully broken. | 🔀 ARCH |
| **T.14** | 🟠 P1 | `Tentacle/Registration/TentaclePollingRegistrar.cs` + `ResolvePollUri:148-153` | Server-returned `subscriptionUri` not validated → malformed URL = startup crash via `UriFormatException`. | ✅ (phase8) | `ResolvePollUri` now uses `Uri.TryCreate(..., UriKind.Absolute, ...)`. Three outcomes: valid absolute URI from server → use it; null/blank → deterministic local fallback `poll://{subscriptionId}/`; malformed OR relative → fall back + structured Warning naming the offending value. A buggy server release sending garbage in `subscriptionUri` no longer crashes every fresh-registering agent. 6 new tests including malformed Theory matrix and relative-URI edge case. |
| **T.15** | 🟠 P1 | `Tentacle/Network/PublicHostNameResolver.cs:121-165` | Cloud-metadata calls use `http.Send().Result` and `.Content.ReadAsStringAsync().Result`. Sync-blocking during startup registration → ThreadPool starvation deadlock risk. | ⬜ |
| **T.16** | 🟡 P2 | various | 8 more P2 items (stdbuf, group-read mode, TcpKeepAlive, CleanupOrphanedWorkspaces CreationTime, process-local mutex multi-pod, etc.) | ⬜ |

---

## Section 7 — 🏔️ Architectural Alignment (deferred, multi-week)

| ID | Description | Scope | Estimated effort |
|---|---|---|---|
| **ARCH.1** | **4-phase script protocol** (`Begin/CheckExitCode/CollectLogs/Clean`) with installId correlation token + exit-code file | Matches Octopus TentacleUpgradeMediator approach; solves "agent dead vs still installing" ambiguity | 1-2 weeks |
| **ARCH.2** | **ScriptService V2/V3** with streaming logs + capabilities negotiation | Removes polling overhead for long-running scripts; enables future Kubernetes script service | 2-3 weeks |
| **ARCH.3** | **Tiered timeouts** (15min install / 2min restart / 2min log / 2min clean) | Replaces single 5-min `ScriptTimeout` | 3-5 days |
| **ARCH.4** | **Deployment-conflict guard** — block upgrade when deployment in-flight on same target | Matches Octopus `BlockUpgradeIfDeploymentIsRunning` | 3-5 days |
| **ARCH.5** | **ScriptIsolationLevel.FullIsolation mutex** per machine | Serializes all script dispatches to same machine regardless of source | 2-3 days |
| **ARCH.6** | **K8s Job + ttlSecondsAfterFinished** instead of Pod | Solves orphan-pod cleanup without depending on agent leader election | 3-5 days |
| **ARCH.7** | **Guid-per-attempt ScriptTicket** ✅ (phase6) | Fixes retry idempotency-breaking semantics. `GenerateTicketId` now `Guid.NewGuid().ToString("N")`; stable hash split off into `GenerateMutexName` (used as `IsolationMutexName` so concurrent same-action dispatches still serialise on agent). | DONE |
| **ARCH.8** | **H-19 framework-level fix**: resource-load-then-permission-check | Touches every `[RequiresPermission]` command | 1 week |
| **ARCH.9** | **IScriptService → fully async** with native CT propagation | Large refactor; solves T.3, T.13, E.1 together | 1-2 weeks |

---

## Execution plan (proposed)

### Phase 1 — P0 Security (Week 1, ~3 days)

Quick-win security fixes, each < 1 day, each a separate PR for clean review:

1. **D.1** — Redis `TypeNameHandling.None` (30 min)
2. **D.2** — Team/role commands `ISpaceScoped` (half day)
3. **B.1** — MasterKey fail-fast on empty (1 hour)
4. **T.1** — TLS pin fallback → opt-in env var (1 hour)
5. **C.1** — K8s default image pin digest (1 hour)
6. **T.2** — ticketId whitelist (30 min)
7. **T.8** — Listening ApiKey https enforce (30 min)

Total: **~1.5 days of coding + 1 day of tests/review = 2-3 days**.

### Phase 2 — P0 Data Integrity (Week 2)

1. **T.5** — StartScript mutex leak protection
2. **T.7** — WriteAdditionalFiles rethrow
3. **T.6** — Listening register localhost:7078 hack removal
4. **A.3** — `_batchStates` thread-safety
5. **A.2** — `Task.WhenAll` exception collection
6. **A.1** — Cancel-vs-Fail race
7. **B.2** — Sensitive vars: remove `.key` file + argv password (requires Calamari side too)

Total: **~4-5 days**.

### Phase 3 — P0 Crypto Modernization (Week 3)

1. **B.3** — AES-GCM + random nonce + random salt (with migration)
2. **B.1** follow-up — key rotation support (B.4)
3. **C.2** — Helm credential via Secret projection

Total: **~1 week**.

### Phase 4 — P0/P1 K8s + Auth hardening (Week 4)

1. **C.3** — K8s health check pod-create dry-run
2. **C.4** — Default non-root pod
3. **D.3** — H-19 framework fix (larger; may spill to week 5)
4. **D.4** — SpaceId injection expansion
5. **D.5/D.6** — API key cache + InternalUser id

Total: **~1-1.5 weeks**.

### Phase 5+ — P1 / P2 / ARCH

Batched by area; sequence per triage discussion after P0 is clean.

---

## Working agreements

- Each fix follows strict **Red-Green-Refactor**:
  1. 思維鏈 — analyze the bug deeply, identify all affected paths
  2. 測試用例 — define failing tests for the bug + edge cases
  3. 確認 — confirm the test is genuinely red against current code
  4. Minimal production code to go green
  5. Reverse-verify — temporarily break the fix, confirm test fails
  6. Full suite pass
- Each PR ≤ 5 files touched, ≤ 500 lines total where possible
- During each fix, **look for related issues** in the touched files — add to this tracking doc if found, don't silently fix-and-forget
- Plan can be adjusted mid-flight if a dependency forces reordering

---

## Open questions

1. **Production impact of B.3 migration** — all existing sensitive-variable ciphertexts need to be readable by the new code path. Dual-path decrypt during transition, then force re-encrypt. Coordination with prod deployment required.
2. **T.3 / T.13 / E.1 interdependence** — these three are all facets of the same sync-over-async → CT-broken problem. Best fixed together as ARCH.9, but each has isolated quick-patch value too.
3. **C.2 Helm Secret projection** — needs agent-side + server-side coordination, and possibly a breaking change for operators who extend `HelmUpgradeScriptBuilder`. Plan rollout window.

---

_Last updated: 2026-04-26 (Phase-8 = B.10 + A.6 + D.8 + T.10 + T.14 + E.10 landed). Prior phases: Phase-7 = B.5 + D.5 + D.6 + N3 + A.4. Phase-6 = A.5 + A.11 + ARCH.7 + 2 new items. Phase-5 = B.6 + B.7 + T.11 + T.12. Phase-1..4 = P0s. Update as items land._

_Phase-7 deep-audit follow-up note_: N3 (SshExecutionMutex unbounded `_locks` dict — Agent D in the post-Phase-5 deep audit) closed via reference-counted entries that auto-evict when the last caller releases. 5 new tests pin the contract (LockCount returns to 0 across acquire/dispose, 100 distinct endpoints all evict, waiter-queued / timeout / concurrent-50×20 all leave dict empty).

_Phase-8 polish bundle (2026-04-26)_: closed B.10 (KDF V2 envelope), A.6 (output-var reserved-prefix), D.8 (per-space vs cross-space API split), T.10 (ListenIPAddress env var), T.14 (subscriptionUri fallback), E.10 (Halibut timeouts env vars). 6 P1/P2 items, 67 new tests, all non-breaking with default modes preserving legacy behaviour. Pushes closed-finding count to 36/85.

_Phase-8.7 namespace genericity cleanup (2026-04-26)_: `EndpointVariableFactory` (used by every transport — KubernetesApi, KubernetesAgent, Ssh, Tentacle, OpenClaw — for endpoint JSON deserialization and `VariableDto` construction) was misplaced under `Squid.Core.Services.DeploymentExecution.Kubernetes`, forcing every non-K8s transport's `IEndpointVariableContributor` and `IHealthCheckStrategy` to import the K8s namespace just to read endpoint payloads. Moved to `Squid.Core.Services.DeploymentExecution.Variables` (generic namespace alongside `AccountVariableExpander`). Test file moved from `Services/Deployments/Kubernetes/` → `Services/Deployments/Execution/`. Net effect: 7 non-K8s files dropped a misleading `using Kubernetes;`, 4 K8s-internal files added the new `using Variables;`. No behavioural change. 0 new tests (existing 7 EndpointVariableFactory tests cover; namespace move is verified by green build + green suite). All cross-project totals unchanged: 4712 / 1196 / 139.

_Phase-9a hardening bundle (2026-04-27)_: 11 items, 38 new tests across UnitTests + Tentacle.Tests. Closes a Phase-8.6 crypto-symmetry debt and 4 P0/P1 robustness items identified in the Listening + Polling + Server + OctopusTentacle deep-audits. Items:
  - **9.1 (Phase-8.6 sym debt — P0)**: Tentacle output-masking decryptor was V1-only; server-emitted V2 envelopes silently failed → sensitive values printed verbatim into agent stdout/stderr. Mirrored Calamari's dual-format reader exactly (V1 mode-gated via SQUID_SENSITIVE_VAR_DECRYPT_LEGACY_ACCEPT). 7 new tests.
  - **9.2 (P0)**: SSH cleanup-finally guarantee. Pre-fix, `CleanupRemoteWorkDirectory` sat AFTER the success-return so any exception left workDir + sensitiveVariables.json on the remote forever. Wrapped in try/finally; cleanup helper now `protected internal virtual` for test observation. 3 new tests.
  - **9.3 (P0)**: PollingTrust startup race. `Start()` background load races concurrent registration's `ReconfigureIfMissingAsync` — both call `TrustOnly` with different lists, last writer drops thumbprints. Serialized via `SemaphoreSlim _reconfigureLock`. 3 new tests.
  - **9.4 (P1)**: SSH cancel kill remote process propagation. User cancellation exits local poll loop via OCE but remote bash kept running until SSH session disposed (or forever if `nohup`/`setsid`). Finally block now fires `pkill -TERM -f workDir; sleep 5; pkill -KILL` on cancellation paths. 3 new tests.
  - **9.5 (P1)**: ShutdownDrainTimeoutSeconds default 30s → 300s. 30s killed any 5-30min infra deploy at SIGTERM. New default + Rule 8 const + pin test (operators may match systemd `TimeoutStopSec` to this value).
  - **9.6 (P1)**: Architecture-genericity 3-item cleanup, same pattern as 8.7. `CacheHitStagingHandler` + `FullUploadStagingHandler` moved from generic `Packages/Staging/Handlers/` to `Targets/Ssh/Packages/Staging/` (cast to `SshPackageStagingContext` was hidden coupling). `HalibutMachineExecutionStrategy` moved from `Infrastructure/` to `Targets/Tentacle/Transport/` (Halibut IS the tentacle wire protocol).
  - **9.7 (P1)**: `[CacheResponse(60)]` on `ICapabilitiesService` + `ICapabilitiesServiceAsync`. Pre-fix, every server health-check poll re-read 3 files from agent disk. 60s TTL chosen vs OctopusTentacle's 600s because Squid response carries fresher upgrade-status metadata. 3 new tests pin TTL value + sync/async drift.
  - **9.10 (P1)**: Halibut bump 8.1.1052 → 8.1.1943 (~900 patch versions to match OctopusTentacle main). Forced StackExchange.Redis 2.6.122 → 2.8.58 to close NU1605 downgrade.
  - **9.11 (P1)**: `OrphanMaxAge` env-var override (SQUID_TENTACLE_ORPHAN_WORKSPACE_TTL_HOURS, 1..720h, default 24h) + Prometheus counter `squid_tentacle_orphaned_workspaces_cleaned_total`. Pre-fix value was hardcoded; high-throughput operators couldn't tighten cleanup window. 3 new tests.
  - **9.15 (P1)**: Polling startup jitter (SQUID_TENTACLE_POLLING_STARTUP_JITTER_MS, 0..300000ms, default 0). Each Tentacle sleeps 0..N ms before first Poll() call → reconnect storms after server restart spread across the fleet instead of arriving in a single instant. Default 0 preserves backward compat. 13 new tests.

Phase-9a deferred to Phase-9b: 9.8 (`IFileTransferService` contract), 9.9 (`tentacle check-services` CLI), 9.12 (`BackwardsCompatibleCapabilitiesV2Helper` for too-old agents), 9.13 (Tentacle SIGHUP config reload — touch cert rotation), 9.14 (server-side polling work-queue bounded). All require larger surface changes; bundled separately to keep Phase-9a small + mergeable.

_Phase-9b Tentacle-contracts bundle (2026-04-27)_: 5 items, ~50 new tests. Closes the OctopusTentacle alignment gaps + a P1-rated server OOM defence missing from Phase-9a:
  - **9b.1 / item 9.14 (P1 OOM defence)**: server-side polling-work admission gate. Pre-fix, an offline polling Tentacle could absorb 1000+ queued dispatches — Halibut's pending-request queue grew unbounded → OOM. New `HalibutPollingWorkAdmission` static gate with per-machine in-flight ConcurrentDictionary, atomic TryAdmit/Release with clamp-at-0. Wired into `HalibutMachineExecutionStrategy.ExecuteScriptAsync` BEFORE Halibut client creation; try/finally release on every exit path. Operator override via `SQUID_HALIBUT_MAX_PENDING_WORK_PER_AGENT` (default 100). Per-machine isolation. New `PollingWorkAdmissionExceededException` with operator-actionable message. 16 unit + 1 integration tests; xUnit Collection serializes the static state across both test classes.
  - **9b.2 / item 9.12 (P1 protocol resilience)**: `BackwardsCompatibleCapabilitiesClient` upgraded from substring-only matching to typed-exception detection. Halibut 8.1.1943 hierarchy: `NoMatchingServiceOrMethodHalibutClientException` (base) ← `ServiceNotFoundHalibutClientException` / `MethodNotFoundHalibutClientException` / `AmbiguousMethodMatchHalibutClientException`. Type-based check is primary; substring fallback retained for older Halibut versions. CRITICAL: explicit-exclude `AmbiguousMethodMatchHalibutClientException` — server-side wiring bug must rethrow rather than silently mask as "old agent". 4 new tests (3 typed-exception positive, 1 negative for Ambiguous).
  - **9b.3 / item 9.8 (P1 OctopusTentacle parity gap #1)**: `IFileTransferService` separate file upload/download wire contract. Mirror of OctopusTentacle's pattern: sync wire (`IFileTransferService`), server-side async (`IFileTransferServiceAsync` w/ CT), client-side async (`IAsyncClientFileTransferService` no CT — Halibut method-shape limitation). Agent impl `LocalFileTransferService` writes under `~/.squid/uploads` with workspace-boundary path rewriting (rooted / `..` traversal / Windows backslash all rewritten to `rewritten-{sha256-prefix}` inside upload root + defence-in-depth post-`Path.Combine` symlink check). Hash-as-we-write: bytes flow through SHA-256 CryptoStream before disk so server can verify integrity AND mid-stream corruption is detected. Registered in `TentacleHalibutHost.DelegateServiceFactory`. Server-side `IHalibutClientFactory.CreateFileTransferClient`. 16 new tests including theory-driven malicious-path matrix.
  - **9b.4 / item 9.13 (P1 zero-downtime cert rotation)**: SIGHUP-driven trust-list hot reload. New `TentacleHalibutHost.ReloadTrustList(string)` — atomic `HalibutRuntime.TrustOnly` replace; defensive null/empty/malformed input is no-op + warn (refuses to wipe trust silently). New `HookConfigReloadOnSighup(Func<string>)` — `PosixSignalRegistration.Create(SIGHUP, ...)` calls reload on signal, `ctx.Cancel = true` so process doesn't terminate. Linux/macOS only — Windows operators get a `NoOpDisposable` (Phase-9b.5's `tentacle service reload` is the platform-portable alternative). Operator workflow: edit config → `kill -HUP $(pgrep squid-tentacle)` or `systemctl reload squid-tentacle`. 6 new tests.
  - **9b.5 / item 9.9 (P1 operator self-diagnostic)**: `tentacle check-services` CLI command. Mirrors OctopusTentacle's `check-services`. Five checks (Configuration, Certificate, IScriptService, ICapabilitiesService, IFileTransferService) each returning (Name, Pass, Detail) with exit code 0 (all pass) / 1 (any fail). Internal `RunChecks(settings)` for testing without Console output. Wired into command resolver between `show-config` and `new-certificate` (the natural diagnostic flow). 7 new tests.

Phase-9b cumulative: closes 5/5 deferred OctopusTentacle gaps. Cross-project totals after Phase-9b: **4745 / 1250 / 139 = 6134 tests, 0 failures** (Phase-9a baseline 6084 + 50 new this bundle: 17 admission gate + 4 typed-exception + 16 file-transfer + 6 SIGHUP reload + 7 check-services).

_Phase-10 P0 security closeout (2026-04-28)_: 3 P0 items, ~32 new tests. Closes the audit's only TRUE cross-space privesc vector + 2 silent-failure / data-leak categories.
  - **10.1 / item C.3 (P0 silent-failure)**: K8s agent RBAC dry-run health check. Pre-fix, an agent whose ServiceAccount RBAC was revoked / namespace was deleted reported "healthy" via Halibut polling — first deploy then failed with cryptic kubectl Forbidden error. New `KubernetesRbacInspector` (agent-side) shells out to `kubectl auth can-i create <resource>` per deploy-critical resource (pods / configmaps / secrets) at startup, surfaces results via Capabilities metadata. Server-side `KubernetesAgentHealthCheckStrategy.InspectRbacMetadata` reads keys; ANY denied resource → unhealthy with operator-actionable detail naming the specific permission. K8s-only (detected via `KUBERNETES_SERVICE_HOST` env) — non-K8s tentacles unaffected. Backward compat: pre-Phase-10.1 agents without metadata fall back to legacy "Halibut reachable = healthy" path. Constants pinned (Rule 8) on BOTH sides — drift would silently break the check. 12 new tests (7 agent-side + 5 server-side).
  - **10.2 / item C.2 (P0 secret leak)**: Helm secret projection via `--values` file replaces `--set` argv emission. Pre-fix, `HelmUpgradeScriptBuilder` emitted `--set "key=value"` putting full value text into helm process argv → visible to ps aux, /proc/<pid>/cmdline, kubelet logs, Sysdig/Falco audit, bash trace logs. For ANY value an operator marks sensitive (DB password, API key, TLS private key), this was unacceptable secret exposure. Fix: project ALL inline values to a generated `inline-values.yaml` file referenced via `--values` (which goes LAST in the chain to preserve `--set` precedence semantics). Generated file lives in per-task workspace (already 0700 via Phase-9.2 cleanup-finally), is wiped by existing finally block, NEVER appears in argv. Dotted keys produce nested YAML structure (matches helm's standard `--set` dot-notation). 6 new tests + 0 regressions across 123 existing Helm-related tests.
  - **10.3 / item D.3 / H-19 (P0 cross-space privesc)**: header SpaceId now membership-checked. The audit's ONLY true cross-space privilege-escalation surface — pre-fix, a user with team membership in Space-1 could send `X-Space-Id: 2` HTTP header, the SpaceIdInjectionSpecification trusted the header verbatim, downstream authorization checked permissions against the spoofed SpaceId, user could read/mutate Space-2 resources despite zero membership. Fix: new `SpaceMembershipSpecification` runs BEFORE the injection spec; reads X-Space-Id, validates user membership via `ISpaceMembershipResolver` (TeamMember → Team → Space chain), throws `CrossSpaceAccessDeniedException` with structured (UserId, RequestedSpaceId) for audit-trail correlation. Internal users bypass via `IsInternal` (Phase-7 D.6 trusted-context pattern, NOT Id-equals-8888 heuristic). Null UserId throws with -1 placeholder (Phase-7 D.6 fail-closed posture). Missing/malformed header falls through (no claim to gate). 11 new tests (4 resolver + 7 middleware). Note: per-resource SpaceId derivation (load entity → check entity.SpaceId matches user's permitted spaces) is a separate, larger refactor tracked as a future ARCH item — Phase-10.3 closes the immediate header-trust vector.

Phase-10 cumulative: closes 3/3 P0 audit findings (D.3, C.2, C.3). Cross-project totals after Phase-10: **4768 / 1257 / 139 = 6164 tests, 0 failures** (Phase-9b baseline 6134 + 30 new this bundle: 12 RBAC + 6 Helm + 12 SpaceMembership). Closes 55/85 findings (65%) — full Phase-9 + 10 closes 19 items.

_Phase-11 ARCH.9 Plan A: agent-side sync-over-async + soft-cancel propagation (2026-04-28)_: 17 new tests. Closes the F1.1 + F1.2 audit findings flagged in the ARCH.9 surface map (T.3 / T.13 / E.1 cluster). Wire contract IScriptService stays V1 sync — **no breaking change**. Internal async work now actually responds to CancelScript intent.
  - **11.1 — `ScriptCancellationRegistry`**: per-ticket `CancellationTokenSource` registry that bridges the wire-level "no CT" limitation with internal async work. `StartScript` calls `GetOrCreate(ticket)` to obtain a soft-cancel token, threads it into mutex acquire + file save. `CancelScript` calls `Cancel(ticket)` to flip the CTS — in-flight async work observes the cancellation. `CompleteScript` / `CancelScript` call `Cleanup(ticket)` to dispose the CTS. Out-of-order RPC defence: if Cancel arrives BEFORE the matching `GetOrCreate` (server cancelled before agent registered), an `EarlyCancelledSentinel` placeholder makes the eventual GetOrCreate yield an already-cancelled token. 9 new tests including high-concurrency stress.
  - **11.2 (audit F1.1) — sync-over-async mutex acquire eliminated**: pre-fix `LocalScriptService:125` was `_isolationMutex.AcquireAsync(command).GetAwaiter().GetResult()` — burned a Halibut RPC thread on a Task.Delay-based async polling loop with allocation churn + threadpool starvation under contention. New `ScriptIsolationMutex.TryAcquireBlocking` is pure-sync polling using `WaitHandle.WaitOne` — no Task allocation, no async state machine, same observable behaviour (100ms→1s exponential poll, deadline check). Optionally accepts CT so a CancelScript mid-acquire short-circuits via `OperationCanceledException` instead of waiting the full configured isolation timeout. 4 new tests.
  - **11.3 (audit F1.2) — hardcoded `CancellationToken.None` on file save replaced**: pre-fix `LocalScriptService:754` was `file.Contents.Receiver().SaveToAsync(tempPath, CancellationToken.None)` — a 1GB sensitiveVariables.json or large package payload would write to completion regardless of CancelScript. Now `WriteAdditionalFiles` takes a `CancellationToken` (default arg preserves backward compat); threaded into both loop-top check (between files) AND inner `SaveToAsync` (mid-stream cancel of a single very-large file aborts via `CopyToAsync`'s CT plumbing). Defence-in-depth: both layers retained. 4 new tests.
  - **11.4 — CancelScript wired to flip the registry CTS**: `LocalScriptService.CancelScript` now calls `_cancellationRegistry.Cancel` BEFORE `_scripts.TryRemove`. Signals any in-flight async work that's NOT yet tracked in `_scripts` (StartScript still mid-acquire or mid-write). Both `CompleteScript` and `CancelScript` call `_cancellationRegistry.Cleanup` to release the per-ticket CTS so the registry doesn't accumulate over agent lifetime.
  - **11.5 — `ScriptPodService` already uses `TryAcquire` (no GetAwaiter().GetResult())**: confirmed no-op via grep audit. The K8s-pod-tentacle path was already non-blocking; ARCH.9 sync-over-async finding was specific to the polling-tentacle path.

**Plan B — V2 wire contract with first-class CT — DEFERRED** to a future phase that bundles E.2 capability dispatch + version negotiation. Plan A's no-wire-break scope intentionally leaves F2.x (CancellationToken discarded in `AsyncScriptServiceAdapter.StartScriptAsync`) — that's a Halibut V1 design limitation that requires V2 wire to fix. The soft-cancel registry is the bridge until then; once Plan B lands, the registry can be retired or re-purposed.

Phase-11 cumulative: closes 1/1 ARCH P0 finding (ARCH.9 Plan A scope). Cross-project totals after Phase-11 + pre-push audit follow-up: **4773 / 1277 / 139 = 6189 tests, 0 failures** (Phase-10 baseline 6169 + 20 new this bundle).

_Phase-11 pre-push audit follow-up (2026-04-28)_: independent paranoid agent caught 2 hidden risks before merge (matches the discipline applied in Phase-9a / Phase-10 audit-fixes):
  - **Audit Issue #1 — CompleteScript early-return CTS leak**: the leak path is StartScript registers with the ScriptCancellationRegistry BEFORE _scripts.TryAdd; if a failure between those steps leaves the registry populated but _scripts empty, a subsequent CompleteScript on that ticket hits the early-return path. Pre-fix, that path didn't call Cleanup → orphaned CTS leaked. Fix: add `_cancellationRegistry.Cleanup(command.Ticket)` before the early return, matching the existing CancelScript pattern (Cleanup on every exit path).
  - **Audit Issue #2 — Null-ticket NRE in CancelScript / CompleteScript**: StartScript validated `command.ScriptTicket != null` and threw ArgumentException with structured message; the other two RPC handlers didn't, so a malformed RPC with null Ticket propagated into the registry which dereferences `ticket.TaskId` → unhandled NRE → opaque Halibut wrap → operator saw cryptic generic error. Fix: explicit null-check + ArgumentException in both, matching StartScript's defensive pattern. 3 additional tests pin both fixes (leak-path call-site + 2 null-ticket validations).

Net: 47/85 findings closed (55%). Cross-project final totals: **4724 / 1221 / 139 = 6084 tests, 0 failures** (Phase-8.7 baseline 6047 + 37 new tests this bundle).
