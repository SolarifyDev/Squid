{{- if not .Values.scriptPod.rbac.useNamespacedRoles }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "squid-tentacle.fullname" . }}-script
  labels:
    {{- include "squid-tentacle.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "squid-tentacle.fullname" . }}-script
subjects:
- kind: ServiceAccount
  name: {{ include "squid-tentacle.script-sa-name" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
