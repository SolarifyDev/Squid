Squid Tentacle has been deployed.

Tentacle Pod:
  kubectl get pods -l {{ include "squid-tentacle.selectorLabels" . | replace ": " "=" | replace "\n" "," }}

Logs:
  kubectl logs -l {{ include "squid-tentacle.selectorLabels" . | replace ": " "=" | replace "\n" "," }}

Script Pods:
  kubectl get pods -l app.kubernetes.io/managed-by=squid-tentacle

Verify registration:
  The tentacle will register with the Squid Server at {{ .Values.tentacle.serverUrl }}
  and begin polling on port {{ .Values.tentacle.serverPollingPort }}.
