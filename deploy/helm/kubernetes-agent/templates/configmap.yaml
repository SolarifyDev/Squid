apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubernetes-agent.fullname" . }}-config
  labels:
    {{- include "kubernetes-agent.labels" . | nindent 4 }}
data:
  appsettings.json: |
    {
      "Tentacle": {
        "ServerUrl": "{{ .Values.tentacle.serverUrl }}",
        "ServerCommsUrl": "{{ .Values.tentacle.serverCommsUrl }}",
        "Roles": "{{ .Values.tentacle.roles }}"
      },
      "Kubernetes": {
        "Namespace": "{{ .Values.kubernetes.namespace }}",
        "UseScriptPods": true,
        "ScriptPodImage": "{{ .Values.scriptPod.image }}",
        "ScriptPodTimeoutSeconds": {{ .Values.scriptPod.timeoutSeconds }}
      }
    }
